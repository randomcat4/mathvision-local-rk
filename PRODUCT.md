# Product

## Register

product

## Users

面向需要长时间监督高强度数学研究的研究者、审稿者与 RK 开发者。用户在桌面环境中同时观察主实例、子实例、证明器与人工审查的进展，并随时进入单个研究工作项查看完整记录。

## Product Purpose

把 RK 编排内核正在发生的研究过程投影为可观察、可追溯、可人工干预的实时研究图。产品必须同时表达顺序执行、并行分叉、依赖汇合、条件门、否决路线、修复循环、动态子问题以及人工暂停与恢复，并保存失败路线而不是用最终结果覆盖历史。

## Brand Personality

精密、克制、可信。它应当像长期运行的科学仪器，而不是通用 SaaS 仪表盘或娱乐化科幻界面。

## Anti-references

- 不延续 MathVision 的组件、接口或品牌结构。
- 不使用满屏霓虹、玻璃拟态、科幻 HUD 装饰或所有节点同时闪烁。
- 不把聊天消息误画成研究节点，也不把运行完成冒充数学验证完成。
- 不隐藏被否决、失败或暂停的路线。

## Design Principles

- 视觉上是一棵稳定生长的树，语义上是一张允许汇合与修复回链的研究图。
- 动画只表达真实状态变化；正在工作的少量节点和路径才获得光效。
- 一个节点对应一个可持久化研究工作项，完整聊天与工件属于节点内部。
- 历史不可覆盖：修复产生新尝试，否决路线保留原位。
- 前端通过稳定的研究运行 Interface 消费快照与增量事件，不绑定具体后端实现。

## Accessibility & Inclusion

以 WCAG 2.2 AA 为基线；所有状态同时使用文字或图标表达，不依赖颜色。支持键盘操作、清晰焦点、200% 缩放和 `prefers-reduced-motion`，在减少动态效果时关闭呼吸、流光和移动光粒。

## Current Vertical Slice

`/rk/researches/gao-live` 是第一个不依赖 MathVision 组件运行的 RK 原生页面。它通过 `ResearchRuntime` 接口读取本地 SQLite 中的 Gao 历史研究：176 个节点、318 条依赖边、549 条节点记录。历史快照保持只读；实时暂停、恢复与增量事件将在同一接口上由 RK 内核适配器提供。

当前页面支持独立画布拖拽、滚轮缩放、真实适配全图、节点搜索定位、悬浮摘要与节点详情。显示设置保存在本机，提供深色/白天模式和六套可选运行光效；光效只改变活动节点与路径，不改变研究状态的语义颜色。尚未接通的能力必须明确呈现为后续接口工作：真实 RK 增量事件、可写的暂停/恢复、条件门决策以及节点工件中的 PDF 预览。

2026-08-18 的桌面 Chrome 基线：SQLite 读取约 50 ms，分层布局约 119 ms，首个文字约 180 ms。大图模式取消全节点入场动画，边层不随节点选择重绘，长聊天使用延迟可见渲染。该基线是本机开发数据，不作为低端设备或远程网络承诺。
