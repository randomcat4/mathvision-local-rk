# 路线图

状态：计划，不代表已经实现。

## P0：冻结私有恢复基线

交付：

- 完整恢复源码、原件链、报告和工具进入私有仓库；
- `node_modules` 与构建产物不入库；
- 完整验证可在干净安装后重放；
- 当前恢复状态和第三方边界有明确文档。

停止门：无法在干净环境构建，或恢复来源链不完整时，不进入产品改造。

## P1：收口前端基础设施

- 建立稳定路由和页面边界；
- 把全局 fetch 拦截收口为可替换适配器；
- 逐个拆出 7 个大型恢复视图；
- 合并重复数据映射，保持行为回归测试。

停止门：任何拆分导致现有本地功能或视觉明显漂移时，先恢复等价性。

## P2：研究线程模型

- 实现 `ResearchThread`、消息 revision、父子分叉和恢复；
- 将聊天与 WorkItem/Attempt 映射；
- 建立线程级活动、错误和费用记录；
- 实现研究线程图。

## P3：RK 只读接入

- 接入 ResearchCatalog、ResearchSnapshot、Activity 和事实图查询；
- 展示 VERIFIED 与 RESEARCH_HISTORY；
- 支持依赖闭包和反向影响闭包；
- 所有不可用状态保持显式。

## P4：RK 命令和工具闭环

- 接入带版本栅栏的命令总线；
- 接入本地模型、Lean、Z3、Python、CAS 和枚举；
- 将运行结果保存为工件和候选材料；
- 完成独立验证、审查和 Claim 提升流程。

## P5：写作与发布

- 将 LaTeX workspace 绑定 RK 候选 TeX；
- 支持审查任务、编译日志和最终 PDF；
- 从线程和 Claim 双向追溯论文段落。

## P6：本地产品化

- 选择桌面壳或本地守护进程；
- 实现备份、迁移、恢复和数据导出；
- 完成跨平台安装与升级；
- 建立性能、崩溃恢复和长任务验收。

