import {
  E as Yt,
  p as Ns,
  r as a,
  t as W,
  ai as Y,
  g as we,
  j as h,
  B as U,
  T as G,
  aw as xn,
  C as pt,
  a6 as Bs,
  V as Ws,
  X as _s,
  aD as Br,
  c4 as mt,
  _ as Pe,
  S as Ie,
  h as it,
  bv as Fs,
  b2 as Xs,
  ac as zs,
  ad as Hs,
} from "./index-BM3ZINIl.js";
import { a as $s, b as Us } from "./useAttachmentContent-15uaQfvd.js";
import { u as Wr } from "./useControlOrMetaShortcut-DL7j4WBM.js";
import { a as _r } from "./useResizablePanelValue-D_NwbF9E.js";
import {
  v as Vs,
  w as Gs,
  e as wn,
  x as Ks,
  i as Ot,
  u as Qs,
  b as Ys,
  a as Js,
  y as Zs,
  z as qs,
  n as eo,
  m as to,
  A as ro,
  B as no,
  C as so,
  E as oo,
  F as Fr,
  p as ao,
  G as io,
  h as lo,
  k as co,
  f as uo,
  H as fo,
  I as ho,
  J as po,
  K as mo,
  M as go,
  N as vo,
  g as xo,
} from "./useLatexQueries-DBpKOPUv.js";
import {
  F as wo,
  g as Co,
  l as yo,
  L as bo,
  a as zt,
  v as So,
  i as ko,
  b as Xr,
  c as jo,
  p as Io,
  w as Nt,
  d as Ro,
  e as Lo,
  f as Eo,
} from "./LatexWorkspaceCanvas-DKG1oD4d.js";
import {
  P as Cn,
  E as Ce,
  a as gt,
  C as yn,
  S as Je,
  b as Po,
  c as To,
  d as Jt,
  D as be,
  V as Ao,
  F as bn,
  g as Do,
  R as Ze,
  W as Sn,
  G as Mo,
  e as Oo,
  f as zr,
  m as No,
  h as Ht,
  i as Bo,
  r as Wo,
  j as _o,
  k as Fo,
  l as Xo,
} from "./source-editor-CGMzw50e.js";
import { d as vt } from "./downloadBlob-CusuPFei.js";
import { a as Ge, g as Zt } from "./cache-BNzcfneg.js";
import { b as zo, a as kn } from "./latexNaming-BAjxoZ-G.js";
import { u as Ho, O as Hr, o as $o } from "./useLatexProjectCreation-BfZAUJk-.js";
import { M as jn, D as qe, a as et, b as tt, c as Ct } from "./MChip-DiH8NXBU.js";
import { D as In } from "./Divider-BcuihGce.js";
import { T as Uo, a as Vo } from "./MarkdownHighlights-C-ZhT8z1.js";
import { F as Go } from "./file-plus-corner-CfeYwU0X.js";
import { H as Ko } from "./history-7HBMn_Gr.js";
import { T as Qo } from "./TimeRelative-HYP_-RcH.js";
import { D as Yo } from "./Drawer-ByPJ5WV0.js";
import { G as Bt } from "./SidebarTreeConnector-DsRNfBIo.js";
import { C as Jo } from "./circle-check-B9fquZbm.js";
import { u as Zo } from "./useBlocker-Cad78cFE.js";
import { A as qt } from "./Alert-DnFVD8li.js";
import { a as qo } from "./file-input-BGVdGffy.js";
import { L as ea, a as ta, b as ra, c as na, d as sa } from "./dialogs-D-Okwmpa.js";
import { S as oa } from "./Slider-v2yN6a4G.js";
import { T as xt, L as aa } from "./TextField-D8vc_sXz.js";
import { a as ia } from "./latexProjectChatMessages-Cty9Jluh.js";
import { g as la, s as ca } from "./latexWorkspacePanelVisibility-BotBQ6b-.js";
import "./useQuery-DM2qiUYb.js";
import "./attachmentsApi-BHgxfYft.js";
import "./authFetch-DXjDztUP.js";
import "./search-CE89ACSU.js";
import "./useMutation-C1SgG9wr.js";
import "./useInfiniteQuery-CdNTitqC.js";
import "./file-plus-_oOf3yl8.js";
import "./upload-Df6L5ERk.js";
import "./code-xml-DmfSENe3.js";
import "./play-3zl_nde-.js";
import "./save-Bre0AB4-.js";
import "./ListItemIcon-CaLBil7Q.js";
import "./square-D0gg5_xj.js";
import "./pencil-DHia85Iy.js";
import "./CopyButton-BcOsqrNJ.js";
import "./triangle-alert-C5eP7BuB.js";
import "./index-D0EP5Hzg.js";
import "./usePdfDocument-D5PFNoxR.js";
import "./pdfDocument-V1r75ju6.js";
import "./usePdfObjectUrl-Cd7ol8-S.js";
import "./chevron-left-BwEFN4zp.js";
import "./katex.min-ADdKj7O-.js";
import "./minimize-2-DiBwEVni.js";
import "./maximize-2-B07z4fzS.js";
import "./LoadingSection-wWBu2CqJ.js";
import "./eraser-B4pclN2p.js";
import "./arrow-right-DvMAP92m.js";
import "./quote-DYxVpGUS.js";
import "./grip-vertical-DCI0_0hx.js";
import "./latexTextPatterns-B444ow6Q.js";
import "./index-Yb6ONXKu.js";
import "./file-code-corner-DV79SyC0.js";
import "./OpenInNewRounded-D-7V5TzF.js";
import "./FormControlLabel-CQvCddOM.js";
/**
 * @license lucide-react v1.16.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */ const ua = [
    [
      "path",
      { d: "M10.94 5.274A7 7 0 0 1 15.71 10h1.79a4.5 4.5 0 0 1 4.222 6.057", key: "1uxyv8" },
    ],
    ["path", { d: "M18.796 18.81A4.5 4.5 0 0 1 17.5 19H9A7 7 0 0 1 5.79 5.78", key: "99tcn7" }],
    ["path", { d: "m2 2 20 20", key: "1ooewy" }],
  ],
  da = Yt("cloud-off", ua);
/**
 * @license lucide-react v1.16.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */ const fa = [
    [
      "path",
      {
        d: "M11 22H6a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h8a2.4 2.4 0 0 1 1.706.706l3.588 3.588A2.4 2.4 0 0 1 20 8v5",
        key: "1jo35a",
      },
    ],
    ["path", { d: "M14 2v5a1 1 0 0 0 1 1h5", key: "wfsgrz" }],
    ["path", { d: "m15 17 5 5", key: "36xl1x" }],
    ["path", { d: "m20 17-5 5", key: "vdz27y" }],
  ],
  ha = Yt("file-x-corner", fa);
/**
 * @license lucide-react v1.16.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */ const pa = [["path", { d: "M21 12a9 9 0 1 1-6.219-8.56", key: "13zald" }]],
  ma = Yt("loader-circle", pa),
  ga = "latex-editor-font-size-rem-v1",
  Rn = 10,
  Ln = 24,
  En = 16,
  $t = (e) => e / En,
  va = (e) => e * En,
  xa = (e) => {
    const t = JSON.parse(e);
    if (typeof t != "number" || !Number.isFinite(t) || t < $t(Rn) || t > $t(Ln))
      throw new Error("Stored LaTeX editor font size must be between 10px and 24px.");
    return t;
  },
  wa = "ai-math.latex-active-compile-job.v1:",
  er = (e) => `${wa}global:${e}`,
  Ca = (e) => {
    if (!e || typeof e != "object") return !1;
    const t = e;
    return typeof t.jobId == "string" && t.jobId.trim().length > 0 && typeof t.source == "string";
  },
  ya = (e) => {
    if (typeof window > "u") return null;
    try {
      const t = window.localStorage.getItem(er(e));
      if (t === null) return null;
      const r = JSON.parse(t);
      return Ca(r) ? r : null;
    } catch (t) {
      return (console.warn("Ignoring unreadable LaTeX compile job listener.", t), null);
    }
  },
  ba = (e, t) => {
    if (!(typeof window > "u"))
      try {
        window.localStorage.setItem(er(e), JSON.stringify(t));
      } catch (r) {
        console.warn("Could not save LaTeX compile job listener.", r);
      }
  },
  Ne = (e) => {
    if (!(typeof window > "u" || !e))
      try {
        window.localStorage.removeItem(er(e));
      } catch (t) {
        console.warn("Could not clear LaTeX compile job listener.", t);
      }
  },
  $r = [],
  lt = (e) => ({
    success: !1,
    diagnostics: [{ severity: "error", code: "compile-infrastructure", message: e }],
    log: e,
    durationMs: 0,
  }),
  Ut = (e) => {
    const t = Date.parse(e.finishedAt ?? e.lastUpdated);
    if (!Number.isFinite(t)) throw new Error("LaTeX compile job timestamps must be valid dates.");
    return t;
  },
  Sa = (e, t, r) => {
    const o = Ut(e);
    return o !== t ? o > t : (e.documentRevision ?? 0) > (r ?? 0);
  },
  ka = ({
    selectedDocument: e,
    contentVersion: t,
    hasUnsavedChanges: r = !1,
    loadLatestSuccessfulCompile: o = !0,
    sourceOverride: s,
    saveCurrentDocument: n,
  }) => {
    const i = Ns(),
      l = Vs(),
      c = Gs(),
      [f, u] = a.useState(null),
      [d, p] = a.useState(null),
      [g, w] = a.useState(null),
      [S, v] = a.useState(null),
      [E, B] = a.useState(null),
      [k, x] = a.useState(null),
      [m, C] = a.useState(null),
      [O, j] = a.useState(!1),
      N = (e == null ? void 0 : e.id) ?? null,
      _ = a.useRef(N),
      V = a.useRef(t),
      P = a.useRef(null),
      [R, b] = a.useState(null);
    ((_.current = N), (V.current = t));
    const M = (R == null ? void 0 : R.jobId) ?? null,
      y = wn(M, { enabled: !!M }),
      H = Ks(N, { enabled: !!(N && !M && o) }),
      I = y.data,
      X = H.data,
      $ = l.isPending || !!M || Ot(I),
      Q = !!(I != null && I.cancelRequestedAt && Ot(I)),
      q = f !== null && d === t,
      oe = q ? f.diagnostics : $r,
      ae = (f == null ? void 0 : f.diagnostics) ?? $r,
      le = !!g && (r || O || S !== t);
    (a.useEffect(() => {
      if (!N) {
        (u(null), p(null), w(null), v(null), B(null), x(null), C(null), j(!1), b(null));
        return;
      }
      (u(null), w(null), v(null), B(null), x(null), C(null), j(!1));
      const F = ya(N);
      (p((F == null ? void 0 : F.source) ?? null), b(F ? { documentId: N, jobId: F.jobId } : null));
    }, [N]),
      a.useEffect(() => {
        if (!(!R || R.documentId !== N || !I)) {
          if (I.documentId && I.documentId !== R.documentId) {
            (b(null), Ne(R.documentId));
            const F = "The LaTeX compile job does not belong to the selected project.";
            (u(lt(F)), W.error(F));
            return;
          }
          if (I.status === "completed") {
            if ((b(null), Ne(R.documentId), !I.result)) {
              const F = "LaTeX compile job finished without a result.";
              (u(lt(F)), W.error(F));
              return;
            }
            (u(I.result),
              I.result.success &&
                (w(I.result),
                v(d),
                B(I.documentRevision ?? null),
                x(I.id),
                C(Ut(I)),
                j(!1),
                i.setQueryData(Ge.latestSuccessfulCompileJob(R.documentId), I),
                W.success("LaTeX compiled.")),
              i.invalidateQueries({ queryKey: Ge.compileHistory(R.documentId) }));
            return;
          }
          if (I.status === "failed" || I.status === "cancelled") {
            if ((b(null), Ne(R.documentId), I.status === "failed")) {
              const F = I.errorMessage ?? "The LaTeX compiler infrastructure failed.";
              (u(lt(F)), W.error(F));
            }
            i.invalidateQueries({ queryKey: Ge.compileHistory(R.documentId) });
          }
        }
      }, [I, R, d, i, N]),
      a.useEffect(() => {
        if (!R || R.documentId !== N || !y.isError) return;
        (b(null), Ne(R.documentId));
        const F = "Could not load the LaTeX compile job.";
        (u(lt(F)), W.error(F));
      }, [R, y.isError, N]),
      a.useEffect(() => {
        if (!(
          !N ||
          M ||
          !(X != null && X.result) ||
          X.documentId !== N ||
          (k !== null && k !== X.id && m !== null && !Sa(X, m, E))
        )) {
          if (k === X.id) {
            const F = X.isStale ?? !1;
            F !== O && (j(F), F && v(null));
            return;
          }
          (w(X.result),
            x(X.id),
            C(Ut(X)),
            v(X.isStale ? null : V.current),
            B(X.documentRevision ?? null),
            j(X.isStale ?? !1),
            !f && !X.isStale && (u(X.result), p(V.current)));
        }
      }, [M, f, m, E, k, O, X, N]));
    const fe = a.useCallback(
        (F = N) => {
          (u(null), p(null), w(null), v(null), B(null), x(null), C(null), j(!1), b(null), Ne(F));
        },
        [N],
      ),
      ce = a.useCallback(
        async (F = !1, A = !1) => {
          if (!(!e || $))
            try {
              const L = V.current,
                D = P.current;
              if (A && (D == null ? void 0 : D.documentId) === e.id && D.contentVersion === L)
                return;
              P.current = { contentVersion: L, documentId: e.id };
              const z = await n("compile"),
                ne = await l.mutateAsync({
                  documentId: e.id,
                  input: {
                    ...(s !== void 0 ? { source: s } : {}),
                    expectedRevision: z.revision,
                    compiler: z.compiler,
                    clean: F,
                  },
                });
              (ba(e.id, { jobId: ne.id, source: L }),
                _.current === e.id && (p(L), b({ documentId: e.id, jobId: ne.id })));
            } catch (L) {
              const D = P.current;
              ((D == null ? void 0 : D.documentId) === e.id &&
                D.contentVersion === V.current &&
                (P.current = null),
                W.error(L instanceof Error ? L.message : "Could not compile LaTeX."));
            }
        },
        [l, $, n, e, s],
      ),
      he = a.useCallback(async () => {
        if (!R) return;
        const { documentId: F, jobId: A } = R;
        try {
          const L = await c.mutateAsync({ jobId: A });
          if ((i.setQueryData(Ge.compileJob(A), L), Ot(L))) {
            W.success("LaTeX cancellation requested.");
            return;
          }
          (Ne(F),
            b((D) => ((D == null ? void 0 : D.documentId) === F && D.jobId === A ? null : D)),
            i.invalidateQueries({ queryKey: Ge.compileHistory(F) }),
            W.success("LaTeX compilation cancelled."));
        } catch (L) {
          W.error(L instanceof Error ? L.message : "Could not cancel the LaTeX compilation.");
        }
      }, [R, c, i]);
    return {
      compileResult: f,
      diagnostics: oe,
      allDiagnostics: ae,
      pdfResult: g,
      pdfJobId: k,
      pdfDocumentRevision: E,
      isPdfStale: le,
      activeCompileJobId: M,
      isCompilingLatex: $,
      isCancellingLatex: c.isPending || Q,
      isCompileResultCurrent: q,
      compileCurrentDocument: ce,
      cancelCurrentCompile: he,
      resetCompileState: fe,
    };
  },
  ja = (e) => {
    const t = e.toLowerCase();
    return t.endsWith(".tex")
      ? `% New LaTeX file
`
      : t.endsWith(".bib")
        ? `% Add bibliography entries here.
`
        : t.endsWith(".sty")
          ? `\\NeedsTeXFormat{LaTeX2e}
\\ProvidesPackage{custom}
`
          : t.endsWith(".cls")
            ? `\\NeedsTeXFormat{LaTeX2e}
\\ProvidesClass{custom}
`
            : "";
  },
  Ia = ({
    createProjectFolderId: e,
    documents: t,
    files: r,
    hasUnsavedChanges: o,
    selectedDocument: s,
    selectedFile: n,
    onDocumentIdChange: i,
    onRebaseDraft: l,
    onResetCompileState: c,
    onResetDraft: f,
    onSelectTextFile: u,
    saveCurrentDocument: d,
    updateDocument: p,
    updateFile: g,
  }) => {
    const w = Qs(),
      S = Ys(),
      v = Js(),
      E = Zs(),
      B = qs(),
      k = eo(),
      x = to(),
      m = ro(),
      [C, O] = a.useState(!1),
      [j, N] = a.useState(!1),
      [_, V] = a.useState(!1),
      P = a.useCallback(async () => {
        o && (await d());
      }, [o, d]),
      R = a.useCallback((A) => i(A.id), [i]),
      { createProject: b, isCreatingProject: M } = Ho({ beforeCreate: P, onCreated: R }),
      y = a.useCallback(
        async (A, L, D) => {
          const z = Y(L, D);
          if (!A) {
            W.error(z);
            return;
          }
          try {
            await S.mutateAsync({ documentId: A.id, expectedRevision: A.revision });
          } catch (ne) {
            W.error(
              `${z} The partially created project could not be removed: ${Y(ne, "project cleanup failed")}`,
            );
            return;
          }
          W.error(z);
        },
        [S],
      ),
      H = a.useCallback(async (A) => !!(await b({ ...A, folderId: e })), [e, b]),
      I = a.useCallback(async () => {
        if (!s) return;
        O(!0);
        let A = null;
        try {
          const L = await d(),
            D = await x.mutateAsync({ documentId: L.id }),
            z = await w.mutateAsync({
              input: {
                name: zo(t, s.name),
                source: s.source,
                compiler: s.compiler,
                folderId: s.folderId,
              },
            });
          A = { id: z.id, revision: z.revision };
          const ne = await k.mutateAsync({
            documentId: z.id,
            file: new File([D], "project-copy.zip", { type: "application/zip" }),
            expectedRevision: z.revision,
            replace: !0,
          });
          ((A = { id: ne.id, revision: ne.revision }),
            await i(ne.id),
            W.success("Project copied."));
        } catch (L) {
          await y(A, L, "Could not copy the LaTeX project.");
        } finally {
          O(!1);
        }
      }, [w, t, x, k, i, y, d, s]),
      X = a.useCallback(
        async (A) => {
          if (!s) return !1;
          N(!0);
          try {
            const L = kn(A);
            if (L === s.name) return !0;
            const D = await d();
            return (
              await p({ documentId: D.id, input: { name: L, expectedRevision: D.revision } }),
              f(),
              W.success("Project renamed."),
              !0
            );
          } catch (L) {
            return (W.error(Y(L, "Could not rename the project.")), !1);
          } finally {
            N(!1);
          }
        },
        [f, d, s, p],
      ),
      $ = a.useCallback(
        async (A) => {
          if (!(!s || A === s.compiler))
            try {
              const L = await d(),
                D = await p({
                  documentId: L.id,
                  input: { compiler: A, expectedRevision: L.revision },
                });
              l(D.revision);
            } catch (L) {
              W.error(Y(L, "Could not change the compiler."));
            }
        },
        [l, d, s, p],
      ),
      Q = a.useCallback(
        async (A, L) => {
          if (!s) return !1;
          try {
            const D = await d(),
              z = await v.mutateAsync({
                documentId: s.id,
                input: {
                  path: L,
                  kind: A,
                  ...(A === "text" ? { content: ja(L) } : {}),
                  expectedRevision: D.revision,
                },
              });
            return (
              l(z.documentRevision),
              z.file.kind === "text" && (await u(z.file)),
              W.success(A === "folder" ? "Folder created." : "File created."),
              !0
            );
          } catch (D) {
            return (W.error(Y(D, "Could not create the project item.")), !1);
          }
        },
        [v, l, u, d, s],
      ),
      q = a.useCallback(
        async (A, L) => {
          if (!s) return !1;
          try {
            const D = await d(),
              z = await g({
                documentId: s.id,
                fileId: A.id,
                input: { path: L, expectedRevision: D.revision },
              });
            return (l(z.documentRevision), W.success("Project path updated."), !0);
          } catch (D) {
            return (W.error(Y(D, "Could not rename the project item.")), !1);
          }
        },
        [l, d, s, g],
      ),
      oe = a.useCallback(
        async (A) => {
          if (s)
            try {
              const L = await d(),
                D = await p({
                  documentId: s.id,
                  input: { mainFilePath: A.path, expectedRevision: L.revision },
                });
              (l(D.revision), W.success(`${A.path} is now the main document.`));
            } catch (L) {
              W.error(Y(L, "Could not change the main document."));
            }
        },
        [l, d, s, p],
      ),
      ae = a.useCallback(
        async (A) => {
          if (s)
            try {
              const L = await d(),
                D = await B.mutateAsync({
                  documentId: s.id,
                  files: A,
                  expectedRevision: L.revision,
                });
              (l(D.documentRevision),
                W.success(`${A.length} ${A.length === 1 ? "file" : "files"} uploaded.`));
            } catch (L) {
              W.error(Y(L, "Could not upload project files."));
            }
        },
        [l, d, s, B],
      ),
      le = a.useCallback(
        async (A, L) => {
          if (!s) return !1;
          try {
            const D = await d(),
              z = await k.mutateAsync({
                documentId: s.id,
                file: A,
                expectedRevision: D.revision,
                replace: L.replace,
              });
            return (
              l(z.revision),
              L.replace && f(),
              W.success(
                L.replace
                  ? "Current project replaced from ZIP."
                  : "ZIP files merged into the project.",
              ),
              !0
            );
          } catch (D) {
            return (W.error(Y(D, "Could not import the ZIP project.")), !1);
          }
        },
        [k, l, f, d, s],
      ),
      fe = a.useCallback(async () => {
        if (s)
          try {
            const A = await d(),
              L = await x.mutateAsync({ documentId: A.id });
            vt(L, `${A.name}.zip`);
          } catch (A) {
            W.error(Y(A, "Could not export the project."));
          }
      }, [x, d, s]),
      ce = a.useCallback(async () => {
        if (!s) return;
        if (r.filter((D) => D.kind !== "folder").length > Hr) {
          W.error(
            `Overleaf accepts at most ${Hr} files per upload. Remove files or export the project ZIP instead.`,
          );
          return;
        }
        const L = window.open("", "_blank");
        if (!L) {
          W.error("Allow pop-ups to open this project in Overleaf.");
          return;
        }
        ((L.opener = null), V(!0));
        try {
          const D = await d(),
            z = await x.mutateAsync({ documentId: D.id });
          if (L.closed)
            throw new Error("The Overleaf tab was closed before the project was ready.");
          await $o({
            archive: z,
            compiler: D.compiler,
            mainDocument: D.mainFilePath,
            name: D.name,
            targetWindow: L,
          });
        } catch (D) {
          (L.close(), W.error(Y(D, "Could not open the project in Overleaf.")));
        } finally {
          V(!1);
        }
      }, [x, r, d, s]),
      he = a.useCallback(
        async (A) => {
          if (s)
            try {
              if (A.kind === "project") {
                const z = await d();
                (await S.mutateAsync({ documentId: z.id, expectedRevision: z.revision }),
                  c(s.id),
                  f(),
                  W.success("Project deleted."));
                return;
              }
              if (!A.file) throw new Error("A project item is required for file deletion.");
              const L = await d(),
                D = await E.mutateAsync({
                  documentId: s.id,
                  fileId: A.file.id,
                  expectedRevision: L.revision,
                });
              (l(D.documentRevision),
                A.file.id === (n == null ? void 0 : n.id) && f(),
                W.success("Project item deleted."));
            } catch (L) {
              throw (W.error(Y(L, "Could not delete the project item.")), L);
            }
        },
        [S, E, l, c, f, d, s, n == null ? void 0 : n.id],
      ),
      F = a.useCallback(
        async (A) => {
          if (!(!s || A.length === 0))
            try {
              const L = await d(),
                D = A.map((z) => {
                  const ne = r.find((Le) => Le.kind === "text" && Le.path === z.path);
                  if (!ne) throw new Error(`Project search returned an unknown file: ${z.path}.`);
                  return { fileId: ne.id, content: z.content };
                });
              (await m.mutateAsync({
                documentId: s.id,
                input: { expectedRevision: L.revision, updates: D },
              }),
                f(),
                W.success(`Updated ${A.length} ${A.length === 1 ? "file" : "files"}.`));
            } catch (L) {
              throw (
                W.error(Y(L, "Project replacement stopped before every file was updated.")),
                L
              );
            }
        },
        [r, f, d, s, m],
      );
    return {
      changeCompiler: $,
      confirmDelete: he,
      createNode: Q,
      createProject: H,
      exportProject: fe,
      importProject: le,
      makeCopy: I,
      openProjectInOverleaf: ce,
      renameProject: X,
      renameNode: q,
      replaceAll: F,
      setMainFile: oe,
      uploadFiles: ae,
      isCopyingProject: C,
      isCreatingProject: M,
      isOpeningProjectInOverleaf: _,
      isRenamingProject: j,
      isDeleting: S.isPending || E.isPending,
      isFileMutationBusy:
        v.isPending ||
        E.isPending ||
        B.isPending ||
        k.isPending ||
        x.isPending ||
        m.isPending ||
        C ||
        M ||
        _ ||
        j,
    };
  },
  Ra = ({
    column: e,
    filePath: t,
    isPdfStale: r,
    jobId: o,
    line: s,
    pdfResult: n,
    onNavigateSource: i,
    onRevealPdf: l,
  }) => {
    const { isPending: c, mutateAsync: f } = no(),
      { isPending: u, mutateAsync: d } = so(),
      [p, g] = a.useState(null),
      w = a.useRef({ line: s, column: e });
    w.current = { line: s, column: e };
    const S = (p == null ? void 0 : p.jobId) === o ? p : null,
      v = !!(o && n != null && n.synctexAttachmentId && !r && t),
      E = n
        ? r
          ? "Recompile the current source before synchronizing"
          : n.synctexAttachmentId
            ? t
              ? null
              : "Select a text file before synchronizing"
            : "Recompile to generate SyncTeX navigation data"
        : "Compile the project to enable SyncTeX",
      B = a.useCallback(async () => {
        if (!(!v || !o || !t))
          try {
            const m = (
              await f({
                jobId: o,
                input: { file: t, line: w.current.line, column: Math.max(0, w.current.column - 1) },
              })
            ).results[0];
            if (!m) {
              W.info("No PDF location was found for this source position.");
              return;
            }
            (g((C) => ({
              jobId: o,
              nonce: ((C == null ? void 0 : C.nonce) ?? 0) + 1,
              position: m,
            })),
              l());
          } catch (x) {
            W.error(Y(x, "Could not synchronize the source with the PDF."));
          }
      }, [v, t, o, l, f]),
      k = a.useCallback(
        async (x) => {
          if (!(!v || !o))
            try {
              const C = (await d({ jobId: o, input: x })).results[0];
              if (!C) {
                W.info("No source location was found for this PDF position.");
                return;
              }
              i(C.file, C.line, C.column ?? 0);
            } catch (m) {
              W.error(Y(m, "Could not synchronize the PDF with the source."));
            }
        },
        [v, o, i, d],
      );
    return {
      canSync: v,
      disabledReason: E,
      isForwardSyncing: c,
      isReverseSyncing: u,
      pdfRequest: S,
      syncPdfToSource: k,
      syncSourceToPdf: B,
    };
  },
  La = (e) => ({
    width: "min(100vw, 76rem)",
    maxWidth: "100vw",
    display: "flex",
    flexDirection: "column",
    [e.breakpoints.down("md")]: { width: "100vw" },
  }),
  Ea = (e) => ({
    flex: "0 0 auto",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: e.spacing(1),
    padding: e.spacing(1.5, 2),
    borderBottom: `1px solid ${we(e.palette.text.primary, 0.08)}`,
  }),
  Pa = (e) => ({
    flex: 1,
    minHeight: 0,
    display: "flex",
    flexDirection: "column",
    overflow: "hidden",
    padding: e.spacing(1.5),
    gap: e.spacing(1.5),
  }),
  Ta = (e) => ({
    flex: "0 0 auto",
    display: "flex",
    alignItems: "center",
    gap: e.spacing(1),
    flexWrap: "wrap",
  }),
  Aa = (e) => ({
    flex: 1,
    minHeight: 0,
    display: "grid",
    gridTemplateColumns: "minmax(16rem, 23rem) minmax(0, 1fr)",
    gap: e.spacing(1.5),
    overflow: "hidden",
    [e.breakpoints.down("md")]: {
      gridTemplateColumns: "1fr",
      gridTemplateRows: "minmax(11rem, 35%) minmax(0, 1fr)",
    },
  }),
  Da = (e) => ({
    minHeight: 0,
    overflowY: "auto",
    display: "flex",
    flexDirection: "column",
    gap: e.spacing(0.75),
    paddingRight: e.spacing(0.5),
  }),
  Ma = (e, t) => ({
    width: "100%",
    display: "flex",
    alignItems: "stretch",
    justifyContent: "flex-start",
    textAlign: "left",
    border: `1px solid ${t ? we(e.palette.primary.main, 0.45) : we(e.palette.text.primary, 0.08)}`,
    borderRadius: e.shape.radiusSmPx,
    backgroundColor: t ? we(e.palette.primary.main, 0.09) : we(e.palette.background.paper, 0.72),
    padding: e.spacing(1),
    transition: e.transitions.create(["background-color", "border-color"]),
    "&:hover": {
      backgroundColor: we(e.palette.primary.main, t ? 0.13 : 0.055),
      borderColor: we(e.palette.primary.main, 0.32),
    },
  }),
  Oa = (e) => ({
    minWidth: 0,
    minHeight: 0,
    display: "flex",
    flexDirection: "column",
    overflow: "hidden",
    border: `1px solid ${we(e.palette.text.primary, 0.08)}`,
    borderRadius: e.shape.radiusSmPx,
    backgroundColor: e.palette.background.paper,
  }),
  Na = (e) => ({
    flex: 1,
    minHeight: 0,
    display: "flex",
    flexDirection: "column",
    gap: e.spacing(1),
    overflowY: "auto",
    padding: e.spacing(1),
  }),
  Ba = (e) => ({
    flex: "0 0 auto",
    minHeight: 0,
    minWidth: 0,
    maxWidth: "100%",
    overflow: "hidden",
    paddingBottom: e.spacing(1),
    borderBottom: `1px solid ${we(e.palette.text.primary, 0.08)}`,
  }),
  Wa = { minWidth: 0, maxWidth: "100%" },
  _a = (e) => ({
    minWidth: 0,
    maxWidth: "none",
    minHeight: "3.5rem",
    alignItems: "flex-start",
    justifyContent: "center",
    textAlign: "left",
    textTransform: "none",
    padding: e.spacing(0.75, 1),
  }),
  Fa = { display: "flex", flex: "1 0 28rem", flexDirection: "column", minHeight: "18rem" },
  Xa = (e) => ({
    flex: 1,
    minHeight: 0,
    overflow: "auto",
    "& .cm-mergeView": { minWidth: "42rem", height: "100%", display: "flex" },
    "& .cm-mergeViewEditors": { minWidth: 0, flex: 1 },
    "& .cm-editor": { minHeight: "100%", height: "100%" },
    "& .cm-scroller": { overflow: "visible" },
    "& .cm-mergeViewGutter": { backgroundColor: we(e.palette.text.primary, 0.035) },
  }),
  je = (e) => ({
    minHeight: "12rem",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    padding: e.spacing(2),
  }),
  za = ({ selectedVersion: e }) =>
    h.jsx(U, {
      sx: Ta,
      children: h.jsx(G, {
        variant: "caption",
        color: "textSecondary",
        children: e
          ? `Revision ${e.documentRevisionStart}${e.documentRevisionEnd !== e.documentRevisionStart ? `–${e.documentRevisionEnd}` : ""} · Compared with the previous checkpoint`
          : "Select a checkpoint",
      }),
    });
class se {
  constructor(t, r, o, s) {
    ((this.fromA = t), (this.toA = r), (this.fromB = o), (this.toB = s));
  }
  offset(t, r = t) {
    return new se(this.fromA + t, this.toA + t, this.fromB + r, this.toB + r);
  }
}
function Ae(e, t, r, o, s, n) {
  if (e == o) return [];
  let i = rr(e, t, r, o, s, n),
    l = nr(e, t + i, r, o, s + i, n);
  ((t += i), (r -= l), (s += i), (n -= l));
  let c = r - t,
    f = n - s;
  if (!c || !f) return [new se(t, r, s, n)];
  if (c > f) {
    let d = e.slice(t, r).indexOf(o.slice(s, n));
    if (d > -1) return [new se(t, t + d, s, s), new se(t + d + f, r, n, n)];
  } else if (f > c) {
    let d = o.slice(s, n).indexOf(e.slice(t, r));
    if (d > -1) return [new se(t, t, s, s + d), new se(r, r, s + d + c, n)];
  }
  if (c == 1 || f == 1) return [new se(t, r, s, n)];
  let u = An(e, t, r, o, s, n);
  if (u) {
    let [d, p, g] = u;
    return Ae(e, t, d, o, s, p).concat(Ae(e, d + g, r, o, p + g, n));
  }
  return Ha(e, t, r, o, s, n);
}
let Ke = 1e9,
  Qe = 0,
  tr = !1;
function Ha(e, t, r, o, s, n) {
  let i = r - t,
    l = n - s;
  if ((Ke < 1e9 && Math.min(i, l) > Ke * 16) || (Qe > 0 && Date.now() > Qe))
    return Math.min(i, l) > Ke * 64 ? [new se(t, r, s, n)] : Ur(e, t, r, o, s, n);
  let c = Math.ceil((i + l) / 2);
  (Wt.reset(c), _t.reset(c));
  let f = (g, w) => e.charCodeAt(t + g) == o.charCodeAt(s + w),
    u = (g, w) => e.charCodeAt(r - g - 1) == o.charCodeAt(n - w - 1),
    d = (i - l) % 2 != 0 ? _t : null,
    p = d ? null : Wt;
  for (let g = 0; g < c; g++) {
    if (g > Ke || (Qe > 0 && !(g & 63) && Date.now() > Qe)) return Ur(e, t, r, o, s, n);
    let w = Wt.advance(g, i, l, c, d, !1, f) || _t.advance(g, i, l, c, p, !0, u);
    if (w) return $a(e, t, r, t + w[0], o, s, n, s + w[1]);
  }
  return [new se(t, r, s, n)];
}
class Pn {
  constructor() {
    this.vec = [];
  }
  reset(t) {
    this.len = t << 1;
    for (let r = 0; r < this.len; r++) this.vec[r] = -1;
    ((this.vec[t + 1] = 0), (this.start = this.end = 0));
  }
  advance(t, r, o, s, n, i, l) {
    for (let c = -t + this.start; c <= t - this.end; c += 2) {
      let f = s + c,
        u =
          c == -t || (c != t && this.vec[f - 1] < this.vec[f + 1])
            ? this.vec[f + 1]
            : this.vec[f - 1] + 1,
        d = u - c;
      for (; u < r && d < o && l(u, d);) (u++, d++);
      if (((this.vec[f] = u), u > r)) this.end += 2;
      else if (d > o) this.start += 2;
      else if (n) {
        let p = s + (r - o) - c;
        if (p >= 0 && p < this.len && n.vec[p] != -1)
          if (i) {
            let g = n.vec[p];
            if (g >= r - u) return [g, s + g - p];
          } else {
            let g = r - n.vec[p];
            if (u >= g) return [u, d];
          }
      }
    }
    return null;
  }
}
const Wt = new Pn(),
  _t = new Pn();
function $a(e, t, r, o, s, n, i, l) {
  let c = !1;
  return (
    !We(e, o) && ++o == r && (c = !0),
    !We(s, l) && ++l == i && (c = !0),
    c ? [new se(t, r, n, i)] : Ae(e, t, o, s, n, l).concat(Ae(e, o, r, s, l, i))
  );
}
function Tn(e, t) {
  let r = 1,
    o = Math.min(e, t);
  for (; r < o;) r = r << 1;
  return r;
}
function rr(e, t, r, o, s, n) {
  if (t == r || t == n || e.charCodeAt(t) != o.charCodeAt(s)) return 0;
  let i = Tn(r - t, n - s);
  for (let l = t, c = s; ;) {
    let f = l + i,
      u = c + i;
    if (f > r || u > n || e.slice(l, f) != o.slice(c, u)) {
      if (i == 1) return l - t - (We(e, l) ? 0 : 1);
      i = i >> 1;
    } else {
      if (f == r || u == n) return f - t;
      ((l = f), (c = u));
    }
  }
}
function nr(e, t, r, o, s, n) {
  if (t == r || s == n || e.charCodeAt(r - 1) != o.charCodeAt(n - 1)) return 0;
  let i = Tn(r - t, n - s);
  for (let l = r, c = n; ;) {
    let f = l - i,
      u = c - i;
    if (f < t || u < s || e.slice(f, l) != o.slice(u, c)) {
      if (i == 1) return r - l - (We(e, l) ? 0 : 1);
      i = i >> 1;
    } else {
      if (f == t || u == s) return r - f;
      ((l = f), (c = u));
    }
  }
}
function Vt(e, t, r, o, s, n, i, l) {
  let c = o.slice(s, n),
    f = null;
  for (;;) {
    if (f || i < l) return f;
    for (let u = t + i; ;) {
      We(e, u) || u++;
      let d = u + i;
      if ((We(e, d) || (d += d == u + 1 ? 1 : -1), d >= r)) break;
      let p = e.slice(u, d),
        g = -1;
      for (; (g = c.indexOf(p, g + 1)) != -1;) {
        let w = rr(e, d, r, o, s + g + p.length, n),
          S = nr(e, t, u, o, s, s + g),
          v = p.length + w + S;
        (!f || f[2] < v) && (f = [u - S, s + g - S, v]);
      }
      u = d;
    }
    if (l < 0) return f;
    i = i >> 1;
  }
}
function An(e, t, r, o, s, n) {
  let i = r - t,
    l = n - s;
  if (i < l) {
    let c = An(o, s, n, e, t, r);
    return c && [c[1], c[0], c[2]];
  }
  return i < 4 || l * 2 < i ? null : Vt(e, t, r, o, s, n, Math.floor(i / 4), -1);
}
function Ur(e, t, r, o, s, n) {
  tr = !0;
  let i = r - t,
    l = n - s,
    c;
  if (i < l) {
    let p = Vt(o, s, n, e, t, r, Math.floor(i / 6), 50);
    c = p && [p[1], p[0], p[2]];
  } else c = Vt(e, t, r, o, s, n, Math.floor(l / 6), 50);
  if (!c) return [new se(t, r, s, n)];
  let [f, u, d] = c;
  return Ae(e, t, f, o, s, u).concat(Ae(e, f + d, r, o, u + d, n));
}
function Dn(e, t) {
  for (let r = 1; r < e.length; r++) {
    let o = e[r - 1],
      s = e[r];
    o.toA > s.fromA - t &&
      o.toB > s.fromB - t &&
      ((e[r - 1] = new se(o.fromA, s.toA, o.fromB, s.toB)), e.splice(r--, 1));
  }
}
function Ua(e, t, r) {
  for (;;) {
    Dn(r, 1);
    let o = !1;
    for (let s = 0; s < r.length; s++) {
      let n = r[s],
        i,
        l;
      ((i = rr(e, n.fromA, n.toA, t, n.fromB, n.toB)) &&
        (n = r[s] = new se(n.fromA + i, n.toA, n.fromB + i, n.toB)),
        (l = nr(e, n.fromA, n.toA, t, n.fromB, n.toB)) &&
          (n = r[s] = new se(n.fromA, n.toA - l, n.fromB, n.toB - l)));
      let c = n.toA - n.fromA,
        f = n.toB - n.fromB;
      if (c && f) continue;
      let u = n.fromA - (s ? r[s - 1].toA : 0),
        d = (s < r.length - 1 ? r[s + 1].fromA : e.length) - n.toA;
      if (!u || !d) continue;
      let p = c ? e.slice(n.fromA, n.toA) : t.slice(n.fromB, n.toB);
      u <= p.length && e.slice(n.fromA - u, n.fromA) == p.slice(p.length - u)
        ? ((r[s] = new se(n.fromA - u, n.toA - u, n.fromB - u, n.toB - u)), (o = !0))
        : d <= p.length &&
          e.slice(n.toA, n.toA + d) == p.slice(0, d) &&
          ((r[s] = new se(n.fromA + d, n.toA + d, n.fromB + d, n.toB + d)), (o = !0));
    }
    if (!o) break;
  }
  return r;
}
function Va(e, t, r) {
  for (let o = 0, s = 0; s < e.length; s++) {
    let n = e[s],
      i = n.toA - n.fromA,
      l = n.toB - n.fromB;
    if ((i && l) || i > 3 || l > 3) {
      let c = s == e.length - 1 ? t.length : e[s + 1].fromA,
        f = n.fromA - o,
        u = c - n.toA,
        d = Gr(t, n.fromA, f),
        p = Vr(t, n.toA, u),
        g = n.fromA - d,
        w = p - n.toA;
      if ((!i || !l) && g && w) {
        let S = Math.max(i, l),
          [v, E, B] = i ? [t, n.fromA, n.toA] : [r, n.fromB, n.toB];
        (S > g && t.slice(d, n.fromA) == v.slice(B - g, B)
          ? ((n = e[s] = new se(d, d + i, n.fromB - g, n.toB - g)),
            (d = n.fromA),
            (p = Vr(t, n.toA, c - n.toA)))
          : S > w &&
            t.slice(n.toA, p) == v.slice(E, E + w) &&
            ((n = e[s] = new se(p - i, p, n.fromB + w, n.toB + w)),
            (p = n.toA),
            (d = Gr(t, n.fromA, n.fromA - o))),
          (g = n.fromA - d),
          (w = p - n.toA));
      }
      if (g || w) n = e[s] = new se(n.fromA - g, n.toA + w, n.fromB - g, n.toB + w);
      else if (i) {
        if (!l) {
          let S = Qr(t, n.fromA, n.toA),
            v,
            E = S < 0 ? -1 : Kr(t, n.toA, n.fromA);
          S > -1 && (v = S - n.fromA) <= u && t.slice(n.fromA, S) == t.slice(n.toA, n.toA + v)
            ? (n = e[s] = n.offset(v))
            : E > -1 &&
              (v = n.toA - E) <= f &&
              t.slice(n.fromA - v, n.fromA) == t.slice(E, n.toA) &&
              (n = e[s] = n.offset(-v));
        }
      } else {
        let S = Qr(r, n.fromB, n.toB),
          v,
          E = S < 0 ? -1 : Kr(r, n.toB, n.fromB);
        S > -1 && (v = S - n.fromB) <= u && r.slice(n.fromB, S) == r.slice(n.toB, n.toB + v)
          ? (n = e[s] = n.offset(v))
          : E > -1 &&
            (v = n.toB - E) <= f &&
            r.slice(n.fromB - v, n.fromB) == r.slice(E, n.toB) &&
            (n = e[s] = n.offset(-v));
      }
    }
    o = n.toA;
  }
  return (Dn(e, 3), e);
}
let Te;
try {
  Te = new RegExp("[\\p{Alphabetic}\\p{Number}]", "u");
} catch {}
function Mn(e) {
  return (e > 48 && e < 58) || (e > 64 && e < 91) || (e > 96 && e < 123);
}
function On(e, t) {
  if (t == e.length) return 0;
  let r = e.charCodeAt(t);
  return r < 192
    ? Mn(r)
      ? 1
      : 0
    : Te
      ? !Wn(r) || t == e.length - 1
        ? Te.test(String.fromCharCode(r))
          ? 1
          : 0
        : Te.test(e.slice(t, t + 2))
          ? 2
          : 0
      : 0;
}
function Nn(e, t) {
  if (!t) return 0;
  let r = e.charCodeAt(t - 1);
  return r < 192
    ? Mn(r)
      ? 1
      : 0
    : Te
      ? !_n(r) || t == 1
        ? Te.test(String.fromCharCode(r))
          ? 1
          : 0
        : Te.test(e.slice(t - 2, t))
          ? 2
          : 0
      : 0;
}
const Bn = 8;
function Vr(e, t, r) {
  if (t == e.length || !Nn(e, t)) return t;
  for (let o = t, s = t + r, n = 0; n < Bn; n++) {
    let i = On(e, o);
    if (!i || o + i > s) return o;
    o += i;
  }
  return t;
}
function Gr(e, t, r) {
  if (!t || !On(e, t)) return t;
  for (let o = t, s = t - r, n = 0; n < Bn; n++) {
    let i = Nn(e, o);
    if (!i || o - i < s) return o;
    o -= i;
  }
  return t;
}
function Kr(e, t, r) {
  for (; t != r; t--) if (e.charCodeAt(t - 1) == 10) return t;
  return -1;
}
function Qr(e, t, r) {
  for (; t != r; t++) if (e.charCodeAt(t) == 10) return t;
  return -1;
}
const Wn = (e) => e >= 55296 && e <= 56319,
  _n = (e) => e >= 56320 && e <= 57343;
function We(e, t) {
  return !t || t == e.length || !Wn(e.charCodeAt(t - 1)) || !_n(e.charCodeAt(t));
}
function Ga(e, t, r) {
  var o;
  let s = r == null ? void 0 : r.override;
  return s
    ? s(e, t)
    : ((Ke = ((o = r == null ? void 0 : r.scanLimit) !== null && o !== void 0 ? o : 1e9) >> 1),
      (Qe = r != null && r.timeout ? Date.now() + r.timeout : 0),
      (tr = !1),
      Ua(e, t, Ae(e, 0, e.length, t, 0, t.length)));
}
function Fn() {
  return !tr;
}
function Xn(e, t, r) {
  return Va(Ga(e, t, r), e, t);
}
const ye = bn.define({ combine: (e) => e[0] }),
  Gt = Je.define(),
  Ka = bn.define(),
  Re = Jt.define({
    create(e) {
      return null;
    },
    update(e, t) {
      for (let r of t.effects) r.is(Gt) && (e = r.value);
      for (let r of t.state.facet(Ka)) e = r(e, t);
      return e;
    },
  });
class Be {
  constructor(t, r, o, s, n, i = !0) {
    ((this.changes = t),
      (this.fromA = r),
      (this.toA = o),
      (this.fromB = s),
      (this.toB = n),
      (this.precise = i));
  }
  offset(t, r) {
    return t || r
      ? new Be(
          this.changes,
          this.fromA + t,
          this.toA + t,
          this.fromB + r,
          this.toB + r,
          this.precise,
        )
      : this;
  }
  get endA() {
    return Math.max(this.fromA, this.toA - 1);
  }
  get endB() {
    return Math.max(this.fromB, this.toB - 1);
  }
  static build(t, r, o) {
    let s = Xn(t.toString(), r.toString(), o);
    return zn(s, t, r, 0, 0, Fn());
  }
  static updateA(t, r, o, s, n) {
    return en(qr(t, s, !0, o.length), t, r, o, n);
  }
  static updateB(t, r, o, s, n) {
    return en(qr(t, s, !1, r.length), t, r, o, n);
  }
}
function Yr(e, t, r, o) {
  let s = r.lineAt(e),
    n = o.lineAt(t);
  return s.to == e && n.to == t && e < r.length && t < o.length ? [e + 1, t + 1] : [s.from, n.from];
}
function Jr(e, t, r, o) {
  let s = r.lineAt(e),
    n = o.lineAt(t);
  return s.from == e && n.from == t ? [e, t] : [s.to + 1, n.to + 1];
}
function zn(e, t, r, o, s, n) {
  let i = [];
  for (let l = 0; l < e.length; l++) {
    let c = e[l],
      [f, u] = Yr(c.fromA + o, c.fromB + s, t, r),
      [d, p] = Jr(c.toA + o, c.toB + s, t, r),
      g = [c.offset(-f + o, -u + s)];
    for (; l < e.length - 1;) {
      let w = e[l + 1],
        [S, v] = Yr(w.fromA + o, w.fromB + s, t, r);
      if (S > d + 1 && v > p + 1) break;
      (g.push(w.offset(-f + o, -u + s)), ([d, p] = Jr(w.toA + o, w.toB + s, t, r)), l++);
    }
    i.push(new Be(g, f, Math.max(f, d), u, Math.max(u, p), n));
  }
  return i;
}
const ct = 1e3;
function Zr(e, t, r, o) {
  let s = 0,
    n = e.length;
  for (;;) {
    if (s == n) {
      let u = 0,
        d = 0;
      s && ({ toA: u, toB: d } = e[s - 1]);
      let p = t - (r ? u : d);
      return [u + p, d + p];
    }
    let i = (s + n) >> 1,
      l = e[i],
      [c, f] = r ? [l.fromA, l.toA] : [l.fromB, l.toB];
    if (c > t) n = i;
    else if (f <= t) s = i + 1;
    else return o ? [l.fromA, l.fromB] : [l.toA, l.toB];
  }
}
function qr(e, t, r, o) {
  let s = [];
  return (
    t.iterChangedRanges((n, i, l, c) => {
      let f = 0,
        u = r ? t.length : o,
        d = 0,
        p = r ? o : t.length;
      (n > ct && ([f, d] = Zr(e, n - ct, r, !0)),
        i < t.length - ct && ([u, p] = Zr(e, i + ct, r, !1)));
      let g = c - l - (i - n),
        w,
        [S, v] = r ? [g, 0] : [0, g];
      s.length && (w = s[s.length - 1]).toA >= f
        ? (s[s.length - 1] = {
            fromA: w.fromA,
            fromB: w.fromB,
            toA: u,
            toB: p,
            diffA: w.diffA + S,
            diffB: w.diffB + v,
          })
        : s.push({ fromA: f, toA: u, fromB: d, toB: p, diffA: S, diffB: v });
    }),
    s
  );
}
function en(e, t, r, o, s) {
  if (!e.length) return t;
  let n = [];
  for (let i = 0, l = 0, c = 0, f = 0; ; i++) {
    let u = i == e.length ? null : e[i],
      d = u ? u.fromA + l : r.length,
      p = u ? u.fromB + c : o.length;
    for (; f < t.length;) {
      let v = t[f];
      if (u && (v.toA + l > d || v.toB + c > p)) break;
      (n.push(v.offset(l, c)), f++);
    }
    if (!u) break;
    let g = u.toA + l + u.diffA,
      w = u.toB + c + u.diffB,
      S = Xn(r.sliceString(d, g), o.sliceString(p, w), s);
    for (let v of zn(S, r, o, d, p, Fn())) n.push(v);
    for (l += u.diffA, c += u.diffB; f < t.length;) {
      let v = t[f];
      if (v.fromA + l > g && v.fromB + c > w) break;
      f++;
    }
  }
  return n;
}
const Qa = { scanLimit: 500 },
  Hn = Ao.fromClass(
    class {
      constructor(e) {
        ({ deco: this.deco, gutter: this.gutter } = nn(e));
      }
      update(e) {
        (e.docChanged ||
          e.viewportChanged ||
          Ya(e.startState, e.state) ||
          Ja(e.startState, e.state)) &&
          ({ deco: this.deco, gutter: this.gutter } = nn(e.view));
      }
    },
    { decorations: (e) => e.deco },
  ),
  ut = Cn.low(
    Do({
      class: "cm-changeGutter",
      markers: (e) => {
        var t;
        return ((t = e.plugin(Hn)) === null || t === void 0 ? void 0 : t.gutter) || Oo.empty;
      },
    }),
  );
function Ya(e, t) {
  return e.field(Re, !1) != t.field(Re, !1);
}
function Ja(e, t) {
  return e.facet(ye) != t.facet(ye);
}
const tn = be.line({ class: "cm-changedLine" }),
  Za = be.mark({ class: "cm-changedText" }),
  qa = be.mark({ tagName: "ins", class: "cm-insertedLine" }),
  ei = be.mark({ tagName: "del", class: "cm-deletedLine" }),
  rn = new (class extends Mo {
    constructor() {
      (super(...arguments), (this.elementClass = "cm-changedLineGutter"));
    }
  })();
function ti(e, t, r, o, s, n) {
  let i = r ? e.fromA : e.fromB,
    l = r ? e.toA : e.toB,
    c = 0;
  if (i != l) {
    (s.add(i, i, tn), s.add(i, l, r ? ei : qa), n && n.add(i, i, rn));
    for (let f = t.iterRange(i, l - 1), u = i; !f.next().done;) {
      if (f.lineBreak) {
        (u++, s.add(u, u, tn), n && n.add(u, u, rn));
        continue;
      }
      let d = u + f.value.length;
      if (o)
        for (; c < e.changes.length;) {
          let p = e.changes[c],
            g = i + (r ? p.fromA : p.fromB),
            w = i + (r ? p.toA : p.toB),
            S = Math.max(u, g),
            v = Math.min(d, w);
          if ((S < v && s.add(S, v, Za), w < d)) c++;
          else break;
        }
      u = d;
    }
  }
}
function nn(e) {
  let t = e.state.field(Re),
    { side: r, highlightChanges: o, markGutter: s, overrideChunk: n } = e.state.facet(ye),
    i = r == "a",
    l = new Ze(),
    c = s ? new Ze() : null,
    { from: f, to: u } = e.viewport;
  for (let d of t) {
    if ((i ? d.fromA : d.fromB) >= u) break;
    (i ? d.toA : d.toB) > f && (!n || !n(e.state, d, l, c)) && ti(d, e.state.doc, i, o, l, c);
  }
  return { deco: l.finish(), gutter: c && c.finish() };
}
class dt extends Sn {
  constructor(t) {
    (super(), (this.height = t));
  }
  eq(t) {
    return this.height == t.height;
  }
  toDOM() {
    let t = document.createElement("div");
    return ((t.className = "cm-mergeSpacer"), (t.style.height = this.height + "px"), t);
  }
  updateDOM(t) {
    return ((t.style.height = this.height + "px"), !0);
  }
  get estimatedHeight() {
    return this.height;
  }
  ignoreEvent() {
    return !1;
  }
}
const wt = Je.define({ map: (e, t) => e.map(t) }),
  Ye = Jt.define({
    create: () => be.none,
    update: (e, t) => {
      for (let r of t.effects) if (r.is(wt)) return r.value;
      return e.map(t.changes);
    },
    provide: (e) => Ce.decorations.from(e),
  }),
  ft = 0.01;
function sn(e, t) {
  if (e.size != t.size) return !1;
  let r = e.iter(),
    o = t.iter();
  for (; r.value;) {
    if (r.from != o.from || Math.abs(r.value.spec.widget.height - o.value.spec.widget.height) > 1)
      return !1;
    (r.next(), o.next());
  }
  return !0;
}
function ri(e, t, r) {
  let o = new Ze(),
    s = new Ze(),
    n = e.state.field(Ye).iter(),
    i = t.state.field(Ye).iter(),
    l = 0,
    c = 0,
    f = 0,
    u = 0,
    d = e.viewport,
    p = t.viewport;
  for (let v = 0; ; v++) {
    let E = v < r.length ? r[v] : null,
      B = E ? E.fromA : e.state.doc.length,
      k = E ? E.fromB : t.state.doc.length;
    if (l < B) {
      let x = e.lineBlockAt(l).top + f,
        m = t.lineBlockAt(c).top + u,
        C = x - m;
      C < -ft
        ? ((f -= C), o.add(l, l, be.widget({ widget: new dt(-C), block: !0, side: -1 })))
        : C > ft && ((u += C), s.add(c, c, be.widget({ widget: new dt(C), block: !0, side: -1 })));
    }
    if (B > l + 1e3 && l < d.from && B > d.from && c < p.from && k > p.from) {
      let x = Math.min(d.from - l, p.from - c);
      ((l += x), (c += x), v--);
    } else if (E) ((l = E.toA), (c = E.toB));
    else break;
    for (; n.value && n.from < l;) ((f -= n.value.spec.widget.height), n.next());
    for (; i.value && i.from < c;) ((u -= i.value.spec.widget.height), i.next());
  }
  for (; n.value;) ((f -= n.value.spec.widget.height), n.next());
  for (; i.value;) ((u -= i.value.spec.widget.height), i.next());
  let g = e.contentHeight + f - (t.contentHeight + u);
  g < ft
    ? o.add(
        e.state.doc.length,
        e.state.doc.length,
        be.widget({ widget: new dt(-g), block: !0, side: 1 }),
      )
    : g > ft &&
      s.add(
        t.state.doc.length,
        t.state.doc.length,
        be.widget({ widget: new dt(g), block: !0, side: 1 }),
      );
  let w = o.finish(),
    S = s.finish();
  (sn(w, e.state.field(Ye)) || e.dispatch({ effects: wt.of(w) }),
    sn(S, t.state.field(Ye)) || t.dispatch({ effects: wt.of(S) }));
}
const Kt = Je.define({ map: (e, t) => t.mapPos(e) });
class ni extends Sn {
  constructor(t) {
    (super(), (this.lines = t));
  }
  eq(t) {
    return this.lines == t.lines;
  }
  toDOM(t) {
    let r = document.createElement("div");
    return (
      (r.className = "cm-collapsedLines"),
      (r.textContent = t.state.phrase("$ unchanged lines", this.lines)),
      r.addEventListener("click", (o) => {
        let s = t.posAtDOM(o.target);
        t.dispatch({ effects: Kt.of(s) });
        let { side: n, sibling: i } = t.state.facet(ye);
        i && i().dispatch({ effects: Kt.of(si(s, t.state.field(Re), n == "a")) });
      }),
      r
    );
  }
  ignoreEvent(t) {
    return t instanceof MouseEvent;
  }
  get estimatedHeight() {
    return 27;
  }
  get type() {
    return "collapsed-unchanged-code";
  }
}
function si(e, t, r) {
  let o = 0,
    s = 0;
  for (let n = 0; ; n++) {
    let i = n < t.length ? t[n] : null;
    if (!i || (r ? i.fromA : i.fromB) >= e) return s + (e - o);
    [o, s] = r ? [i.toA, i.toB] : [i.toB, i.toA];
  }
}
const oi = Jt.define({
  create(e) {
    return be.none;
  },
  update(e, t) {
    e = e.map(t.changes);
    for (let r of t.effects) r.is(Kt) && (e = e.update({ filter: (o) => o != r.value }));
    if (e.size && t.state.field(Re) != t.startState.field(Re, !1)) {
      let r = t.state.facet(ye).side == "a",
        o = [];
      for (let s of t.state.field(Re))
        e.between(r ? s.fromA : s.fromB, r ? s.toA : s.toB, (n) => {
          o.push(n);
        });
      o.length && (e = e.update({ filter: (s) => o.indexOf(s) < 0 }));
    }
    return e;
  },
  provide: (e) => Ce.decorations.from(e),
});
function on({ margin: e = 3, minSize: t = 4 }) {
  return oi.init((r) => ai(r, e, t));
}
function ai(e, t, r) {
  let o = new Ze(),
    s = e.facet(ye).side == "a",
    n = e.field(Re),
    i = 1;
  for (let l = 0; ; l++) {
    let c = l < n.length ? n[l] : null,
      f = l ? i + t : 1,
      u = c ? e.doc.lineAt(s ? c.fromA : c.fromB).number - 1 - t : e.doc.lines,
      d = u - f + 1;
    if (
      (d >= r &&
        o.add(e.doc.line(f).from, e.doc.line(u).to, be.replace({ widget: new ni(d), block: !0 })),
      !c)
    )
      break;
    i = e.doc.lineAt(Math.min(e.doc.length, s ? c.toA : c.toB)).number;
  }
  return o.finish();
}
const ii = Ce.styleModule.of(
    new To({
      ".cm-mergeView": { overflowY: "auto" },
      ".cm-mergeViewEditors": { display: "flex", alignItems: "stretch" },
      ".cm-mergeViewEditor": { flexGrow: 1, flexBasis: 0, overflow: "hidden" },
      ".cm-merge-revert": { width: "1.6em", flexGrow: 0, flexShrink: 0, position: "relative" },
      ".cm-merge-revert button": {
        position: "absolute",
        display: "block",
        width: "100%",
        boxSizing: "border-box",
        textAlign: "center",
        background: "none",
        border: "none",
        font: "inherit",
        cursor: "pointer",
      },
    }),
  ),
  li = Ce.baseTheme({
    ".cm-mergeView & .cm-scroller, .cm-mergeView &": {
      height: "auto !important",
      overflowY: "visible !important",
    },
    "&.cm-merge-a .cm-changedLine, .cm-deletedChunk": {
      backgroundColor: "rgba(160, 128, 100, .08)",
    },
    "&.cm-merge-b .cm-changedLine, .cm-inlineChangedLine": {
      backgroundColor: "rgba(100, 160, 128, .08)",
    },
    "&light.cm-merge-a .cm-changedText, &light .cm-deletedChunk .cm-deletedText": {
      background: "linear-gradient(#ee443366, #ee443366) bottom/100% 2px no-repeat",
    },
    "&dark.cm-merge-a .cm-changedText, &dark .cm-deletedChunk .cm-deletedText": {
      background: "linear-gradient(#ffaa9966, #ffaa9966) bottom/100% 2px no-repeat",
    },
    "&light.cm-merge-b .cm-changedText": {
      background: "linear-gradient(#22bb22aa, #22bb22aa) bottom/100% 2px no-repeat",
    },
    "&dark.cm-merge-b .cm-changedText": {
      background: "linear-gradient(#88ff88aa, #88ff88aa) bottom/100% 2px no-repeat",
    },
    "&.cm-merge-b .cm-deletedText": { background: "#ff000033" },
    ".cm-insertedLine, .cm-deletedLine, .cm-deletedLine del": { textDecoration: "none" },
    ".cm-deletedChunk": {
      paddingLeft: "6px",
      "& .cm-chunkButtons": { position: "absolute", insetInlineEnd: "5px" },
      "& button": {
        border: "none",
        cursor: "pointer",
        color: "white",
        margin: "0 2px",
        borderRadius: "3px",
        "&[name=accept]": { background: "#2a2" },
        "&[name=reject]": { background: "#d43" },
      },
    },
    ".cm-collapsedLines": {
      padding: "5px 5px 5px 10px",
      cursor: "pointer",
      "&:before": { content: '"⦚"', marginInlineEnd: "7px" },
      "&:after": { content: '"⦚"', marginInlineStart: "7px" },
    },
    "&light .cm-collapsedLines": {
      color: "#444",
      background:
        "linear-gradient(to bottom, transparent 0, #f3f3f3 30%, #f3f3f3 70%, transparent 100%)",
    },
    "&dark .cm-collapsedLines": {
      color: "#ddd",
      background: "linear-gradient(to bottom, transparent 0, #222 30%, #222 70%, transparent 100%)",
    },
    ".cm-changeGutter": { width: "3px", paddingLeft: "1px" },
    "&light.cm-merge-a .cm-changedLineGutter, &light .cm-deletedLineGutter": { background: "#e43" },
    "&dark.cm-merge-a .cm-changedLineGutter, &dark .cm-deletedLineGutter": { background: "#fa9" },
    "&light.cm-merge-b .cm-changedLineGutter": { background: "#2b2" },
    "&dark.cm-merge-b .cm-changedLineGutter": { background: "#8f8" },
    ".cm-inlineChangedLineGutter": { background: "#75d" },
  }),
  an = new yn(),
  ht = new yn();
class ci {
  constructor(t) {
    ((this.revertDOM = null),
      (this.revertToA = !1),
      (this.revertToLeft = !1),
      (this.measuring = -1),
      (this.diffConf = t.diffConfig || Qa));
    let r = [
        Cn.low(Hn),
        li,
        ii,
        Ye,
        Ce.updateListener.of((d) => {
          this.measuring < 0 &&
            (d.heightChanged || d.viewportChanged) &&
            !d.transactions.some((p) => p.effects.some((g) => g.is(wt))) &&
            this.measure();
        }),
      ],
      o = [
        ye.of({
          side: "a",
          sibling: () => this.b,
          highlightChanges: t.highlightChanges !== !1,
          markGutter: t.gutter !== !1,
        }),
      ];
    t.gutter !== !1 && o.push(ut);
    let s = gt.create({
        doc: t.a.doc,
        selection: t.a.selection,
        extensions: [
          t.a.extensions || [],
          Ce.editorAttributes.of({ class: "cm-merge-a" }),
          ht.of(o),
          r,
        ],
      }),
      n = [
        ye.of({
          side: "b",
          sibling: () => this.a,
          highlightChanges: t.highlightChanges !== !1,
          markGutter: t.gutter !== !1,
        }),
      ];
    t.gutter !== !1 && n.push(ut);
    let i = gt.create({
      doc: t.b.doc,
      selection: t.b.selection,
      extensions: [
        t.b.extensions || [],
        Ce.editorAttributes.of({ class: "cm-merge-b" }),
        ht.of(n),
        r,
      ],
    });
    this.chunks = Be.build(s.doc, i.doc, this.diffConf);
    let l = [Re.init(() => this.chunks), an.of(t.collapseUnchanged ? on(t.collapseUnchanged) : [])];
    ((s = s.update({ effects: Je.appendConfig.of(l) }).state),
      (i = i.update({ effects: Je.appendConfig.of(l) }).state),
      (this.dom = document.createElement("div")),
      (this.dom.className = "cm-mergeView"),
      (this.editorDOM = this.dom.appendChild(document.createElement("div"))),
      (this.editorDOM.className = "cm-mergeViewEditors"));
    let c = t.orientation || "a-b",
      f = document.createElement("div");
    f.className = "cm-mergeViewEditor";
    let u = document.createElement("div");
    ((u.className = "cm-mergeViewEditor"),
      this.editorDOM.appendChild(c == "a-b" ? f : u),
      this.editorDOM.appendChild(c == "a-b" ? u : f),
      (this.a = new Ce({
        state: s,
        parent: f,
        root: t.root,
        dispatchTransactions: (d) => this.dispatch(d, this.a),
      })),
      (this.b = new Ce({
        state: i,
        parent: u,
        root: t.root,
        dispatchTransactions: (d) => this.dispatch(d, this.b),
      })),
      this.setupRevertControls(
        !!t.revertControls,
        t.revertControls == "b-to-a",
        t.renderRevertControl,
      ),
      t.parent && t.parent.appendChild(this.dom),
      this.scheduleMeasure());
  }
  dispatch(t, r) {
    if (t.some((o) => o.docChanged)) {
      let o = t[t.length - 1],
        s = t.reduce((i, l) => i.compose(l.changes), Po.empty(t[0].startState.doc.length));
      ((this.chunks =
        r == this.a
          ? Be.updateA(this.chunks, o.newDoc, this.b.state.doc, s, this.diffConf)
          : Be.updateB(this.chunks, this.a.state.doc, o.newDoc, s, this.diffConf)),
        r.update([...t, o.state.update({ effects: Gt.of(this.chunks) })]));
      let n = r == this.a ? this.b : this.a;
      (n.update([n.state.update({ effects: Gt.of(this.chunks) })]), this.scheduleMeasure());
    } else r.update(t);
  }
  reconfigure(t) {
    if (("diffConfig" in t && (this.diffConf = t.diffConfig), "orientation" in t)) {
      let n = t.orientation != "b-a";
      if (n != (this.editorDOM.firstChild == this.a.dom.parentNode)) {
        let i = this.a.dom.parentNode,
          l = this.b.dom.parentNode;
        (i.remove(),
          l.remove(),
          this.editorDOM.insertBefore(n ? i : l, this.editorDOM.firstChild),
          this.editorDOM.appendChild(n ? l : i),
          (this.revertToLeft = !this.revertToLeft),
          this.revertDOM && (this.revertDOM.textContent = ""));
      }
    }
    if ("revertControls" in t || "renderRevertControl" in t) {
      let n = !!this.revertDOM,
        i = this.revertToA,
        l = this.renderRevert;
      ("revertControls" in t && ((n = !!t.revertControls), (i = t.revertControls == "b-to-a")),
        "renderRevertControl" in t && (l = t.renderRevertControl),
        this.setupRevertControls(n, i, l));
    }
    let r = "highlightChanges" in t,
      o = "gutter" in t,
      s = "collapseUnchanged" in t;
    if (r || o || s) {
      let n = [],
        i = [];
      if (r || o) {
        let l = this.a.state.facet(ye),
          c = o ? t.gutter !== !1 : l.markGutter,
          f = r ? t.highlightChanges !== !1 : l.highlightChanges;
        (n.push(
          ht.reconfigure([
            ye.of({ side: "a", sibling: () => this.b, highlightChanges: f, markGutter: c }),
            c ? ut : [],
          ]),
        ),
          i.push(
            ht.reconfigure([
              ye.of({ side: "b", sibling: () => this.a, highlightChanges: f, markGutter: c }),
              c ? ut : [],
            ]),
          ));
      }
      if (s) {
        let l = an.reconfigure(t.collapseUnchanged ? on(t.collapseUnchanged) : []);
        (n.push(l), i.push(l));
      }
      (this.a.dispatch({ effects: n }), this.b.dispatch({ effects: i }));
    }
    this.scheduleMeasure();
  }
  setupRevertControls(t, r, o) {
    ((this.revertToA = r),
      (this.revertToLeft = this.revertToA == (this.editorDOM.firstChild == this.a.dom.parentNode)),
      (this.renderRevert = o),
      !t && this.revertDOM
        ? (this.revertDOM.remove(), (this.revertDOM = null))
        : t && !this.revertDOM
          ? ((this.revertDOM = this.editorDOM.insertBefore(
              document.createElement("div"),
              this.editorDOM.firstChild.nextSibling,
            )),
            this.revertDOM.addEventListener("mousedown", (s) => this.revertClicked(s)),
            (this.revertDOM.className = "cm-merge-revert"))
          : this.revertDOM && (this.revertDOM.textContent = ""));
  }
  scheduleMeasure() {
    if (this.measuring < 0) {
      let t = this.dom.ownerDocument.defaultView || window;
      this.measuring = t.requestAnimationFrame(() => {
        ((this.measuring = -1), this.measure());
      });
    }
  }
  measure() {
    (ri(this.a, this.b, this.chunks), this.revertDOM && this.updateRevertButtons());
  }
  updateRevertButtons() {
    let t = this.revertDOM,
      r = t.firstChild,
      o = this.a.viewport,
      s = this.b.viewport;
    for (let n = 0; n < this.chunks.length; n++) {
      let i = this.chunks[n];
      if (i.fromA > o.to || i.fromB > s.to) break;
      if (i.fromA < o.from || i.fromB < s.from) continue;
      let l = this.a.lineBlockAt(i.fromA).top + "px";
      for (; r && +r.dataset.chunk < n;) r = ln(r);
      r && r.dataset.chunk == String(n)
        ? (r.style.top != l && (r.style.top = l), (r = r.nextSibling))
        : t.insertBefore(this.renderRevertButton(l, n), r);
    }
    for (; r;) r = ln(r);
  }
  renderRevertButton(t, r) {
    let o;
    if (this.renderRevert) o = this.renderRevert();
    else {
      o = document.createElement("button");
      let s = this.a.state.phrase("Revert this chunk");
      (o.setAttribute("aria-label", s),
        o.setAttribute("title", s),
        (o.textContent = this.revertToLeft ? "⇜" : "⇝"));
    }
    return ((o.style.top = t), o.setAttribute("data-chunk", String(r)), o);
  }
  revertClicked(t) {
    let r = t.target,
      o;
    for (; r && r.parentNode != this.revertDOM;) r = r.parentNode;
    if (r && (o = this.chunks[r.dataset.chunk])) {
      let [s, n, i, l, c, f] = this.revertToA
          ? [this.b, this.a, o.fromB, o.toB, o.fromA, o.toA]
          : [this.a, this.b, o.fromA, o.toA, o.fromB, o.toB],
        u = s.state.sliceDoc(i, Math.max(i, l - 1));
      (i != l && f <= n.state.doc.length && (u += s.state.lineBreak),
        n.dispatch({
          changes: { from: c, to: Math.min(n.state.doc.length, f), insert: u },
          userEvent: "revert",
        }),
        t.preventDefault());
    }
  }
  destroy() {
    (this.a.destroy(),
      this.b.destroy(),
      this.measuring > -1 &&
        (this.dom.ownerDocument.defaultView || window).cancelAnimationFrame(this.measuring),
      this.dom.remove());
  }
}
function ln(e) {
  let t = e.nextSibling;
  return (e.remove(), t);
}
const ui = ({ file: e }) => {
    const t = xn(),
      r = a.useRef(null),
      o = e.oldContent ?? "",
      s = e.newContent ?? "";
    return (
      a.useEffect(() => {
        const n = r.current;
        if (!n) return;
        const i = new ci({
          a: { doc: o, extensions: [gt.readOnly.of(!0), Ce.editable.of(!1), ...zr(t)] },
          b: { doc: s, extensions: [gt.readOnly.of(!0), Ce.editable.of(!1), ...zr(t)] },
          collapseUnchanged: { margin: 3, minSize: 4 },
          gutter: !0,
          highlightChanges: !0,
          orientation: "a-b",
          parent: n,
        });
        return () => {
          i.destroy();
        };
      }, [s, o, t]),
      h.jsx(U, { ref: r, sx: Xa })
    );
  },
  di = (e) => (e === "llm" ? "Assistant" : e.charAt(0).toUpperCase() + e.slice(1)),
  fi = (e) =>
    e.label
      ? e.label
      : e.changedFiles.length === 0
        ? "Project metadata"
        : `${e.changedFiles.length} changed file${e.changedFiles.length === 1 ? "" : "s"}`,
  $n = (e) => e.newPath ?? e.oldPath ?? "Unknown file",
  Un = (e) =>
    e.oldIsDeleted && !e.newIsDeleted
      ? "Added"
      : !e.oldIsDeleted && e.newIsDeleted
        ? "Deleted"
        : "Changed",
  hi = ({ file: e }) => {
    const t = e.oldKind === "text" || e.newKind === "text",
      r = Un(e);
    return h.jsxs(U, {
      sx: Fa,
      children: [
        h.jsxs(U, {
          sx: {
            display: "flex",
            alignItems: "center",
            justifyContent: "space-between",
            gap: 1,
            flexWrap: "wrap",
            padding: 1.25,
          },
          children: [
            h.jsx(G, { variant: "subtitle2", noWrap: !0, children: $n(e) }),
            h.jsx(jn, {
              label: r,
              color: r === "Added" ? "success" : r === "Changed" ? "default" : "error",
              size: "small",
            }),
          ],
        }),
        h.jsx(In, {}),
        t && h.jsx(U, { sx: { flex: 1, minHeight: 0 }, children: h.jsx(ui, { file: e }) }),
        !t &&
          h.jsx(U, {
            sx: je,
            children: h.jsx(G, {
              variant: "body2",
              color: "textSecondary",
              align: "center",
              children:
                "This file is binary or a folder. Its metadata changed, but there is no text diff to render.",
            }),
          }),
      ],
    });
  },
  pi = { Added: Go, Changed: wo, Deleted: ha },
  mi = ({
    isPending: e,
    isError: t,
    selectedVersion: r,
    metadataChanged: o,
    files: s,
    selectedFileId: n,
    onFileSelect: i,
    isContentPending: l,
    isContentError: c,
  }) => {
    const f = s.find((u) => u.fileId === n) ?? s[0] ?? null;
    return h.jsxs(U, {
      sx: Oa,
      children: [
        e && h.jsx(U, { sx: je, children: h.jsx(pt, { size: 28 }) }),
        t &&
          h.jsx(U, {
            sx: je,
            children: h.jsx(G, {
              variant: "body2",
              color: "error",
              align: "center",
              children: "Could not load this comparison.",
            }),
          }),
        !e &&
          !t &&
          r &&
          !o &&
          s.length === 0 &&
          h.jsx(U, {
            sx: je,
            children: h.jsx(G, {
              variant: "body2",
              color: "textSecondary",
              align: "center",
              children: "No changes from the previous checkpoint.",
            }),
          }),
        !e &&
          !t &&
          r &&
          o &&
          s.length === 0 &&
          h.jsx(U, {
            sx: je,
            children: h.jsx(G, {
              variant: "body2",
              color: "textSecondary",
              align: "center",
              children: "Project settings changed, but no source files changed.",
            }),
          }),
        !e &&
          !t &&
          f &&
          h.jsxs(U, {
            sx: Na,
            children: [
              h.jsx(U, {
                sx: Ba,
                children: h.jsx(Uo, {
                  value: f.fileId,
                  "aria-label": "Changed files",
                  orientation: "horizontal",
                  variant: "scrollable",
                  scrollButtons: !1,
                  sx: Wa,
                  onChange: (u, d) => {
                    i(d);
                  },
                  children: s.map((u) => {
                    const d = $n(u),
                      p = Un(u),
                      g = pi[p];
                    return h.jsx(
                      Vo,
                      {
                        value: u.fileId,
                        "aria-label": `${d} (${p})`,
                        sx: _a,
                        label: h.jsxs(U, {
                          sx: { display: "flex", alignItems: "center", gap: 0.5 },
                          children: [
                            d,
                            h.jsx(Bs, {
                              title: p,
                              placement: "bottom",
                              children: h.jsx(g, { "aria-hidden": "true" }),
                            }),
                          ],
                        }),
                      },
                      u.fileId,
                    );
                  }),
                }),
              }),
              l && h.jsx(U, { sx: je, children: h.jsx(pt, { size: 28 }) }),
              c &&
                h.jsx(U, {
                  sx: je,
                  children: h.jsx(G, {
                    variant: "body2",
                    color: "error",
                    align: "center",
                    children: "Could not load this file comparison.",
                  }),
                }),
              !l && !c && h.jsx(hi, { file: f }),
            ],
          }),
      ],
    });
  },
  gi = ({ onClose: e }) =>
    h.jsxs(U, {
      sx: Ea,
      children: [
        h.jsxs(U, {
          sx: { minWidth: 0, display: "flex", alignItems: "center", gap: 1 },
          children: [
            h.jsx(Ko, { size: 20, "aria-hidden": !0 }),
            h.jsxs(U, {
              sx: { minWidth: 0 },
              children: [
                h.jsx(G, { variant: "h6", noWrap: !0, children: "Project history" }),
                h.jsx(G, {
                  variant: "subtitle2",
                  color: "textSecondary",
                  noWrap: !0,
                  children: "Read-only source checkpoints · builds are shown separately",
                }),
              ],
            }),
          ],
        }),
        h.jsx(Ws, {
          tooltip: "Close project history",
          "aria-label": "Close project history",
          onClick: e,
          children: h.jsx(_s, { size: 18 }),
        }),
      ],
    }),
  vi = ({
    versions: e,
    selectedVersionId: t,
    onVersionSelect: r,
    hasNextPage: o,
    isFetchingNextPage: s,
    onLoadMore: n,
  }) =>
    h.jsxs(U, {
      sx: Da,
      children: [
        e.map((i) => {
          const l = i.id === t,
            c = i.label ?? di(i.origin);
          return h.jsx(
            Br,
            {
              sx: (f) => Ma(f, l),
              onClick: () => {
                r(i.id);
              },
              children: h.jsxs(U, {
                sx: { width: "100%" },
                children: [
                  h.jsxs(U, {
                    sx: { display: "flex", justifyContent: "space-between", gap: 1 },
                    children: [
                      h.jsx(jn, {
                        label: c,
                        size: "small",
                        color: c === "Assistant" ? "primary" : "default",
                        variant: "filled",
                      }),
                      h.jsx(G, {
                        variant: "caption",
                        color: "textSecondary",
                        noWrap: !0,
                        children: h.jsx(Qo, { date: i.lastUpdated }),
                      }),
                    ],
                  }),
                  h.jsx(G, { variant: "body2", noWrap: !0, children: fi(i) }),
                  i.changedFiles.length > 0 &&
                    h.jsx(G, {
                      variant: "caption",
                      color: "textSecondary",
                      noWrap: !0,
                      children: i.changedFiles.map((f) => f.path).join(", "),
                    }),
                ],
              }),
            },
            i.id,
          );
        }),
        o &&
          h.jsxs(Br, {
            sx: { justifyContent: "center", padding: 1 },
            disabled: s,
            onClick: n,
            children: [
              s && h.jsx(pt, { size: 18 }),
              !s && h.jsx(G, { variant: "button", children: "Load more" }),
            ],
          }),
      ],
    }),
  xi = ({ documentId: e, open: t, onClose: r }) => {
    var S, v, E, B;
    const o = oo(e, { enabled: t }),
      s = a.useMemo(() => {
        var k;
        return ((k = o.data) == null ? void 0 : k.pages.flatMap((x) => x.items)) ?? [];
      }, [(S = o.data) == null ? void 0 : S.pages]),
      [n, i] = a.useState(null),
      l = s.find((k) => k.id === n) ?? s[0] ?? null,
      c = Fr({
        documentId: e,
        versionId: (l == null ? void 0 : l.id) ?? null,
        baseVersionId: null,
        fileId: null,
        includeContent: !1,
        enabled: t,
      }),
      f = a.useMemo(() => {
        var k;
        return ((k = c.data) == null ? void 0 : k.files) ?? [];
      }, [(v = c.data) == null ? void 0 : v.files]),
      [u, d] = a.useState(null),
      p = a.useMemo(() => {
        var k, x;
        return (
          ((k = f.find((m) => m.fileId === u)) == null ? void 0 : k.fileId) ??
          ((x = f[0]) == null ? void 0 : x.fileId) ??
          null
        );
      }, [f, u]),
      g = Fr({
        documentId: e,
        versionId: (l == null ? void 0 : l.id) ?? null,
        baseVersionId: null,
        fileId: p,
        includeContent: !0,
        enabled: t && p !== null,
      }),
      w = a.useMemo(() => {
        var x;
        const k = new Map(
          (((x = g.data) == null ? void 0 : x.files) ?? []).map((m) => [m.fileId, m]),
        );
        return f.map((m) => k.get(m.fileId) ?? m);
      }, [(E = g.data) == null ? void 0 : E.files, f]);
    return h.jsxs(Yo, {
      anchor: "right",
      open: t,
      onClose: r,
      slotProps: { paper: { variant: "panel", sx: La } },
      sx: { zIndex: 9999 },
      children: [
        h.jsx(gi, { onClose: r }),
        h.jsxs(U, {
          sx: Pa,
          children: [
            o.isPending && h.jsx(U, { sx: je, children: h.jsx(pt, { size: 28 }) }),
            o.isError &&
              h.jsx(U, {
                sx: je,
                children: h.jsx(G, {
                  variant: "body2",
                  color: "error",
                  align: "center",
                  children: "Could not load project history.",
                }),
              }),
            !o.isPending &&
              !o.isError &&
              s.length === 0 &&
              h.jsx(U, {
                sx: je,
                children: h.jsx(G, {
                  variant: "body2",
                  color: "textSecondary",
                  align: "center",
                  children: "No source history has been recorded yet.",
                }),
              }),
            s.length > 0 &&
              h.jsxs(h.Fragment, {
                children: [
                  h.jsx(za, { selectedVersion: l }),
                  h.jsxs(U, {
                    sx: Aa,
                    children: [
                      h.jsx(vi, {
                        versions: s,
                        selectedVersionId: (l == null ? void 0 : l.id) ?? null,
                        onVersionSelect: (k) => {
                          i(k);
                        },
                        hasNextPage: o.hasNextPage,
                        isFetchingNextPage: o.isFetchingNextPage,
                        onLoadMore: () => {
                          o.fetchNextPage();
                        },
                      }),
                      h.jsx(mi, {
                        isPending: c.isPending,
                        isError: c.isError,
                        selectedVersion: l,
                        metadataChanged: ((B = c.data) == null ? void 0 : B.metadataChanged) ?? !1,
                        files: w,
                        selectedFileId: p,
                        onFileSelect: d,
                        isContentPending: p !== null && g.isPending,
                        isContentError: p !== null && g.isError,
                      }),
                    ],
                  }),
                ],
              }),
          ],
        }),
      ],
    });
  },
  wi = (e) => ({
    flex: "0 0 2rem",
    minHeight: "2rem",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: e.spacing(1),
    paddingInline: e.spacing(1.25),
    borderTop: `1px solid ${we(e.palette.text.primary, 0.08)}`,
    backgroundColor: we(e.palette.background.paper, 0.94),
    overflowX: "auto",
  }),
  cn = (e) => ({
    flex: "0 0 auto",
    display: "flex",
    alignItems: "center",
    gap: e.spacing(1),
    whiteSpace: "nowrap",
  }),
  Ci = (e) =>
    e === "saving"
      ? {
          icon: h.jsx(ma, { "aria-hidden": !0, size: 14 }),
          label: "Saving…",
          color: "textSecondary",
        }
      : e === "failed"
        ? { icon: h.jsx(Bt, { "aria-hidden": !0, size: 14 }), label: "Save failed", color: "error" }
        : e === "offline"
          ? {
              icon: h.jsx(da, { "aria-hidden": !0, size: 14 }),
              label: "Offline draft",
              color: "warning",
            }
          : e === "conflict"
            ? {
                icon: h.jsx(Bt, { "aria-hidden": !0, size: 14 }),
                label: "Save conflict",
                color: "error",
              }
            : e === "dirty"
              ? {
                  icon: h.jsx(Bt, { "aria-hidden": !0, size: 14 }),
                  label: "Unsaved",
                  color: "warning",
                }
              : {
                  icon: h.jsx(Jo, { "aria-hidden": !0, size: 14 }),
                  label: e === "saved" ? "Saved" : "Ready",
                  color: "success",
                },
  yi = ({ column: e, diagnostics: t, engine: r, line: o, saveStatus: s, wordCount: n }) => {
    const i = t.filter((f) => f.severity === "error").length,
      l = t.filter((f) => f.severity === "warning").length,
      c = Ci(s);
    return h.jsxs(U, {
      component: "footer",
      sx: wi,
      children: [
        h.jsxs(U, {
          sx: cn,
          role: "status",
          "aria-live": "polite",
          "aria-label": `Document status: ${c.label}`,
          children: [c.icon, h.jsx(G, { variant: "caption", color: c.color, children: c.label })],
        }),
        h.jsxs(U, {
          sx: cn,
          children: [
            h.jsxs(G, {
              variant: "caption",
              color: i ? "error" : "textSecondary",
              children: [i, " errors"],
            }),
            h.jsxs(G, {
              variant: "caption",
              color: l ? "warning" : "textSecondary",
              children: [l, " warnings"],
            }),
            h.jsxs(G, { variant: "caption", children: ["Ln ", o, ", Col ", e] }),
            h.jsxs(G, { variant: "caption", children: [n, " ", n === 1 ? "word" : "words"] }),
            h.jsx(G, { variant: "caption", children: r }),
          ],
        }),
      ],
    });
  },
  bi = ({ createProjectRequested: e, onCreateProjectRequestHandled: t }) => {
    const [r, o] = a.useState(!1),
      [s, n] = a.useState(!1),
      [i, l] = a.useState(!1),
      [c, f] = a.useState(!1),
      [u, d] = a.useState(!1),
      [p, g] = a.useState(null),
      [w, S] = a.useState(null),
      v = a.useCallback(() => o(!0), []),
      E = a.useCallback(() => o(!1), []),
      B = a.useCallback(() => n(!0), []),
      k = a.useCallback(() => n(!1), []),
      x = a.useCallback(() => l(!0), []),
      m = a.useCallback(() => l(!1), []),
      C = a.useCallback(() => f(!0), []),
      O = a.useCallback(() => f(!1), []),
      j = a.useCallback(() => d(!0), []),
      N = a.useCallback(() => d(!1), []),
      _ = a.useCallback((b) => S(b), []),
      V = a.useCallback(() => S(null), []),
      P = a.useCallback((b) => g(b), []),
      R = a.useCallback(() => g(null), []);
    return (
      a.useEffect(() => {
        e && (v(), t == null || t());
      }, [e, t, v]),
      a.useEffect(() => {
        const b = (M) => {
          M.defaultPrevented ||
            M.isComposing ||
            M.repeat ||
            M.altKey ||
            !M.shiftKey ||
            M.code !== "KeyF" ||
            (!M.ctrlKey && !M.metaKey) ||
            (M.preventDefault(), C());
        };
        return (
          document.addEventListener("keydown", b),
          () => {
            document.removeEventListener("keydown", b);
          }
        );
      }, [C]),
      {
        assetPreview: w,
        closeAssetPreview: V,
        closeCreateProject: E,
        closeDeleteConfirmation: R,
        closeEditorSettings: k,
        closeKeybindings: m,
        closeProjectSearch: O,
        closeRenameProject: N,
        deleteConfirmation: p,
        isCreateProjectOpen: r,
        isEditorSettingsOpen: s,
        isKeybindingsOpen: i,
        isProjectSearchOpen: c,
        isRenameProjectOpen: u,
        openEditorSettings: B,
        openKeybindings: x,
        openProjectSearch: C,
        openRenameProject: j,
        previewAsset: _,
        requestDelete: P,
      }
    );
  },
  Si = 900,
  un = (e, t) => (e ? (t ? "unsaved" : "saved") : "idle"),
  ki = (e) => (e instanceof Error ? e : new Error("Could not save LaTeX changes.")),
  ji = ({
    scopeKey: e,
    snapshot: t,
    snapshotVersion: r,
    isDirty: o,
    saveSnapshot: s,
    autoSaveEnabled: n = !0,
    delayMs: i = Si,
  }) => {
    if (!Number.isFinite(i) || i < 0)
      throw new Error("LaTeX autosave delay must be a non-negative number.");
    if (e !== null && e.trim().length === 0)
      throw new Error("LaTeX autosave scope key must not be blank.");
    if (e && (t === null || r === null || r.trim().length === 0))
      throw new Error("LaTeX autosave requires a snapshot and version for an active scope.");
    const [l, c] = a.useState(() => un(e, o)),
      [f, u] = a.useState(null),
      [d, p] = a.useState(null),
      [g, w] = a.useState(null),
      [S, v] = a.useState(() => (e && !o ? r : null)),
      [E, B] = a.useState(!1),
      k = a.useRef(null),
      x = a.useRef(null),
      m = a.useRef(e),
      C = a.useRef(null),
      O = a.useRef(0),
      j = a.useRef(e),
      N = a.useRef(t),
      _ = a.useRef(r),
      V = a.useRef(o),
      P = a.useRef(n),
      R = a.useRef(E),
      b = a.useRef(S),
      M = a.useRef(s),
      y = t !== null;
    ((j.current = e),
      (N.current = t),
      (_.current = r),
      (V.current = o),
      (P.current = n),
      (R.current = E),
      (b.current = S),
      (M.current = s));
    const H = a.useCallback(() => {
        k.current !== null && (window.clearTimeout(k.current), (k.current = null));
      }, []),
      I = a.useCallback(() => {
        ((R.current = !0), H(), B(!0), x.current || c(V.current ? "unsaved" : "saved"));
      }, [H]),
      X = a.useCallback(() => {
        ((R.current = !1), B(!1));
      }, []),
      $ = a.useCallback(async () => {
        const ae = x.current;
        ae && (await ae);
      }, []),
      Q = a.useCallback(
        async function ae(le = "autosave") {
          H();
          const fe = x.current;
          if (fe) {
            try {
              await fe;
            } catch {}
            return ae(le);
          }
          const ce = j.current,
            he = N.current,
            F = _.current,
            A = he !== null && F !== null && V.current;
          if (!ce || !A || (le === "autosave" && b.current === F)) {
            ce && c("saved");
            return;
          }
          const L = O.current,
            D = M.current(he, le);
          ((x.current = D), c("saving"), u(null));
          try {
            const z = await D;
            return (
              O.current === L &&
                j.current === ce &&
                ((C.current = null),
                (b.current = F),
                v(F),
                p(Date.now()),
                w(ce),
                c(_.current === F ? "saved" : P.current && !R.current ? "scheduled" : "unsaved")),
              z
            );
          } catch (z) {
            const ne = ki(z);
            throw (
              O.current === L && j.current === ce && ((C.current = F), c("failed"), u(ne)),
              ne
            );
          } finally {
            x.current === D && (x.current = null);
          }
        },
        [H],
      ),
      q = a.useCallback(
        (ae = _.current) => {
          if (!ae)
            throw new Error("A snapshot version is required to mark LaTeX changes as saved.");
          (H(),
            (C.current = null),
            (b.current = ae),
            v(ae),
            p(Date.now()),
            w(j.current),
            c("saved"),
            u(null));
        },
        [H],
      );
    return (
      a.useEffect(() => {
        if (m.current !== e) {
          ((m.current = e),
            (O.current += 1),
            H(),
            (C.current = null),
            v(e && !o ? r : null),
            p(null),
            w(null),
            u(null),
            c(un(e, o)));
          return;
        }
        if (!e || !y || r === null) {
          (H(), c("idle"));
          return;
        }
        if (E) {
          (H(), x.current || c(o ? "unsaved" : "saved"));
          return;
        }
        if (!o || S === r) {
          (H(), (C.current = null), c("saved"), u(null));
          return;
        }
        if (!n) {
          (H(), c("unsaved"));
          return;
        }
        if (C.current !== r)
          return (
            c("scheduled"),
            (k.current = window.setTimeout(() => {
              ((k.current = null), Q().catch(() => {}));
            }, i)),
            H
          );
      }, [n, H, i, o, y, E, Q, S, e, r]),
      a.useEffect(
        () => () => {
          ((O.current += 1), H());
        },
        [H],
      ),
      {
        status: l,
        error: f,
        lastSavedAt: d,
        lastSavedScopeKey: g,
        savedSnapshotVersion: S,
        hasUnsavedChanges: !!(e && t !== null && r !== null && o && S !== r),
        saveNow: Q,
        retrySave: Q,
        markSaved: q,
        cancelScheduledSave: H,
        pause: I,
        resume: X,
        waitForActiveSave: $,
      }
    );
  },
  Ii = 180,
  Ft = (e) =>
    e instanceof Error ? e : new Error("Could not access the local LaTeX recovery draft."),
  Ri = (e, t) =>
    e.fileName === t.fileName &&
    e.source === t.source &&
    (e.projectName === void 0 || t.projectName === void 0 || e.projectName === t.projectName),
  dn = (e, t) => JSON.stringify([e.fileName, e.projectName ?? null, Zt(e.source), t]),
  Li = ({
    recoveryKey: e,
    baseline: t,
    draft: r,
    hasUnsavedChanges: o,
    onRestore: s,
    enabled: n = !0,
    writeDelayMs: i = Ii,
    store: l = yo,
  }) => {
    if (!Number.isFinite(i) || i < 0)
      throw new Error("LaTeX recovery draft write delay must be a non-negative number.");
    if (e && (!t || !r))
      throw new Error("LaTeX recovery requires a baseline and draft for an active file.");
    const c = e ? Co(e) : null,
      [f, u] = a.useState({ keyId: null, status: "idle", recoveredDraft: null, error: null }),
      [d, p] = a.useState(0),
      g = a.useRef(null),
      w = a.useRef(null),
      S = a.useRef(c),
      v = a.useRef({ keyId: null, present: !1 }),
      E = a.useRef({ keyId: null, signature: null }),
      B = a.useRef({
        keyId: c,
        recoveryKey: e,
        baseline: t,
        draft: r,
        hasUnsavedChanges: o,
        enabled: n,
      }),
      k =
        f.keyId === c
          ? f
          : { keyId: c, status: n && c ? "loading" : "idle", recoveredDraft: null, error: null },
      x = a.useRef(k);
    ((S.current = c),
      (B.current = {
        keyId: c,
        recoveryKey: e,
        baseline: t,
        draft: r,
        hasUnsavedChanges: o,
        enabled: n,
      }),
      (x.current = k));
    const m = a.useCallback(() => {
        g.current !== null && (window.clearTimeout(g.current), (g.current = null));
      }, []),
      C = a.useCallback(
        async function P() {
          m();
          const R = w.current;
          if (R) {
            try {
              await R;
            } catch {}
            return P();
          }
          const b = B.current;
          if (
            !b.enabled ||
            !b.keyId ||
            !b.recoveryKey ||
            !b.baseline ||
            !b.draft ||
            !b.hasUnsavedChanges ||
            x.current.status !== "ready" ||
            x.current.recoveredDraft
          )
            return;
          const M = dn(b.draft, b.baseline.revision);
          if (
            v.current.keyId === b.keyId &&
            v.current.present &&
            E.current.keyId === b.keyId &&
            E.current.signature === M
          )
            return;
          const y = {
              schemaVersion: 1,
              ...b.recoveryKey,
              fileName: b.draft.fileName,
              projectName: b.draft.projectName,
              source: b.draft.source,
              baseRevision: b.baseline.revision,
              updatedAt: Date.now(),
            },
            H = l.write(y);
          ((w.current = H),
            u({ keyId: b.keyId, status: "writing", recoveredDraft: null, error: null }));
          try {
            (await H,
              S.current === b.keyId &&
                ((v.current = { keyId: b.keyId, present: !0 }),
                (E.current = { keyId: b.keyId, signature: M }),
                u({ keyId: b.keyId, status: "ready", recoveredDraft: null, error: null })));
          } catch (I) {
            const X = Ft(I);
            throw (
              S.current === b.keyId &&
                u({ keyId: b.keyId, status: "failed", recoveredDraft: null, error: X }),
              X
            );
          } finally {
            w.current === H && (w.current = null);
          }
        },
        [m, l],
      ),
      O = a.useCallback(async () => {
        const P = B.current;
        if (!P.keyId || !P.recoveryKey) return;
        m();
        const R = w.current;
        if (R)
          try {
            await R;
          } catch {}
        const b = x.current.recoveredDraft;
        u({ keyId: P.keyId, status: "removing", recoveredDraft: null, error: null });
        try {
          (await l.remove(P.recoveryKey),
            S.current === P.keyId &&
              ((v.current = { keyId: P.keyId, present: !1 }),
              (E.current = { keyId: P.keyId, signature: null }),
              u({ keyId: P.keyId, status: "ready", recoveredDraft: null, error: null })));
        } catch (M) {
          const y = Ft(M);
          throw (
            S.current === P.keyId &&
              u({ keyId: P.keyId, status: "failed", recoveredDraft: b, error: y }),
            y
          );
        }
      }, [m, l]),
      j = a.useCallback(() => {
        var R;
        const P = x.current.recoveredDraft;
        if (!P) throw new Error("There is no local LaTeX recovery draft to restore.");
        (s({
          fileName: P.fileName,
          projectName: P.projectName ?? ((R = B.current.draft) == null ? void 0 : R.projectName),
          source: P.source,
        }),
          u({ keyId: x.current.keyId, status: "ready", recoveredDraft: null, error: null }));
      }, [s]),
      N = a.useCallback(async () => {
        if (!x.current.recoveredDraft)
          throw new Error("There is no local LaTeX recovery draft to discard.");
        await O();
      }, [O]),
      _ = a.useCallback(() => {
        m();
        const P = x.current;
        if (
          P.error instanceof bo &&
          (P.error.operation === "write" || P.error.operation === "remove")
        ) {
          u({ keyId: P.keyId, status: "ready", recoveredDraft: P.recoveredDraft, error: null });
          return;
        }
        p((R) => R + 1);
      }, [m]);
    (a.useEffect(() => {
      m();
      const P = B.current;
      if (!n || !c || !P.recoveryKey || !P.baseline) {
        ((v.current = { keyId: c, present: !1 }),
          (E.current = { keyId: c, signature: null }),
          u({ keyId: c, status: "idle", recoveredDraft: null, error: null }));
        return;
      }
      const R = P.recoveryKey,
        b = P.baseline;
      let M = !1;
      return (
        u({ keyId: c, status: "loading", recoveredDraft: null, error: null }),
        (async () => {
          try {
            let y = await l.read(R);
            if (M) return;
            if (
              ((v.current = { keyId: c, present: y !== null }),
              (E.current = { keyId: c, signature: y ? dn(y, y.baseRevision) : null }),
              y && Ri(y, b))
            ) {
              if ((await l.remove(R), M)) return;
              ((y = null),
                (v.current = { keyId: c, present: !1 }),
                (E.current = { keyId: c, signature: null }));
            }
            u({ keyId: c, status: "ready", recoveredDraft: y, error: null });
          } catch (y) {
            if (M) return;
            u({ keyId: c, status: "failed", recoveredDraft: null, error: Ft(y) });
          }
        })(),
        () => {
          M = !0;
        }
      );
    }, [
      t == null ? void 0 : t.fileName,
      t == null ? void 0 : t.projectName,
      t == null ? void 0 : t.revision,
      t == null ? void 0 : t.source,
      m,
      n,
      c,
      e == null ? void 0 : e.fileId,
      e == null ? void 0 : e.workspaceId,
      d,
      l,
    ]),
      a.useEffect(() => {
        if (!(!n || !c || k.status !== "ready" || k.recoveredDraft)) {
          if (!o) {
            v.current.keyId === c && v.current.present && O().catch(() => {});
            return;
          }
          return (
            (g.current = window.setTimeout(() => {
              ((g.current = null), C().catch(() => {}));
            }, i)),
            m
          );
        }
      }, [
        m,
        O,
        r == null ? void 0 : r.fileName,
        r == null ? void 0 : r.projectName,
        r == null ? void 0 : r.source,
        n,
        o,
        c,
        C,
        k.recoveredDraft,
        k.status,
        i,
      ]));
    const V = a.useRef(C);
    return (
      (V.current = C),
      a.useEffect(() => {
        if (!n || typeof document > "u") return;
        const P = () => {
            V.current().catch(() => {});
          },
          R = () => {
            document.visibilityState === "hidden" && P();
          };
        return (
          document.addEventListener("visibilitychange", R),
          window.addEventListener("pagehide", P),
          () => {
            (document.removeEventListener("visibilitychange", R),
              window.removeEventListener("pagehide", P));
          }
        );
      }, [n]),
      a.useEffect(
        () => () => {
          m();
        },
        [m],
      ),
      {
        status: k.status,
        error: k.error,
        recoveredDraft: k.recoveredDraft,
        hasRecoveredDraft: k.recoveredDraft !== null,
        isRecoveryFromCurrentRevision:
          k.recoveredDraft !== null &&
          k.recoveredDraft.baseRevision === (t == null ? void 0 : t.revision),
        persistCurrentDraft: C,
        clearRecoveryDraft: O,
        restoreRecoveredDraft: j,
        discardRecoveredDraft: N,
        retryStorage: _,
      }
    );
  },
  Xt = (e, t, r) => ({
    documentId: e.id,
    fileId: t.id,
    fileRevision: t.revision,
    baseDocumentRevision: r,
    baselineProjectName: e.name,
    baselineSource: t.content ?? "",
    projectName: e.name,
    source: t.content ?? "",
  }),
  fn = (e, t, r) => !!(e && t && r && e.documentId === t.id && e.fileId === r.id),
  Ei = (e) => e.projectName !== e.baselineProjectName || e.source !== e.baselineSource,
  Pi = ({
    document: e,
    documentRevision: t,
    file: r,
    historySessionId: o,
    updateDocument: s,
    updateFile: n,
  }) => {
    const [i, l] = a.useState(null),
      c = a.useRef((e == null ? void 0 : e.id) ?? null);
    c.current = (e == null ? void 0 : e.id) ?? null;
    const f = t ?? (e == null ? void 0 : e.revision) ?? null,
      u = a.useMemo(
        () => (!e || !r || f === null ? null : fn(i, e, r) ? i : Xt(e, r, f)),
        [f, e, r, i],
      ),
      d = u ? Ei(u) : !1;
    a.useEffect(() => {
      !u ||
        !e ||
        !r ||
        f === null ||
        d ||
        (u.fileRevision === r.revision &&
          u.baseDocumentRevision === f &&
          u.baselineProjectName === e.name) ||
        l(Xt(e, r, f));
    }, [f, e, u, r, d]);
    const p = a.useCallback(
        (x) => {
          if (!e || !r || f === null)
            throw new Error("A LaTeX text file must be selected before editing.");
          l((m) => x(fn(m, e, r) ? m : Xt(e, r, f)));
        },
        [f, e, r],
      ),
      g = a.useCallback(
        (x) => {
          p((m) => ({ ...m, source: x }));
        },
        [p],
      ),
      w = a.useCallback(
        (x) => {
          p((m) => ({ ...m, projectName: x }));
        },
        [p],
      ),
      S = a.useCallback(
        (x) => {
          p((m) => ({
            ...m,
            ...(x.projectName !== void 0 ? { projectName: x.projectName } : {}),
            source: x.source,
          }));
        },
        [p],
      ),
      v = a.useCallback(() => {
        const x = (e == null ? void 0 : e.id) ?? null;
        l((m) => (!x || c.current !== x || (m == null ? void 0 : m.documentId) !== x ? m : null));
      }, [e == null ? void 0 : e.id]),
      E = a.useCallback(
        (x) => {
          if (!Number.isInteger(x) || x < 1)
            throw new Error("A valid LaTeX project revision is required.");
          const m = (e == null ? void 0 : e.id) ?? null;
          l((C) =>
            !m || c.current !== m || (C == null ? void 0 : C.documentId) !== m
              ? C
              : { ...C, baseDocumentRevision: x },
          );
        },
        [e == null ? void 0 : e.id],
      ),
      B = a.useMemo(
        () =>
          u && r
            ? {
                documentId: u.documentId,
                fileId: u.fileId,
                expectedRevision: u.baseDocumentRevision,
                filePath: r.path,
                projectName: u.projectName,
                source: u.source,
                historySessionId: o ?? `${u.documentId}:${u.fileId}`,
              }
            : null,
        [u, r, o],
      ),
      k = a.useCallback(
        async (x, m) => {
          if (!e || !r || x.documentId !== e.id || x.fileId !== r.id)
            throw new Error("The active LaTeX file changed before its save could start.");
          const C = u;
          if (!C) throw new Error("The active LaTeX draft is unavailable.");
          const j = x.projectName !== C.baselineProjectName ? kn(x.projectName) : null;
          let N = x.expectedRevision,
            _ = C.fileRevision;
          if (x.source !== C.baselineSource || m !== "autosave") {
            const R = await n({
                documentId: x.documentId,
                fileId: x.fileId,
                input: {
                  content: x.source,
                  expectedRevision: N,
                  historyOrigin: m,
                  historySessionId: x.historySessionId,
                },
              }),
              b = R.documentRevision,
              M = R.file.revision;
            ((N = b),
              (_ = M),
              l((y) =>
                !y || y.documentId !== x.documentId || y.fileId !== x.fileId
                  ? y
                  : { ...y, fileRevision: M, baseDocumentRevision: b, baselineSource: x.source },
              ));
          }
          let P = { ...e, revision: N, ...(r.path === e.mainFilePath ? { source: x.source } : {}) };
          return (
            j !== null &&
              ((P = await s({
                documentId: x.documentId,
                input: {
                  name: j,
                  expectedRevision: N,
                  historyOrigin: m,
                  historySessionId: x.historySessionId,
                },
              })),
              (N = P.revision)),
            l((R) =>
              !R || R.documentId !== x.documentId || R.fileId !== x.fileId
                ? R
                : {
                    ...R,
                    fileRevision: _,
                    baseDocumentRevision: N,
                    baselineProjectName: P.name,
                    baselineSource: x.source,
                    projectName: R.projectName === x.projectName ? P.name : R.projectName,
                  },
            ),
            { ...P, revision: N }
          );
        },
        [e, u, r, s, n],
      );
    return {
      draftProjectName: (u == null ? void 0 : u.projectName) ?? "",
      draftSource: (u == null ? void 0 : u.source) ?? "",
      baselineProjectName: (u == null ? void 0 : u.baselineProjectName) ?? "",
      baselineSource: (u == null ? void 0 : u.baselineSource) ?? "",
      baseDocumentRevision: (u == null ? void 0 : u.baseDocumentRevision) ?? null,
      isDirty: d,
      snapshot: B,
      setDraftProjectName: w,
      setDraftSource: g,
      restoreDraft: S,
      resetDraft: v,
      rebaseDraft: E,
      saveSnapshot: k,
    };
  },
  hn = (e) =>
    e instanceof Error ? e : new Error("Could not complete the pending LaTeX workspace action."),
  pn = (e, t) => {
    ((e.current = null), t(!1));
  },
  Ti = ({ hasUnsavedChanges: e, saveChanges: t, enabled: r = !0, onBlock: o, onUnblock: s }) => {
    const [n, i] = a.useState(!1),
      [l, c] = a.useState(!1),
      [f, u] = a.useState(null),
      d = a.useRef(null),
      p = a.useRef(r && e),
      g = a.useRef(o),
      w = a.useRef(s),
      S = a.useRef(!1);
    ((p.current = r && e), (g.current = o), (w.current = s));
    const v = a.useCallback(() => {
        var _;
        S.current || ((S.current = !0), (_ = g.current) == null || _.call(g));
      }, []),
      E = a.useCallback(() => {
        var _;
        S.current && ((S.current = !1), (_ = w.current) == null || _.call(w));
      }, []),
      B = a.useCallback(() => p.current, []),
      k = Zo({ shouldBlockFn: B, enableBeforeUnload: B, disabled: !r, withResolver: !0 }),
      x = a.useCallback(
        async (_) => {
          if (!p.current) return (await _(), !0);
          if (d.current || k.status === "blocked")
            throw new Error(
              "Resolve the current unsaved-changes prompt before starting another transition.",
            );
          return (
            v(),
            u(null),
            i(!0),
            new Promise((V, P) => {
              d.current = { action: _, resolve: V, reject: (R) => P(R) };
            })
          );
        },
        [v, k.status],
      ),
      m = a.useCallback(async () => {
        if (k.status === "blocked") return (k.proceed(), !0);
        const _ = d.current;
        if (!_) return !1;
        pn(d, i);
        try {
          return (await _.action(), _.resolve(!0), !0);
        } catch (V) {
          const P = hn(V);
          return (u(P), _.reject(P), !1);
        }
      }, [k]),
      C = a.useCallback(() => {
        if ((u(null), k.status === "blocked")) {
          k.reset();
          return;
        }
        const _ = d.current;
        _ && (pn(d, i), _.resolve(!1));
      }, [k]),
      O = a.useCallback(async () => {
        if (!t)
          throw new Error("A save function is required to save before leaving the LaTeX editor.");
        (c(!0), u(null));
        try {
          return (await t(), await m());
        } catch (_) {
          const V = hn(_);
          return (u(V), !1);
        } finally {
          c(!1);
        }
      }, [m, t]);
    a.useEffect(
      () => () => {
        const _ = d.current;
        _ && (_.resolve(!1), (d.current = null));
      },
      [],
    );
    const j = k.status === "blocked" ? "router" : n ? "guarded-action" : null,
      N = j !== null;
    return (
      a.useEffect(() => {
        if (N) {
          v();
          return;
        }
        E();
      }, [N, v, E]),
      a.useEffect(() => {
        !e && N && m();
      }, [e, N, m]),
      {
        isBlocked: N,
        transitionKind: j,
        pendingPathname: k.status === "blocked" ? k.next.pathname : null,
        isSavingBeforeTransition: l,
        error: f,
        runGuardedAction: x,
        saveAndProceed: O,
        discardAndProceed: m,
        stayOnCurrentFile: C,
        stay: C,
      }
    );
  },
  Ai = (e) => {
    var r;
    const t = (r = e.path.split(".").at(-1)) == null ? void 0 : r.toLowerCase();
    return t === "tex" || t === "ltx"
      ? "tex"
      : ["bbx", "bib", "bst", "cbx", "lbx"].includes(t ?? "")
        ? "bibliography"
        : [
              "cfg",
              "clo",
              "def",
              "dtx",
              "enc",
              "fd",
              "ins",
              "ist",
              "map",
              "pgf",
              "sty",
              "tikz",
              "xdy",
            ].includes(t ?? "")
          ? "style"
          : t === "cls"
            ? "class"
            : ["png", "jpg", "jpeg", "svg", "eps", "webp"].includes(t ?? "")
              ? "image"
              : t === "pdf"
                ? "pdf"
                : ["csv", "tsv", "dat", "json"].includes(t ?? "")
                  ? "data"
                  : "other";
  },
  Di = (e) =>
    e.kind === "text"
      ? `text:${Zt(e.content ?? "")}`
      : e.kind === "binary"
        ? `binary:${e.attachmentId ?? ""}:${e.sizeBytes}`
        : "folder",
  Mi = (e) =>
    e
      .map((t) => ({ id: t.id, path: t.path, kind: t.kind, contentIdentity: Di(t) }))
      .sort((t, r) => t.path.localeCompare(r.path)),
  Oi = ({
    activeSourceFingerprint: e,
    activeFileId: t,
    compiler: r,
    documentId: o,
    fileVersions: s,
    mainFilePath: n,
  }) => {
    const i = `text:${e}`;
    return JSON.stringify([
      o,
      r,
      n,
      s.map((l) => [l.id, l.path, l.kind, l.id === t ? i : l.contentIdentity]),
    ]);
  },
  Ni = (e, t) => {
    const r = e ?? [],
      o = t ?? [];
    return (
      r.length === o.length &&
      r.every((s, n) => {
        const i = o[n];
        return (
          s.path === i.path &&
          s.content === i.content &&
          s.kind === i.kind &&
          s.mediaType === i.mediaType
        );
      })
    );
  },
  Bi = ({ document: e, documentRevision: t, file: r, files: o }) => {
    const s = a.useId(),
      n = (e == null ? void 0 : e.id) ?? null,
      i = (r == null ? void 0 : r.id) ?? null,
      l = a.useMemo(() => (n && i ? `${n}:${i}:${s}` : null), [n, i, s]),
      c = ao(),
      f = io(),
      {
        baseDocumentRevision: u,
        baselineProjectName: d,
        baselineSource: p,
        draftProjectName: g,
        draftSource: w,
        isDirty: S,
        rebaseDraft: v,
        resetDraft: E,
        restoreDraft: B,
        saveSnapshot: k,
        setDraftSource: x,
        snapshot: m,
      } = Pi({
        document: e,
        documentRevision: t,
        file: r,
        historySessionId: l,
        updateDocument: c.mutateAsync,
        updateFile: f.mutateAsync,
      }),
      C = a.useMemo(() => Zt(w), [w]),
      O = m ? [m.expectedRevision, m.fileId, m.projectName, C].join(":") : null,
      j = ji({
        scopeKey: e && r ? `${e.id}:${r.id}` : null,
        snapshot: m,
        snapshotVersion: O,
        isDirty: S,
        saveSnapshot: k,
      }),
      { resume: N, saveNow: _ } = j,
      [V, P] = a.useState(null),
      R = a.useCallback(
        (Q) => {
          (v(Q), N(), P(Q));
        },
        [v, N],
      );
    a.useEffect(() => {
      V !== null && (P(null), _("manual").catch(() => {}));
    }, [V, _]);
    const b = Li({
        recoveryKey: e && r ? { workspaceId: e.id, fileId: r.id } : null,
        baseline:
          r && u !== null
            ? { fileName: r.path, projectName: d, source: p, revision: `${u}:${r.revision}` }
            : null,
        draft: r ? { fileName: r.path, projectName: g, source: w } : null,
        hasUnsavedChanges: j.hasUnsavedChanges,
        onRestore: B,
      }),
      M = Ti({
        hasUnsavedChanges: j.hasUnsavedChanges,
        saveChanges: () => _("manual"),
        onBlock: j.pause,
        onUnblock: j.resume,
      }),
      y = a.useRef(null),
      H = a.useMemo(() => {
        const Q = {
            mainFilePath: e == null ? void 0 : e.mainFilePath,
            files: o
              .filter((oe) => oe.kind !== "folder" && oe.id !== (r == null ? void 0 : r.id))
              .map((oe) => ({
                path: oe.path,
                content: oe.content ?? void 0,
                kind: Ai(oe),
                mediaType: oe.contentType ?? void 0,
              })),
          },
          q = y.current;
        return q !== null && q.mainFilePath === Q.mainFilePath && Ni(q.files, Q.files)
          ? q
          : ((y.current = Q), Q);
      }, [e == null ? void 0 : e.mainFilePath, r == null ? void 0 : r.id, o]),
      I = a.useMemo(() => Mi(o), [o]),
      X = a.useMemo(
        () =>
          Oi({
            activeFileId: (r == null ? void 0 : r.id) ?? null,
            activeSourceFingerprint: C,
            compiler: (e == null ? void 0 : e.compiler) ?? null,
            documentId: (e == null ? void 0 : e.id) ?? null,
            fileVersions: I,
            mainFilePath: (e == null ? void 0 : e.mainFilePath) ?? null,
          }),
        [
          e == null ? void 0 : e.compiler,
          e == null ? void 0 : e.id,
          e == null ? void 0 : e.mainFilePath,
          C,
          r == null ? void 0 : r.id,
          I,
        ],
      ),
      $ = a.useCallback(
        async (Q = "manual") => {
          if (!e) throw new Error("A LaTeX project must be selected before saving.");
          return (await _(Q)) ?? { ...e, name: g || e.name, revision: t ?? e.revision };
        },
        [e, t, g, _],
      );
    return {
      autosave: j,
      draftProjectName: g,
      draftSource: w,
      projectContentVersion: X,
      projectContext: H,
      rebaseDraft: v,
      rebaseDraftAndSave: R,
      recovery: b,
      resetDraft: E,
      saveCurrentDocument: $,
      setDraftSource: x,
      unsavedChanges: M,
      updateDocumentMutation: c,
      updateFileMutation: f,
    };
  },
  Wi = ({
    closeProjectSearch: e,
    files: t,
    onFileIdChange: r,
    previewAsset: o,
    selectedDocument: s,
    selectedFile: n,
    stayOnCurrentFile: i,
  }) => {
    const l = a.useRef(null),
      c = a.useRef(null),
      f = a.useCallback(() => {
        const m = l.current;
        return !m || m.fileId !== (n == null ? void 0 : n.id) || !m.view.dom.isConnected
          ? null
          : m.view;
      }, [n == null ? void 0 : n.id]),
      u = a.useCallback(
        (m) => {
          const C = f();
          if (!C) return !1;
          const O = Math.min(Math.max(0, m), C.state.doc.length);
          return (
            C.dispatch({
              selection: { anchor: O },
              effects: Ce.scrollIntoView(O, { y: "center" }),
            }),
            C.focus(),
            !0
          );
        },
        [f],
      ),
      d = a.useCallback(
        (m, C = 0) => {
          const O = f();
          if (!O) return !1;
          const j = O.state.doc.line(Math.min(Math.max(1, m), O.state.doc.lines)),
            N = Math.min(j.to, j.from + Math.max(0, C));
          return u(N);
        },
        [f, u],
      ),
      p = a.useCallback(
        (m) => {
          const C = c.current;
          (C == null ? void 0 : C.fileId) === m &&
            d(C.line, C.column) &&
            c.current === C &&
            (c.current = null);
        },
        [d],
      );
    a.useEffect(() => {
      var O;
      const m = n == null ? void 0 : n.id;
      if (!m || ((O = c.current) == null ? void 0 : O.fileId) !== m) return;
      const C = window.setTimeout(() => {
        p(m);
      }, 0);
      return () => {
        window.clearTimeout(C);
      };
    }, [p, n == null ? void 0 : n.id]);
    const g = a.useCallback(
        async (m) => {
          if (m.kind === "binary") {
            o(m);
            return;
          }
          if (!(m.kind !== "text" || m.id === (n == null ? void 0 : n.id))) {
            if (!s) throw new Error("A LaTeX project must be selected before opening a file.");
            try {
              ((c.current = null), await r(s.id, m.id));
            } catch (C) {
              W.error(Y(C, "Could not switch files."));
            }
          }
        },
        [r, o, s, n == null ? void 0 : n.id],
      ),
      w = a.useCallback(
        (m, C, O = 0) => {
          const j = t.find((N) => N.kind === "text" && N.path === m);
          if (!j) {
            W.error(`Could not find ${m} in this project.`);
            return;
          }
          if (j.id === (n == null ? void 0 : n.id)) {
            d(C, O) || (c.current = { column: O, fileId: j.id, line: C });
            return;
          }
          if (!s)
            throw new Error("A LaTeX project must be selected before opening a source location.");
          ((c.current = { column: O, fileId: j.id, line: C }),
            Promise.resolve(r(s.id, j.id)).catch((N) => {
              ((c.current = null), W.error(Y(N, "Could not open the source location.")));
            }));
        },
        [t, r, d, s, n == null ? void 0 : n.id],
      ),
      S = a.useCallback(() => {
        ((c.current = null), i());
      }, [i]),
      v = a.useCallback(
        (m) => {
          if (m.file) {
            const C = m.file.replace(/^\.?\//, ""),
              O = t.find((j) => j.kind === "text" && (j.path === C || C.endsWith(`/${j.path}`)));
            if (O) {
              w(O.path, m.line ?? 1, Math.max(0, (m.column ?? 1) - 1));
              return;
            }
          }
          m.line &&
            (n == null ? void 0 : n.kind) === "text" &&
            w(n.path, m.line, Math.max(0, (m.column ?? 1) - 1));
        },
        [t, w, n],
      ),
      E = a.useCallback((m) => w(m.filePath, m.line), [w]),
      B = a.useCallback(
        (m) => {
          (w(m.filePath, m.line), e());
        },
        [e, w],
      ),
      k = a.useCallback(
        (m) => {
          const C = n == null ? void 0 : n.id;
          if (!C) throw new Error("A LaTeX text file must be selected before creating its editor.");
          ((l.current = { fileId: C, view: m }), p(C));
        },
        [p, n == null ? void 0 : n.id],
      );
    return {
      getEditorSelection: a.useCallback(() => {
        const m = f();
        if (!m) return null;
        const C = m.state.selection.main;
        return { source: m.state.doc.toString(), start: C.from, end: C.to };
      }, [f]),
      handleCancelUnsavedTransition: S,
      handleCreateEditor: k,
      handleNavigateOutline: E,
      handleOpenDiagnostic: v,
      handleSearchNavigate: B,
      handleSelectFile: g,
      navigateToFileLine: w,
      revealEditorOffset: u,
    };
  },
  _i = (e) => {
    const t =
      e.errorMessage ??
      (e.status === "cancelled"
        ? "This compilation was cancelled."
        : `This compile job ended with status "${e.status}" without producing a result.`);
    return {
      success: !1,
      diagnostics: [
        {
          severity: e.status === "cancelled" ? "info" : "error",
          code: e.status === "failed" ? "compile-infrastructure" : "compile-status",
          message: t,
        },
      ],
      log: t,
      durationMs: 0,
    };
  },
  Fi = ({
    currentCompileResult: e,
    currentPdfDocumentRevision: t,
    currentPdfIsStale: r,
    currentPdfResult: o,
    selectedHistoryJob: s,
  }) => {
    if (!s) return { compileResult: e, pdfDocumentRevision: t, pdfIsStale: r, pdfResult: o };
    const n = s.result ?? _i(s),
      i = n.success && !!(n.pdfAttachmentId || n.pdfBase64);
    return {
      compileResult: n,
      pdfDocumentRevision: i ? (s.documentRevision ?? null) : null,
      pdfIsStale: i ? (s.isStale ?? !0) : !1,
      pdfResult: i ? n : null,
    };
  },
  Xi = (e) =>
    e.error instanceof mt && e.error.code === "latex_revision_conflict"
      ? "conflict"
      : e.status === "saving"
        ? "saving"
        : e.status === "failed" && e.error instanceof mt && e.error.status === null
          ? "offline"
          : e.status === "failed"
            ? "failed"
            : e.hasUnsavedChanges
              ? "dirty"
              : e.status === "saved"
                ? "saved"
                : "idle",
  zi = ({
    autosave: e,
    compileResult: t,
    currentDiagnostics: r,
    documentRevision: o,
    isPdfStale: s,
    liveDiagnosticsState: n,
    pdfDocumentRevision: i,
    pdfJobId: l,
    pdfResult: c,
    selectedFile: f,
    selectedHistoryJob: u,
  }) => {
    const d = Xi(e),
      p = a.useMemo(
        () => (n && n.fileName === (f == null ? void 0 : f.path) ? n.diagnostics : []),
        [n, f == null ? void 0 : f.path],
      ),
      g = a.useMemo(() => No(r, p), [p, r]),
      w = Fi({
        currentCompileResult: t,
        currentPdfDocumentRevision: i,
        currentPdfIsStale: s,
        currentPdfResult: c,
        selectedHistoryJob: u,
      }),
      S = w.compileResult,
      v = w.pdfResult,
      E = u ? ((S == null ? void 0 : S.diagnostics) ?? []) : g,
      B = w.pdfIsStale || !!(u && (e.hasUnsavedChanges || u.documentRevision !== o)),
      k = u ? (v ? u.id : null) : l,
      x = (f == null ? void 0 : f.path) ?? null;
    return {
      activeFileDiagnostics: a.useMemo(
        () =>
          r.filter((C) => {
            if (!C.file || !x) return !0;
            const O = C.file.replace(/^\.?\//, "");
            return O === x || O.endsWith(`/${x}`);
          }),
        [r, x],
      ),
      currentWorkspaceDiagnostics: g,
      displayedCompileResult: S,
      displayedDiagnostics: E,
      displayedPdfIsStale: B,
      displayedPdfJobId: k,
      displayedPdfResult: v,
      displayedPdfRevision: w.pdfDocumentRevision,
      saveStatus: d,
    };
  },
  mn = { display: "block", width: "100%", maxHeight: "70vh", objectFit: "contain" },
  Hi = ({ file: e, onClose: t }) => {
    var o, s;
    const r = $s(e == null ? void 0 : e.attachmentId);
    return h.jsxs(qe, {
      open: !!e,
      fullWidth: !0,
      maxWidth: "md",
      "aria-labelledby": "latex-asset-preview-title",
      onClose: t,
      children: [
        h.jsx(et, {
          id: "latex-asset-preview-title",
          children: (e == null ? void 0 : e.path) ?? "Project asset",
        }),
        h.jsxs(tt, {
          dividers: !0,
          children: [
            r.isLoading &&
              h.jsx(G, {
                variant: "body2",
                color: "textSecondary",
                align: "center",
                children: "Loading asset…",
              }),
            r.error &&
              h.jsx(qt, {
                severity: "error",
                variant: "outlined",
                children: "Could not load this project asset.",
              }),
            r.objectUrl &&
              ((o = e == null ? void 0 : e.contentType) == null
                ? void 0
                : o.startsWith("image/")) &&
              h.jsx(U, { component: "img", src: r.objectUrl, alt: e.path, sx: mn }),
            r.objectUrl &&
              (e == null ? void 0 : e.contentType) === "application/pdf" &&
              h.jsx(U, {
                component: "iframe",
                src: r.objectUrl,
                title: e.path,
                sx: { ...mn, height: "70vh", border: 0 },
              }),
            r.objectUrl &&
              !((s = e == null ? void 0 : e.contentType) != null && s.startsWith("image/")) &&
              (e == null ? void 0 : e.contentType) !== "application/pdf" &&
              h.jsxs(U, {
                children: [
                  h.jsx(qo, { size: 24 }),
                  h.jsx(G, {
                    variant: "body2",
                    color: "textSecondary",
                    children:
                      "This asset is available to the compiler. Download it to inspect its contents.",
                  }),
                ],
              }),
          ],
        }),
        h.jsxs(Ct, {
          children: [
            h.jsx(Pe, { variant: "outlined", onClick: t, children: "Close" }),
            h.jsx(Pe, {
              variant: "contained",
              disabled: !e,
              onClick: () => {
                e &&
                  r
                    .load()
                    .then((n) => {
                      vt(n, e.path.split("/").at(-1) ?? e.path);
                    })
                    .catch((n) => {
                      W.error(
                        n instanceof Error ? n.message : "Could not download the project asset.",
                      );
                    });
              },
              children: "Download",
            }),
          ],
        }),
      ],
    });
  },
  $i = ({ fontSizePx: e, onClose: t, onFontSizeChange: r }) =>
    h.jsxs(qe, {
      open: !0,
      fullWidth: !0,
      maxWidth: "xs",
      "aria-labelledby": "latex-editor-settings-title",
      onClose: t,
      children: [
        h.jsx(et, { id: "latex-editor-settings-title", children: "Editor settings" }),
        h.jsx(tt, {
          dividers: !0,
          children: h.jsxs(Ie, {
            spacing: 2,
            children: [
              h.jsx(G, {
                variant: "body2",
                color: "textSecondary",
                children:
                  "Adjust the source editor font size. The font family and line height continue to follow the application theme.",
              }),
              h.jsxs(Ie, {
                spacing: 1,
                children: [
                  h.jsx(G, { variant: "subtitle2", children: "Font size" }),
                  h.jsx(oa, {
                    "aria-label": "LaTeX editor font size",
                    min: Rn,
                    max: Ln,
                    step: 1,
                    value: e,
                    valueLabelDisplay: "on",
                    valueLabelFormat: (o) => `${o}px`,
                    onChange: (o, s) => {
                      typeof s == "number" && r(s);
                    },
                  }),
                  h.jsxs(G, {
                    variant: "caption",
                    color: "textSecondary",
                    children: [e, "px · Saved for this browser"],
                  }),
                ],
              }),
            ],
          }),
        }),
        h.jsx(Ct, { children: h.jsx(Pe, { variant: "contained", onClick: t, children: "Done" }) }),
      ],
    }),
  Ui = {
    display: "grid",
    gridTemplateColumns: "minmax(10rem, 1fr) minmax(11rem, 1fr)",
    alignItems: "center",
  },
  gn = (e) =>
    zt.reduce((t, r) => ((t[r.key] = e[r.key] || ""), t), {
      openCommandPalette: "",
      formatDocument: "",
      formatSelection: "",
      nextDiagnostic: "",
      previousDiagnostic: "",
    }),
  Vi = ({ keybindings: e, onClose: t, onSave: r }) => {
    const [o, s] = a.useState(() => gn(e)),
      [n, i] = a.useState(null),
      l = () => {
        try {
          const c = So(zt.reduce((f, u) => ((f[u.key] = o[u.key].trim() || !1), f), { ...Ht }));
          (r(c), t());
        } catch (c) {
          i(c instanceof Error ? c.message : "The keyboard shortcuts are invalid.");
        }
      };
    return h.jsxs(qe, {
      open: !0,
      fullWidth: !0,
      maxWidth: "sm",
      "aria-labelledby": "latex-keybindings-title",
      onClose: t,
      children: [
        h.jsx(et, { id: "latex-keybindings-title", children: "Keyboard shortcuts" }),
        h.jsx(tt, {
          dividers: !0,
          children: h.jsxs(Ie, {
            spacing: 1.5,
            children: [
              h.jsx(G, {
                variant: "body2",
                color: "textSecondary",
                children:
                  "Use CodeMirror-style shortcuts such as Mod-Enter, Ctrl-Space, or Shift-F8. “Mod” maps to Command on macOS and Control elsewhere. Mod-S and Mod-Enter compile the project, like Overleaf. Use the Save button when you want to save without compiling. Leave a field empty to disable that editor shortcut.",
              }),
              n && h.jsx(qt, { severity: "error", variant: "outlined", children: n }),
              zt.map((c) =>
                h.jsxs(
                  Ie,
                  {
                    spacing: 1,
                    sx: Ui,
                    children: [
                      h.jsxs(Ie, {
                        children: [
                          h.jsx(G, { variant: "subtitle2", children: c.label }),
                          h.jsx(G, {
                            variant: "caption",
                            color: "textSecondary",
                            children: c.description,
                          }),
                        ],
                      }),
                      h.jsx(xt, {
                        fullWidth: !0,
                        size: "small",
                        label: `${c.label} shortcut`,
                        value: o[c.key],
                        onChange: (f) => {
                          (i(null), s((u) => ({ ...u, [c.key]: f.target.value })));
                        },
                      }),
                    ],
                  },
                  c.key,
                ),
              ),
            ],
          }),
        }),
        h.jsxs(Ct, {
          children: [
            h.jsx(Pe, {
              variant: "outlined",
              onClick: () => {
                (s(gn(Ht)), i(null));
              },
              children: "Restore defaults",
            }),
            h.jsx(Pe, { variant: "outlined", onClick: t, children: "Cancel" }),
            h.jsx(Pe, { variant: "contained", onClick: l, children: "Save shortcuts" }),
          ],
        }),
      ],
    });
  },
  Vn = 500,
  vn = 256,
  Gi = (e) => e.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"),
  Gn = ({ caseSensitive: e = !1, query: t, useRegularExpression: r = !1, wholeWord: o = !1 }) => {
    if (!t) return null;
    if (t.length > vn) throw new Error(`Project search queries must not exceed ${vn} characters.`);
    if (r && !ko(t))
      throw new Error(
        "This regular expression can take too long to evaluate. Simplify nested repetitions and try again.",
      );
    const s = r ? t : Gi(t),
      n = o ? `(?<![\\p{L}\\p{N}_])(?:${s})(?![\\p{L}\\p{N}_])` : s;
    return new RegExp(n, e ? "gu" : "giu");
  },
  Ki = (e, t) => {
    for (const r of e.matchAll(t))
      if (!r[0])
        throw new Error("The project search expression must consume at least one character.");
  },
  Kn = (e, t, r) => [
    { path: t, content: r, kind: "tex" },
    ...((e == null ? void 0 : e.files) ?? []).filter((o) => o.path !== t && Bo(o)),
  ],
  Qi = (e, t, r, o) => {
    const s = Gn(o);
    if (!s) return [];
    const n = [],
      i = Kn(e, t, r);
    for (const l of i) {
      const c = l.content ?? "";
      let f = 1,
        u = 0,
        d = c.indexOf(`
`);
      for (const p of c.matchAll(s)) {
        if (!p[0])
          throw new Error("The project search expression must consume at least one character.");
        for (; d !== -1 && d < p.index;)
          ((f += 1),
            (u = d + 1),
            (d = c.indexOf(
              `
`,
              u,
            )));
        if (
          (n.push({
            id: `${l.path}:${p.index}:${p[0]}`,
            filePath: l.path,
            from: p.index,
            to: p.index + p[0].length,
            line: f,
            column: p.index - u + 1,
            matchedText: p[0],
            preview: c.slice(u, d === -1 ? c.length : d).trim(),
          }),
          n.length === Vn)
        )
          return n;
      }
    }
    return n;
  },
  Yi = (e, t, r, o, s) => {
    const n = Gn(o);
    return n
      ? Kn(e, t, r)
          .map((l) => {
            const c = l.content ?? "";
            Ki(c, n);
            const f = o.useRegularExpression ? c.replace(n, s) : c.replace(n, () => s);
            return f === c ? null : { path: l.path, content: f };
          })
          .filter((l) => l !== null)
      : [];
  },
  Ji = { minHeight: 0, overflow: "hidden" },
  Zi = { padding: 1.5 },
  qi = { flexWrap: "wrap" },
  el = { alignItems: "center", justifyContent: "space-between" },
  tl = { minHeight: 0, overflowY: "auto" },
  rl = { width: "100%", minWidth: 0 },
  nl = 180,
  sl = (e) => (e instanceof Error ? e.message : "The search expression is invalid."),
  ol = ({
    currentFilePath: e,
    currentSource: t,
    projectContext: r,
    onNavigate: o,
    onReplaceAll: s,
  }) => {
    const [n, i] = a.useState(""),
      [l, c] = a.useState(""),
      [f, u] = a.useState(!1),
      [d, p] = a.useState(!1),
      [g, w] = a.useState(!1),
      [S, v] = a.useState(!1),
      [E, B] = a.useState(null),
      k = a.useMemo(
        () => ({ caseSensitive: f, query: n, useRegularExpression: g, wholeWord: d }),
        [f, n, g, d],
      ),
      [x, m] = a.useState(k);
    a.useEffect(() => {
      const j = window.setTimeout(() => {
        m(k);
      }, nl);
      return () => {
        window.clearTimeout(j);
      };
    }, [k]);
    const C = x !== k,
      O = a.useMemo(() => {
        try {
          return { error: null, matches: Qi(r, e, t, x) };
        } catch (j) {
          return { error: sl(j), matches: [] };
        }
      }, [e, t, x, r]);
    return h.jsxs(Ie, {
      sx: Ji,
      children: [
        h.jsxs(Ie, {
          spacing: 1.25,
          sx: Zi,
          children: [
            h.jsx(G, { variant: "subtitle2", children: "Search project" }),
            h.jsx(xt, {
              fullWidth: !0,
              size: "small",
              value: n,
              label: "Find",
              error: !!O.error,
              helperText: O.error,
              disabled: S,
              onChange: (j) => {
                i(j.target.value);
              },
            }),
            s &&
              h.jsx(xt, {
                fullWidth: !0,
                size: "small",
                value: l,
                label: "Replace with",
                disabled: S,
                onChange: (j) => {
                  c(j.target.value);
                },
              }),
            h.jsxs(Ie, {
              direction: "row",
              spacing: 0.75,
              sx: qi,
              children: [
                h.jsx(it, {
                  size: "small",
                  variant: f ? "contained" : "outlined",
                  "aria-pressed": f,
                  disabled: S,
                  onClick: () => {
                    u((j) => !j);
                  },
                  children: "Match case",
                }),
                h.jsx(it, {
                  size: "small",
                  variant: d ? "contained" : "outlined",
                  "aria-pressed": d,
                  disabled: S,
                  onClick: () => {
                    p((j) => !j);
                  },
                  children: "Whole word",
                }),
                h.jsx(it, {
                  size: "small",
                  variant: g ? "contained" : "outlined",
                  "aria-pressed": g,
                  disabled: S,
                  onClick: () => {
                    w((j) => !j);
                  },
                  children: "Regular expression",
                }),
              ],
            }),
            h.jsxs(Ie, {
              direction: "row",
              sx: el,
              children: [
                h.jsxs(G, {
                  variant: "caption",
                  children: [
                    O.matches.length === Vn ? `${O.matches.length}+` : O.matches.length,
                    " ",
                    O.matches.length === 1 ? "match" : "matches",
                  ],
                }),
                s &&
                  h.jsx(it, {
                    size: "small",
                    disabled: !n || S || C || !!O.error || O.matches.length === 0,
                    onClick: () => {
                      (async () => {
                        (v(!0), B(null));
                        try {
                          await s(Yi(r, e, t, k, l));
                        } catch (j) {
                          B(j instanceof Error ? j.message : "Could not replace project matches.");
                        } finally {
                          v(!1);
                        }
                      })();
                    },
                    children: S ? "Replacing…" : "Replace all",
                  }),
              ],
            }),
            E && h.jsx(qt, { severity: "error", variant: "outlined", children: E }),
          ],
        }),
        h.jsx(In, {}),
        h.jsx(aa, {
          dense: !0,
          disablePadding: !0,
          sx: tl,
          children: O.matches.map((j) =>
            h.jsx(
              Fs,
              {
                onClick: () => {
                  o(j);
                },
                children: h.jsxs(Ie, {
                  sx: rl,
                  children: [
                    h.jsx(G, { variant: "body2", noWrap: !0, children: j.preview }),
                    h.jsxs(G, {
                      variant: "caption",
                      color: "textSecondary",
                      noWrap: !0,
                      children: [j.filePath, ":", j.line, ":", j.column],
                    }),
                  ],
                }),
              },
              j.id,
            ),
          ),
        }),
      ],
    });
  },
  al = { height: "min(38rem, 72vh)", display: "flex", flexDirection: "column", padding: 0 },
  il = ({
    currentFilePath: e,
    currentSource: t,
    open: r,
    projectContext: o,
    onClose: s,
    onNavigate: n,
    onReplaceAll: i,
  }) =>
    h.jsxs(qe, {
      open: r,
      fullWidth: !0,
      maxWidth: "md",
      "aria-labelledby": "latex-project-search-title",
      onClose: s,
      children: [
        h.jsx(et, {
          id: "latex-project-search-title",
          children: "Search and replace across project",
        }),
        h.jsx(tt, {
          dividers: !0,
          sx: al,
          children: h.jsx(ol, {
            currentFilePath: e,
            currentSource: t,
            projectContext: o,
            onNavigate: n,
            onReplaceAll: i,
          }),
        }),
      ],
    }),
  ll = ({ isRenaming: e, open: t, projectName: r, onCancel: o, onRename: s }) => {
    const [n, i] = a.useState(r);
    return h.jsxs(qe, {
      open: t,
      fullWidth: !0,
      maxWidth: "xs",
      "aria-labelledby": "latex-rename-project-title",
      onClose: e ? void 0 : o,
      children: [
        h.jsx(et, { id: "latex-rename-project-title", children: "Rename project" }),
        h.jsx(tt, {
          children: h.jsx(xt, {
            autoFocus: !0,
            fullWidth: !0,
            label: "Project name",
            value: n,
            disabled: e,
            onChange: (l) => {
              i(l.target.value);
            },
          }),
        }),
        h.jsxs(Ct, {
          children: [
            h.jsx(Pe, { variant: "outlined", disabled: e, onClick: o, children: "Cancel" }),
            h.jsx(Pe, {
              variant: "contained",
              disabled: e || n.trim().length === 0,
              onClick: () => {
                s(n).then((l) => {
                  l && o();
                });
              },
              children: e ? "Renaming…" : "Rename",
            }),
          ],
        }),
      ],
    });
  },
  cl = ({
    currentFilePath: e,
    dialogs: t,
    draftSource: r,
    editorFontSizePx: o,
    editorKeybindings: s,
    projectActions: n,
    projectContext: i,
    selectedDocument: l,
    onEditorFontSizeChange: c,
    onKeybindingsChange: f,
    onSearchNavigate: u,
  }) => {
    var d;
    return h.jsxs(h.Fragment, {
      children: [
        t.isCreateProjectOpen &&
          h.jsx(ea, {
            isCreating: n.isCreatingProject,
            open: !0,
            onClose: t.closeCreateProject,
            onCreate: n.createProject,
          }),
        t.isRenameProjectOpen &&
          l &&
          h.jsx(
            ll,
            {
              isRenaming: n.isRenamingProject,
              open: !0,
              projectName: l.name,
              onCancel: t.closeRenameProject,
              onRename: n.renameProject,
            },
            l.id,
          ),
        t.isKeybindingsOpen &&
          h.jsx(Vi, { keybindings: s, onClose: t.closeKeybindings, onSave: f }),
        t.isEditorSettingsOpen &&
          h.jsx($i, { fontSizePx: o, onClose: t.closeEditorSettings, onFontSizeChange: c }),
        t.isProjectSearchOpen &&
          h.jsx(il, {
            currentFilePath: e,
            currentSource: r,
            open: !0,
            projectContext: i,
            onClose: t.closeProjectSearch,
            onNavigate: u,
            onReplaceAll: (p) =>
              n.replaceAll(p).then(() => {
                t.closeProjectSearch();
              }),
          }),
        t.deleteConfirmation &&
          h.jsx(ta, {
            isDeleting: n.isDeleting,
            open: !0,
            target:
              t.deleteConfirmation.kind === "project"
                ? "this project"
                : (((d = t.deleteConfirmation.file) == null ? void 0 : d.path) ?? "this item"),
            onCancel: t.closeDeleteConfirmation,
            onConfirm: () => {
              if (t.deleteConfirmation)
                return n
                  .confirmDelete(t.deleteConfirmation)
                  .then(t.closeDeleteConfirmation)
                  .catch(() => {});
            },
          }),
        t.assetPreview && h.jsx(Hi, { file: t.assetPreview, onClose: t.closeAssetPreview }),
      ],
    });
  },
  ul = ({
    conflictCurrentRevision: e,
    hasRecoveredDraft: t,
    isConflict: r,
    isDiscardingRecovery: o,
    isRecoveryFromCurrentRevision: s,
    isSavingBeforeTransition: n,
    isTransitionBlocked: i,
    recoveredDraft: l,
    recoveryError: c,
    unsavedError: f,
    onCancelUnsavedTransition: u,
    onDiscardRecoveredDraft: d,
    onDiscardUnsavedChanges: p,
    onKeepLocalChanges: g,
    onReloadServerCopy: w,
    onRestoreRecoveredDraft: S,
    onSaveAndContinue: v,
  }) =>
    h.jsxs(h.Fragment, {
      children: [
        i &&
          h.jsx(ra, {
            error: f,
            isDiscarding: o,
            isSaving: n,
            open: !0,
            onCancel: u,
            onDiscard: p,
            onSaveAndContinue: v,
          }),
        t &&
          h.jsx(na, {
            error: c,
            isDiscarding: o,
            isCurrentRevision: s,
            open: !0,
            record: l,
            onDiscard: () => {
              d().catch(() => {});
            },
            onRestore: S,
          }),
        r &&
          h.jsx(sa, {
            currentRevision: e,
            error: c,
            isReloading: o,
            open: !0,
            onKeepLocalChanges: g,
            onReloadServerCopy: w,
          }),
      ],
    }),
  dl = ({
    requestedDocumentId: e,
    requestedFileId: t,
    onDocumentIdChange: r,
    onFileIdChange: o,
  }) => {
    var p;
    const s = lo(),
      n = a.useMemo(() => s.data ?? [], [s.data]),
      i = a.useMemo(() => n.find((g) => g.id === e) ?? n[0] ?? null, [n, e]),
      l = (i == null ? void 0 : i.id) ?? null,
      c = co(l),
      f = a.useMemo(() => {
        var g;
        return ((g = c.data) == null ? void 0 : g.files) ?? [];
      }, [(p = c.data) == null ? void 0 : p.files]),
      u = l && l === e && t ? t : void 0,
      d = a.useMemo(
        () =>
          f.find((g) => g.id === u && g.kind === "text") ??
          f.find((g) => g.path === (i == null ? void 0 : i.mainFilePath) && g.kind === "text") ??
          f.find((g) => g.kind === "text") ??
          null,
        [f, i == null ? void 0 : i.mainFilePath, u],
      );
    return (
      a.useEffect(() => {
        if (!(s.isPending || s.isError)) {
          if (i) {
            e !== i.id && r(i.id, { replace: !0 });
            return;
          }
          e !== null && n.length === 0 && r(null, { replace: !0 });
        }
      }, [n.length, s.isError, s.isPending, r, e, i]),
      a.useEffect(() => {
        if (c.isPending || c.isError || !i || e !== i.id) return;
        const g = (d == null ? void 0 : d.id) ?? null;
        t !== g && o(i.id, g, { replace: !0 });
      }, [c.isError, c.isPending, o, e, t, i, d == null ? void 0 : d.id]),
      {
        documents: n,
        documentsQuery: s,
        files: f,
        filesQuery: c,
        selectedDocument: i,
        selectedDocumentId: l,
        selectedFile: d,
      }
    );
  },
  fl = [],
  hl = ({
    documentId: e,
    documentRevision: t,
    draftSource: r,
    files: o,
    hasUnsavedChanges: s,
    isChatRunning: n,
    selectedFileId: i,
    getEditorSelection: l,
    onRefreshProject: c,
    onRevealEditorOffset: f,
    onSaveCurrentDocument: u,
    onSelectFile: d,
    onProposalApplied: p,
  }) => {
    var _e, De;
    const [g, w] = a.useState(null),
      [S, v] = a.useState(null),
      [E, B] = a.useState(!1),
      k = a.useRef(null),
      x = (g == null ? void 0 : g.documentId) === e ? g.proposalId : null,
      m = uo(e, x),
      { error: C, isPending: O, mutateAsync: j, reset: N, variables: _ } = fo(),
      { isPending: V, mutateAsync: P } = ho(),
      { error: R, isPending: b, mutateAsync: M, reset: y, variables: H } = po(),
      I = ((_e = m.data) == null ? void 0 : _e.status) === "pending" ? m.data : null,
      X = wn((I == null ? void 0 : I.validationCompileJobId) ?? null),
      $ = X.data,
      Q =
        ($ == null ? void 0 : $.status) === "completed" &&
        ((De = $.result) == null ? void 0 : De.success) === !0,
      q = a.useMemo(() => {
        var J, Z, te;
        return I
          ? I.validationErrorMessage
            ? I.validationErrorMessage
            : X.error
              ? Y(X.error, "The automatic proposal compile status could not be loaded.")
              : ($ == null ? void 0 : $.status) === "failed" ||
                  ($ == null ? void 0 : $.status) === "cancelled" ||
                  (($ == null ? void 0 : $.status) === "completed" &&
                    ((J = $.result) == null ? void 0 : J.success) !== !0)
                ? ($.errorMessage ??
                  ((te = (Z = $.result) == null ? void 0 : Z.diagnostics[0]) == null
                    ? void 0
                    : te.message) ??
                  "The proposed project did not compile successfully, so it cannot be applied.")
                : null
          : null;
      }, [I, $, X.error]),
      oe = !!I && !Q && !q,
      ae = (S == null ? void 0 : S.proposalId) === (I == null ? void 0 : I.id) ? S : null,
      le = a.useMemo(
        () => (I == null ? void 0 : I.edits.filter((J) => J.fileId === i)) ?? fl,
        [I, i],
      ),
      fe = a.useMemo(() => Wo(r, le), [r, le]),
      ce = a.useCallback(
        (J) => {
          e &&
            w((Z) =>
              (Z == null ? void 0 : Z.documentId) !== e
                ? { dismissedProposalId: null, documentId: e, proposalId: J }
                : J === Z.dismissedProposalId || J === Z.proposalId
                  ? Z
                  : { dismissedProposalId: Z.dismissedProposalId, documentId: e, proposalId: J },
            );
        },
        [e],
      ),
      he = a.useCallback(async () => {
        if (!(!e || !x))
          try {
            (await P({ documentId: e, proposalId: x }),
              w({ dismissedProposalId: x, documentId: e, proposalId: null }),
              B(!1),
              N(),
              y());
          } catch (J) {
            W.error(Y(J, "The proposed edits could not be dismissed."));
          }
      }, [x, P, e, N, y]),
      F = a.useCallback(
        (J) => {
          const Z = o.find((te) => te.kind === "text" && te.id === J);
          if (!Z) throw new Error("The proposed LaTeX file is no longer available.");
          d(Z);
        },
        [o, d],
      ),
      A = a.useCallback(
        (J) => {
          var te;
          const Z = _o(fe.ranges, ((te = l()) == null ? void 0 : te.start) ?? null, J);
          Z && f(Z.from);
        },
        [l, f, fe.ranges],
      ),
      L = a.useCallback(() => {
        A("next");
      }, [A]),
      D = a.useCallback(() => {
        A("previous");
      }, [A]),
      z = a.useCallback(async () => {
        if (!I || !e) throw new Error("The proposed LaTeX edits are unavailable.");
        if (!Q)
          throw new Error("The proposed project must compile successfully before PDF review.");
        if ((S == null ? void 0 : S.proposalId) === I.id) {
          B(!0);
          return;
        }
        y();
        try {
          const J = await M({ documentId: e, proposalId: I.id });
          (v({ proposalId: I.id, ...J }), B(!0));
        } catch {}
      }, [e, S, M, I, y, Q]),
      ne = a.useCallback(() => {
        B(!1);
      }, []);
    a.useEffect(() => {
      if (!I || k.current === I.id) return;
      if (I.edits.some((Z) => Z.fileId === i)) {
        k.current = I.id;
        return;
      }
      if (s || t !== I.baseDocumentRevision) return;
      const J = o.find((Z) => {
        var te;
        return Z.kind === "text" && Z.id === ((te = I.edits[0]) == null ? void 0 : te.fileId);
      });
      J && ((k.current = I.id), d(J));
    }, [t, o, s, d, I, i]);
    const Le = a.useCallback(async () => {
        if (!I || !e) throw new Error("The proposed LaTeX edits are unavailable.");
        if (n) {
          W.warning("Wait for the current project chat response before applying proposed edits.");
          return;
        }
        if (!Q)
          throw new Error(
            "The proposed project must compile successfully before it can be applied.",
          );
        try {
          if ((await u()).revision !== I.baseDocumentRevision) {
            (W.warning(ia), await c());
            return;
          }
          const Z = await j({ documentId: e, proposalId: I.id });
          (p(e, Z.documentRevision),
            B(!1),
            w({ dismissedProposalId: I.id, documentId: e, proposalId: null }),
            W.success("LaTeX edits applied."));
          try {
            await c();
          } catch (te) {
            W.error(Y(te, "The edits were applied, but the editor could not refresh."));
          }
        } catch (J) {
          if (Xs(J)) {
            try {
              await c();
            } catch (Z) {
              W.error(Y(Z, "The edits were not applied, and the editor could not refresh."));
            }
            return;
          }
          W.error(Y(J, "The proposed edits could not be applied."));
        }
      }, [j, e, n, c, u, p, I, Q]),
      pe = a.useMemo(
        () =>
          I
            ? {
                applyError:
                  C && (_ == null ? void 0 : _.proposalId) === I.id
                    ? Y(C, "The proposed edits could not be applied.")
                    : null,
                hasUnsavedChanges: s,
                isChatRunning: n,
                isApplying: O,
                isDismissing: V,
                isPreparingPdfPreview: b,
                isStale: t !== I.baseDocumentRevision,
                isValidationPending: oe,
                pdfPreviewError:
                  R && (H == null ? void 0 : H.proposalId) === I.id
                    ? Y(R, "The proposed PDF preview could not be prepared.")
                    : null,
                previewError: le.length > 0 ? fe.error : null,
                proposal: I,
                validationError: q,
                onApply: () => {
                  Le();
                },
                onDismiss: () => {
                  he();
                },
                onNextChange: L,
                onOpenFile: F,
                onPreviewPdf: () => {
                  z();
                },
                onPreviousChange: D,
              }
            : null,
        [
          C,
          _ == null ? void 0 : _.proposalId,
          t,
          Le,
          he,
          L,
          F,
          z,
          D,
          s,
          n,
          O,
          V,
          b,
          oe,
          R,
          H == null ? void 0 : H.proposalId,
          fe.error,
          I,
          le.length,
          q,
        ],
      ),
      re = a.useMemo(() => null, [ae, ne, E, I, $, X.error]);
    return { onProposalIdChange: ce, pdfDiff: re, proposedEdits: le, review: pe };
  },
  pl = () => {
    const e = a.useRef(null),
      [t, r] = a.useState(() => typeof window < "u" && window.innerWidth <= Xr),
      o = a.useCallback((s) => {
        if (s <= 0) return;
        const n = s <= Xr;
        r((i) => (i === n ? i : n));
      }, []);
    return (
      a.useLayoutEffect(() => {
        const s = e.current;
        if (!s) throw new Error("The LaTeX workspace container was not mounted.");
        const n = () => {
          o(s.getBoundingClientRect().width);
        };
        if ((n(), typeof ResizeObserver > "u"))
          return (
            window.addEventListener("resize", n),
            () => {
              window.removeEventListener("resize", n);
            }
          );
        const i = new ResizeObserver(([l]) => {
          o((l == null ? void 0 : l.contentRect.width) ?? s.getBoundingClientRect().width);
        });
        return (
          i.observe(s),
          () => {
            i.disconnect();
          }
        );
      }, [o]),
      { isCompactLayout: t, workspaceRef: e }
    );
  },
  Qt = (e) => (e === "pdf" ? "pdf" : "source"),
  ml = (e, t) => (e === "problems" ? Qt(t) : "problems"),
  gl = [],
  jc = ({
    requestedDocumentId: e,
    requestedFileId: t = null,
    createProjectRequested: r = !1,
    createProjectFolderId: o = null,
    headerToolbar: s,
    panelVisibility: n,
    renderProjectChat: i,
    onDocumentIdChange: l,
    onFileIdChange: c,
    onCreateProjectRequestHandled: f,
    onProjectFileActionsChange: u,
    onProjectActionsBusyChange: d,
    onProjectBusyChange: p,
    onProjectExpansionRequest: g,
    onPanelVisibilityChange: w,
  }) => {
    var Or;
    const S = xn(),
      { isCompactLayout: v, workspaceRef: E } = pl(),
      [B, k] = a.useState(null),
      x = a.useCallback(
        (T) => {
          (k(T), u == null || u(T));
        },
        [u],
      ),
      m = a.useRef(!1),
      C = a.useRef(null),
      O = a.useRef(null),
      j = a.useRef(null),
      N = a.useCallback(
        async (T, K, ee) => {
          var de;
          (T === C.current &&
            K !== O.current &&
            (await ((de = j.current) == null ? void 0 : de.call(j))),
            await c(T, K, ee));
        },
        [c],
      );
    a.useEffect(
      () => (
        (m.current = !0),
        () => {
          m.current = !1;
        }
      ),
      [],
    );
    const {
      documents: _,
      documentsQuery: V,
      files: P,
      filesQuery: R,
      selectedDocument: b,
      selectedDocumentId: M,
      selectedFile: y,
    } = dl({
      requestedDocumentId: e,
      requestedFileId: t,
      onDocumentIdChange: l,
      onFileIdChange: N,
    });
    a.useEffect(() => {
      ((C.current = M), (O.current = (y == null ? void 0 : y.id) ?? null));
    }, [M, y == null ? void 0 : y.id]);
    const H = R.refetch,
      I = V.refetch,
      X =
        ((Or = R.data) == null ? void 0 : Or.documentRevision) ??
        (b == null ? void 0 : b.revision) ??
        null,
      $ = la(n),
      [Q, q] = a.useState(() => Qt($)),
      [oe, ae] = a.useState(!1),
      [le, fe] = a.useState(!1),
      [ce, he] = a.useState("problems"),
      [F, A] = a.useState(!1),
      L = v ? Q === "source" : $ !== "pdf",
      D = v ? Q === "pdf" : $ !== "source",
      z = v ? Q === "problems" : F && !oe,
      ne = v ? Q === "chat" : n.chat,
      Le = mo(M),
      pe = bi({ createProjectRequested: r, onCreateProjectRequestHandled: f }),
      [re, _e] = a.useState(null),
      De = (re == null ? void 0 : re.mainFilePath) ?? (b == null ? void 0 : b.mainFilePath) ?? null,
      J = go({
        documentId: M,
        documentRevision: (re == null ? void 0 : re.documentRevision) ?? null,
        fileId: (y == null ? void 0 : y.id) ?? null,
      }),
      Z = vo({
        documentId: M,
        documentRevision: (re == null ? void 0 : re.documentRevision) ?? null,
        path: y && !J.isError ? null : ((y == null ? void 0 : y.path) ?? De),
      }),
      te = y && !J.isError ? J : Z,
      [Qn, yt] = a.useState(!1),
      [bt, sr] = a.useState(!1),
      Fe = a.useRef(0),
      Se = a.useCallback(() => {
        ((Fe.current += 1), _e(null));
      }, []),
      [rt, or] = a.useState(null),
      [Xe, Yn] = a.useState({
        line: 1,
        column: 1,
        cursorOffset: 0,
        selectionLength: 0,
        wordCount: 0,
        characterCount: 0,
      }),
      [Jn, Zn] = a.useState(null),
      { value: St, setValue: qn } = _r({
        storageKey: jo,
        defaultValue: Ht,
        parse: Io,
        readErrorMessage: "Ignoring unreadable LaTeX keyboard shortcuts.",
        writeErrorMessage: "Could not save LaTeX keyboard shortcuts.",
      }),
      es = a.useMemo(() => Fo(S), [S]),
      { value: kt, setValue: ts } = _r({
        storageKey: ga,
        defaultValue: es,
        parse: xa,
        readErrorMessage: "Ignoring unreadable LaTeX editor font size.",
        writeErrorMessage: "Could not save LaTeX editor font size.",
      }),
      {
        autosave: ie,
        draftSource: Ee,
        projectContentVersion: rs,
        projectContext: jt,
        rebaseDraft: ns,
        rebaseDraftAndSave: ss,
        recovery: ve,
        resetDraft: It,
        saveCurrentDocument: ke,
        setDraftSource: ar,
        unsavedChanges: Me,
        updateDocumentMutation: ir,
        updateFileMutation: lr,
      } = Bi({ document: b, documentRevision: X, file: y, files: P });
    j.current = () => ke("manual");
    const cr = a.useRef(Ee);
    cr.current = Ee;
    const os = Me.stayOnCurrentFile,
      {
        getEditorSelection: Rt,
        handleCancelUnsavedTransition: as,
        handleCreateEditor: ur,
        handleNavigateOutline: dr,
        handleOpenDiagnostic: fr,
        handleSearchNavigate: hr,
        handleSelectFile: pr,
        navigateToFileLine: mr,
        revealEditorOffset: is,
      } = Wi({
        closeProjectSearch: pe.closeProjectSearch,
        files: P,
        onFileIdChange: N,
        previewAsset: pe.previewAsset,
        selectedDocument: b,
        selectedFile: y,
        stayOnCurrentFile: os,
      }),
      {
        compileResult: nt,
        diagnostics: ls,
        isCancellingLatex: cs,
        isCompilingLatex: ze,
        isPdfStale: us,
        pdfDocumentRevision: ds,
        pdfJobId: fs,
        pdfResult: hs,
        cancelCurrentCompile: ps,
        compileCurrentDocument: st,
        resetCompileState: ms,
      } = ka({
        selectedDocument: b,
        contentVersion: rs,
        hasUnsavedChanges: ie.hasUnsavedChanges,
        loadLatestSuccessfulCompile: D,
        saveCurrentDocument: ke,
      });
    a.useEffect(() => {
      !rt ||
        M !== rt.documentId ||
        X === null ||
        X < rt.documentRevision ||
        ie.hasUnsavedChanges ||
        ze ||
        ((y == null ? void 0 : y.kind) === "text" && Ee !== (y.content ?? "")) ||
        (or(null), st(!1, !0));
    }, [ie.hasUnsavedChanges, st, X, Ee, ze, rt, M, y]);
    const {
        activeFileDiagnostics: gr,
        displayedCompileResult: ue,
        displayedDiagnostics: Lt,
        displayedPdfIsStale: Et,
        displayedPdfJobId: gs,
        displayedPdfResult: me,
        displayedPdfRevision: vr,
        saveStatus: xr,
      } = zi({
        autosave: ie,
        compileResult: nt,
        currentDiagnostics: ls,
        documentRevision: X,
        isPdfStale: us,
        liveDiagnosticsState: Jn,
        pdfDocumentRevision: ds,
        pdfJobId: fs,
        pdfResult: hs,
        selectedFile: y,
        selectedHistoryJob: re,
      }),
      vs = xr === "conflict",
      wr = a.useMemo(() => {
        if (!re) return null;
        const T = re.documentRevision ?? null,
          K = De,
          ee = (y == null ? void 0 : y.path) ?? K;
        if (T === null || !ee)
          return {
            status: "unavailable",
            revision: T,
            path: ee,
            mainFilePath: K,
            source: "",
            message: "This build does not have a persisted source revision.",
          };
        if (te.isPending)
          return { status: "loading", revision: T, path: ee, mainFilePath: K, source: "" };
        if (te.isError)
          return {
            status: "error",
            revision: T,
            path: ee,
            mainFilePath: K,
            source: "",
            message: Y(te.error, "Source history is unavailable for this build."),
          };
        const de = te.data;
        return de
          ? de.isDeleted || de.kind !== "text"
            ? {
                status: "unavailable",
                revision: T,
                path: de.path,
                mainFilePath: K,
                source: "",
                message: de.isDeleted
                  ? "This file did not exist in the selected build."
                  : "The selected build snapshot is not a text file.",
              }
            : {
                status: "ready",
                revision: T,
                path: de.path,
                mainFilePath: K,
                source: de.content ?? "",
              }
          : {
              status: "error",
              revision: T,
              path: ee,
              mainFilePath: K,
              source: "",
              message: "Source history is unavailable for this build.",
            };
      }, [te.data, te.error, te.isError, te.isPending, y, re, De]),
      Cr = Us(ue == null ? void 0 : ue.outputAttachmentId, { cacheContent: !1 });
    a.useEffect(() => {
      if (!(!nt || nt.success)) {
        if ((Se(), he("problems"), v)) {
          q("problems");
          return;
        }
        A(!0);
      }
    }, [Se, nt, v]);
    const yr = b ? `${b.name}.pdf` : "compiled.pdf",
      xs = a.useCallback(
        async (T) => {
          !m.current || !b || C.current !== b.id || (v && q("source"), await N(b.id, T.id));
        },
        [N, v, b],
      ),
      ws = a.useCallback(
        async (T) => {
          const K = M;
          !m.current || (C.current !== K && C.current !== T) || (await l(T));
        },
        [l, M],
      ),
      xe = Ia({
        createProjectFolderId: o,
        documents: _,
        files: P,
        hasUnsavedChanges: ie.hasUnsavedChanges,
        selectedDocument: b,
        selectedFile: y,
        onDocumentIdChange: ws,
        onRebaseDraft: ns,
        onResetCompileState: ms,
        onResetDraft: It,
        onSelectTextFile: xs,
        saveCurrentDocument: ke,
        updateDocument: ir.mutateAsync,
        updateFile: lr.mutateAsync,
      }),
      He = xe.isFileMutationBusy || R.isFetching || ir.isPending || lr.isPending || ze,
      br = He || ie.hasUnsavedChanges;
    (a.useEffect(() => {
      p == null || p(He);
    }, [He, p]),
      a.useEffect(() => {
        d == null || d(br);
      }, [br, d]),
      a.useEffect(
        () => () => {
          (p == null || p(!1), d == null || d(!1));
        },
        [d, p],
      ),
      a.useEffect(() => {
        (Se(), yt(!1));
      }, [Se, M]));
    const Pt = a.useCallback(async () => {
        try {
          (await ke(), W.success("Project saved."));
        } catch (T) {
          W.error(Y(T, "Could not save the LaTeX project."));
        }
      }, [ke]),
      Cs = a.useCallback(async () => {
        if (!(!b || bt)) {
          sr(!0);
          try {
            (await ke("manual"), yt(!0));
          } catch (T) {
            W.error(Y(T, "Could not save the LaTeX project before opening history."));
          } finally {
            sr(!1);
          }
        }
      }, [bt, ke, b]),
      Sr = a.useCallback(async () => {
        const T = Rt(),
          K =
            T !== null &&
            T.source ===
              cr.current.replace(
                /\r\n?/g,
                `
`,
              ) &&
            T.start !== T.end,
          ee = (y == null ? void 0 : y.id) ?? null;
        return {
          documentRevision: (await ke()).revision,
          activeFileId: ee,
          selectionStart: K ? T.start : null,
          selectionEnd: K ? T.end : null,
        };
      }, [Rt, ke, y == null ? void 0 : y.id]),
      Tt = a.useCallback(async () => {
        const [T, K] = await Promise.all([H(), I()]);
        if (T.error) throw T.error;
        if (K.error) throw K.error;
        Se();
      }, [Se, I, H]),
      ys = a.useCallback((T, K) => {
        or({ documentId: T, documentRevision: K });
      }, []),
      {
        onProposalIdChange: kr,
        proposedEdits: jr,
        review: ot,
      } = hl({
        documentId: M,
        documentRevision: X,
        draftSource: Ee,
        files: P,
        hasUnsavedChanges: ie.hasUnsavedChanges,
        isChatRunning: le,
        selectedFileId: (y == null ? void 0 : y.id) ?? null,
        getEditorSelection: Rt,
        onRefreshProject: Tt,
        onRevealEditorOffset: is,
        onSaveCurrentDocument: ke,
        onSelectFile: pr,
        onProposalApplied: ys,
      }),
      $e = a.useCallback(
        async (T = !1) => {
          (Se(), await st(T), A(!0));
        },
        [Se, st],
      );
    (Wr({ enabled: !!b, keyCode: "KeyS", onShortcut: () => $e(!1) }),
      Wr({ enabled: !!(b && !ze), keyCode: "Enter", onShortcut: () => $e(!1) }));
    const at = a.useCallback(
        (T) => {
          (ae(!1), w(T));
        },
        [w],
      ),
      At = a.useCallback(() => {
        at({ ...n, chat: !n.chat });
      }, [at, n]),
      Ue = a.useCallback(
        (T) => {
          (q(Qt(T)), at(ca(n, T)));
        },
        [at, n],
      ),
      bs = a.useCallback((T) => {
        q(T);
      }, []),
      Ss = a.useCallback(() => {
        (q("source"), ae(!0), w({ ...n, source: !0, pdf: !1 }));
      }, [w, n]),
      Ve = a.useCallback(() => {
        if (v) {
          q("pdf");
          return;
        }
        $ === "source" && Ue("pdf");
      }, [Ue, v, $]),
      Oe = a.useCallback(() => {
        if (v) {
          q("source");
          return;
        }
        $ === "pdf" && Ue("source");
      }, [Ue, v, $]),
      ks = a.useCallback(
        (T, K, ee) => {
          (Oe(), mr(T, K, ee));
        },
        [mr, Oe],
      ),
      Ir = a.useCallback(
        (T) => {
          (Oe(), fr(T));
        },
        [fr, Oe],
      ),
      js = a.useCallback(
        (T) => {
          (Oe(), hr(T));
        },
        [hr, Oe],
      ),
      ge = Ra({
        column: Xe.column,
        filePath: (y == null ? void 0 : y.path) ?? null,
        isPdfStale: Et,
        jobId: gs,
        line: Xe.line,
        pdfResult: me,
        onNavigateSource: ks,
        onRevealPdf: Ve,
      }),
      { syncSourceToPdf: Rr } = ge,
      Dt = a.useCallback(() => {
        $e(!1);
      }, [$e]),
      Lr = a.useCallback(() => {
        Pt();
      }, [Pt]),
      Er = a.useCallback(() => {
        Rr();
      }, [Rr]),
      Pr = a.useCallback(async () => {
        if (ue != null && ue.outputAttachmentId)
          try {
            const T = await Cr(),
              K = (b == null ? void 0 : b.name) ?? "latex";
            vt(T, `${K}-outputs.zip`);
          } catch (T) {
            W.error(Y(T, "Could not download generated LaTeX files."));
          }
      }, [ue == null ? void 0 : ue.outputAttachmentId, Cr, b]),
      Tr = a.useCallback((T, K) => {
        Zn((ee) =>
          (ee == null ? void 0 : ee.fileName) === T && Xo(ee.diagnostics, K)
            ? ee
            : { fileName: T, diagnostics: K },
        );
      }, []),
      Is = a.useCallback(() => {
        pe.requestDelete({ kind: "project" });
      }, [pe]),
      Rs = a.useCallback(
        (T) => {
          if (!M) throw new Error("A LaTeX project must be selected before managing its files.");
          if (!B) throw new Error("The LaTeX project file actions are unavailable.");
          (g == null || g(M), B.openMenu(T));
        },
        [g, B, M],
      ),
      Mt = a.useCallback(() => {
        if (v) {
          q((T) => ml(T, $));
          return;
        }
        (ae(!1), A(!z));
      }, [v, $, z]),
      Ls = a.useCallback(
        (T) => (pe.requestDelete({ kind: "file", file: T }), Promise.resolve()),
        [pe],
      ),
      Ar = a.useCallback(() => {
        ue && vt(new Blob([ue.log], { type: "text/plain;charset=utf-8" }), "compile.log");
      }, [ue]),
      Dr = a.useCallback(() => {
        Pr();
      }, [Pr]),
      Mr = a.useCallback(
        (T) => {
          const K = Fe.current + 1;
          ((Fe.current = K),
            xo(T.id)
              .then((ee) => {
                var de, Nr;
                if (Fe.current === K) {
                  if (
                    (_e(ee),
                    (de = ee.result) != null &&
                      de.success &&
                      (ee.result.pdfAttachmentId || ee.result.pdfBase64))
                  ) {
                    Ve();
                    return;
                  }
                  he((Nr = ee.result) != null && Nr.diagnostics.length ? "problems" : "log");
                }
              })
              .catch((ee) => {
                Fe.current === K && W.error(Y(ee, "Could not load the selected compilation."));
              }));
        },
        [Ve],
      ),
      Es = a.useCallback(() => {
        H();
      }, [H]),
      Ps = {
        isBusy: He,
        isError: R.isError,
        selectedDocumentId: M,
        onCreateNode: xe.createNode,
        onDeleteNode: Ls,
        onImportProject: xe.importProject,
        onRenameNode: xe.renameNode,
        onSelectFile: pr,
        onSetMainFile: xe.setMainFile,
        onUploadFiles: xe.uploadFiles,
        onRetryLoad: Es,
      },
      Ts = h.jsx(Eo, { ref: x, ...Ps }, M ?? "no-project"),
      As = a.useMemo(
        () =>
          ne && M && X !== null
            ? i({
                documentId: M,
                documentRevision: X,
                onActiveRunChange: fe,
                onEditProposalIdChange: kr,
                onHide: v ? void 0 : At,
                prepareSubmission: Sr,
                refreshProject: Tt,
              })
            : null,
        [X, kr, At, v, Sr, Tt, i, M, ne],
      ),
      Ds = a.useMemo(
        () => ({
          diagnostics: gr,
          draftSource: Ee,
          fileId: (y == null ? void 0 : y.id) ?? null,
          filePath: (y == null ? void 0 : y.path) ?? null,
          fontSizeRem: kt,
          isMainFile: (y == null ? void 0 : y.path) === (b == null ? void 0 : b.mainFilePath),
          historicalSource: wr,
          isSyncing: ge.isForwardSyncing,
          keybindings: St,
          editProposalReview: ot,
          projectContext: jt,
          proposedEdits: jr,
          syncDisabledReason: ge.disabledReason,
          onCompile: Dt,
          onClearHistoricalSource: Se,
          onCreateEditor: ur,
          onLiveDiagnosticsChange: Tr,
          onNavigateOutline: dr,
          onSave: Lr,
          onSaveShortcut: Dt,
          onSourceChange: ar,
          onStatusChange: Yn,
          onSyncPdf: Er,
        }),
        [
          gr,
          Se,
          Ee,
          kt,
          St,
          ot,
          ur,
          Tr,
          dr,
          Dt,
          Lr,
          Er,
          wr,
          jt,
          jr,
          b == null ? void 0 : b.mainFilePath,
          y == null ? void 0 : y.id,
          y == null ? void 0 : y.path,
          ar,
          ge.disabledReason,
          ge.isForwardSyncing,
        ],
      ),
      Ms = a.useMemo(
        () => ({
          canSync: ge.canSync,
          isSyncing: ge.isReverseSyncing,
          pdfAttachmentId: me == null ? void 0 : me.pdfAttachmentId,
          pdfBase64: me == null ? void 0 : me.pdfBase64,
          pdfDownloadFileName: yr,
          isStale: Et,
          pdfRevision: vr,
          syncRequest: ge.pdfRequest,
          onReverseSync: ge.syncPdfToSource,
        }),
        [
          Et,
          me == null ? void 0 : me.pdfAttachmentId,
          me == null ? void 0 : me.pdfBase64,
          vr,
          yr,
          ge.canSync,
          ge.isReverseSyncing,
          ge.pdfRequest,
          ge.syncPdfToSource,
        ],
      ),
      Os = a.useMemo(
        () => ({
          activeTab: ce,
          compileHistory: Le.data ?? gl,
          compileResult: ue,
          diagnostics: Lt,
          editProposalReview: ot,
          isOpen: z,
          selectedCompileJobId: re == null ? void 0 : re.id,
          onActiveTabChange: he,
          onDownloadLog: Ar,
          onDownloadOutputs: Dr,
          onOpenDiagnostic: Ir,
          onSelectCompileJob: Mr,
          onShowPdf: Ve,
          onToggle: Mt,
        }),
        [Le.data, ue, Lt, ce, ot, Ar, Dr, Ir, Mr, Mt, Ve, re == null ? void 0 : re.id, z],
      );
    return V.isPending
      ? h.jsx(U, {
          ref: E,
          sx: Nt,
          children: h.jsx(zs, {
            text: "Preparing LaTeX workspace",
            subtext: "Loading projects, compiler state, and preview tools.",
            tone: "latex",
          }),
        })
      : V.isError
        ? h.jsx(U, {
            ref: E,
            sx: Nt,
            children: h.jsx(Hs, {
              title: "LaTeX workspace unavailable",
              subtitle: "We could not open your LaTeX projects.",
            }),
          })
        : h.jsxs(U, {
            ref: E,
            sx: Nt,
            children: [
              h.jsx(Ro, {
                compiler: (b == null ? void 0 : b.compiler) ?? "pdflatex",
                headerToolbar: s,
                isCancelling: cs,
                isCompiling: ze,
                isCompactLayout: v,
                isCopying: xe.isCopyingProject,
                isFocusLayout: oe,
                isOpeningInOverleaf: xe.isOpeningProjectInOverleaf,
                isOpeningHistory: bt,
                isProjectBusy: He,
                isSaving: ie.status === "saving",
                panelVisibility: n,
                problemsOpen: z,
                selectedDocument: b,
                selectedFilePath: (y == null ? void 0 : y.path) ?? null,
                onCancelCompile: ps,
                onCompile: $e,
                onCompilerChange: xe.changeCompiler,
                onDeleteProject: Is,
                onFocusLayout: Ss,
                onMakeCopy: xe.makeCopy,
                onOpenFileActions: B ? Rs : void 0,
                onOpenEditorSettings: pe.openEditorSettings,
                onOpenKeybindings: pe.openKeybindings,
                onOpenProjectInOverleaf: xe.openProjectInOverleaf,
                onOpenHistory: () => {
                  Cs();
                },
                onRenameProject: pe.openRenameProject,
                onOpenSearch: pe.openProjectSearch,
                onSave: Pt,
                onPrimaryViewChange: Ue,
                onToggleChat: At,
                onToggleProblems: Mt,
              }),
              h.jsx(Lo, {
                chatPanel: As,
                filesLoadError: R.isError,
                isCompactLayout: v,
                recoveryError: ve.error,
                showPdf: D,
                showChat: ne,
                showSource: L,
                workspaceView: Q,
                sourcePanelProps: Ds,
                pdfPanelProps: Ms,
                diagnosticsDockProps: Os,
                onRetryRecovery: ve.retryStorage,
                onWorkspaceViewChange: bs,
              }),
              h.jsx(xi, {
                documentId: M,
                open: Qn,
                onClose: () => {
                  yt(!1);
                },
              }),
              h.jsx(yi, {
                column: Xe.column,
                diagnostics: Lt,
                engine: (b == null ? void 0 : b.compiler) ?? "pdflatex",
                line: Xe.line,
                saveStatus: xr,
                wordCount: Xe.wordCount,
              }),
              Ts,
              h.jsx(cl, {
                currentFilePath: (y == null ? void 0 : y.path) ?? "main.tex",
                dialogs: pe,
                draftSource: Ee,
                editorFontSizePx: va(kt),
                editorKeybindings: St,
                projectActions: xe,
                projectContext: jt,
                selectedDocument: b,
                onEditorFontSizeChange: (T) => {
                  ts($t(T));
                },
                onKeybindingsChange: qn,
                onSearchNavigate: js,
              }),
              h.jsx(ul, {
                conflictCurrentRevision: ie.error instanceof mt ? ie.error.currentRevision : null,
                hasRecoveredDraft: ve.hasRecoveredDraft,
                isConflict: vs,
                isDiscardingRecovery: ve.status === "removing",
                isRecoveryFromCurrentRevision: ve.isRecoveryFromCurrentRevision,
                isSavingBeforeTransition: Me.isSavingBeforeTransition || ie.status === "saving",
                isTransitionBlocked: Me.isBlocked,
                recoveredDraft: ve.recoveredDraft,
                recoveryError: ve.error,
                unsavedError: ve.error ?? Me.error ?? ie.error,
                onCancelUnsavedTransition: as,
                onDiscardRecoveredDraft: ve.discardRecoveredDraft,
                onDiscardUnsavedChanges: () => {
                  (async () => {
                    (ie.pause(),
                      await ie.waitForActiveSave(),
                      await ve.clearRecoveryDraft(),
                      It(),
                      await Me.discardAndProceed());
                  })().catch(() => {});
                },
                onKeepLocalChanges: () => {
                  ie.error instanceof mt &&
                    ie.error.currentRevision !== null &&
                    ss(ie.error.currentRevision);
                },
                onReloadServerCopy: () => {
                  (async () => {
                    (await ve.clearRecoveryDraft(),
                      It(),
                      await Promise.all([V.refetch(), R.refetch()]));
                  })().catch(() => {});
                },
                onRestoreRecoveredDraft: ve.restoreRecoveredDraft,
                onSaveAndContinue: () => {
                  Me.saveAndProceed();
                },
              }),
            ],
          });
  };
export { jc as LatexWorkspaceContent };
