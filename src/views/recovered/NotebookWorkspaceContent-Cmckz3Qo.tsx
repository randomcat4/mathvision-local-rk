import {
  createNotebookDragPlugins as Zr,
  isSortableDragEntity as N,
  notebookDragSensors as Jr,
  useRecoveredSortable as Yr,
} from "../../vendor/recovered/dndKitSortableRuntime";
import {
  E as it,
  r as b,
  j as o,
  h as _Component2,
  x as _Component3,
  g as R,
  e as _Component7,
  B as I,
  T as W,
  _ as A,
  V as _Component12,
  q as ut,
  ad as _Component29,
  ac as _Component28,
  C as _Component26,
} from "./index-BM3ZINIl.js";
import {
  D as ft,
  h as bt,
  n as P,
  r as _e,
  W as ht,
  i as Se,
  j as mt,
  k as je,
  p as xt,
  l as vt,
  m as yt,
  o as kt,
  q as Ct,
  s as Fe,
  t as wt,
  v as Ie,
  w as De,
  x as _t,
  y as St,
  z as jt,
  R as Oe,
  E as It,
  F as qe,
  G as Pe,
  H as Dt,
  I as ce,
  J as Ot,
  K as O,
  L as Pt,
  N as G,
  Q as Et,
  U as ue,
  V as Rt,
  X as At,
  Y as Tt,
  Z as Nt,
  _ as _Component15,
  $ as Ee,
  a0 as Lt,
  a1 as Mt,
  a2 as Bt,
} from "./ChatPanelMoreMenu-B1X9SOWN.js";
import { i as Wt } from "./threadMessageListLazy-ghC-RLRq.js";
import {
  N as Gt,
  h as Ue,
  u as $t,
  a as Vt,
  b as Kt,
  c as Ht,
  d as Ft,
  e as qt,
} from "./route.lazy-B7bltzFg.js";
import { g as Ut } from "./ThinkingElapsedLabel-DKCKD95O.js";
import { b as Qt } from "./MarkdownHighlights-C-ZhT8z1.js";
import { b as Yt, I as Xt } from "./TextField-D8vc_sXz.js";
import { L as Jt } from "./ListItemIcon-CaLBil7Q.js";
import { L as Zt } from "./ListItemText-CBxudZz_.js";
import {
  d as _Component0,
  n as _Component1,
  L as _Component10,
} from "./assistantStructuredResponseLazy-CCAemx3W.js";
import { f as Qe } from "./SupportingArtifactCard-B1XjYGZI.js";
import { a as nr } from "./useAttachmentContent-15uaQfvd.js";
import "./ChatInteractionOverrides-dyfOoEnd.js";
import "./AgentTaskProgress-bXMst_Kr.js";
import { a as _Component8 } from "./Markdown-Dh8LhaRc.js";
import "./MessageBubble-DqxHrk03.js";
import {
  V as _Component4,
  O as _Component5,
} from "./ViewportVisualizationFigure-BIlJdDlH.js";
import { d as ir } from "./downloadBlob-CusuPFei.js";
import { D as _Component6 } from "./katex.min-ADdKj7O-.js";
import { A as Y } from "./Alert-DnFVD8li.js";
import { G as _Component11 } from "./grip-vertical-DCI0_0hx.js";
import { P as _Component14 } from "./play-3zl_nde-.js";
import {
  d as _Component16,
  D as _Component25,
  a as _Component22,
  b as _Component23,
  c as _Component24,
} from "./MChip-DiH8NXBU.js";
import { S as _Component17 } from "./save-Bre0AB4-.js";
import { j as _Component18, N as Re } from "./SidebarTreeConnector-DsRNfBIo.js";
import { C as _Component21 } from "./OpenInNewRounded-D-7V5TzF.js";
import { u as vr } from "./useBlocker-Cad78cFE.js";
import "./useMutation-C1SgG9wr.js";
import "./chatsApi-Ck-JYICO.js";
import "./chatQueryCache-DOdNJrP_.js";
import "./FormControlLabel-CQvCddOM.js";
import "./useIsMobile-SdtLFChL.js";
import "./DebouncedTextField-DEJkKI5N.js";
import "./useGetUserType-DlzkERhz.js";
import "./useQuery-DM2qiUYb.js";
import "./search-CE89ACSU.js";
import "./WorkspaceSidePanelFrame-BElEFQhq.js";
import "./useResizablePanelWidth-DfMwOMGi.js";
import "./useResizablePanelValue-D_NwbF9E.js";
import "./LogoutRounded-BsETu1qq.js";
import "./LinkOffRounded-CKGtm4_4.js";
import "./attachmentsApi-BHgxfYft.js";
import "./authFetch-DXjDztUP.js";
import "./Divider-BcuihGce.js";
import "./AlertTitle-B2wnIUF2.js";
import "./LoadingSection-wWBu2CqJ.js";
import "./chatMessageSelectors-CRUAC26Y.js";
import "./Badge-b-klHfWK.js";
import "./cache-BNzcfneg.js";
import "./file-code-corner-DV79SyC0.js";
import "./streamNdjson-sRiOwtSP.js";
import "./circle-DSozMRKK.js";
import "./FormGroup-BsLWbxpH.js";
import "./TimeRelative-HYP_-RcH.js";
import "./panel-left-close-BNWX01EL.js";
import "./useInfiniteQuery-CdNTitqC.js";
import "./useLocation-BxGXieoN.js";
import "./Drawer-ByPJ5WV0.js";
import "./latexWorkspacePanelVisibility-BotBQ6b-.js";
import "./panel-left-open-BNgbA-Qm.js";
import "./CopyButton-BcOsqrNJ.js";
import "./useLatexQueries-DBpKOPUv.js";
import "./pencil-DHia85Iy.js";
import "./index-Yb6ONXKu.js";
import "./triangle-alert-C5eP7BuB.js";
import "./circle-check-B9fquZbm.js";
import "./code-xml-DmfSENe3.js";
import "./Card-Bf0WPnf4.js";
import "./StructuredMarkdown-DAWLLRpf.js";
import "./index-DzMT-bSF.js";
import "./file-input-BGVdGffy.js";
import "./panel-right-close-BXzsVzHe.js";
import "./chevron-left-BwEFN4zp.js";
import "./minimize-2-DiBwEVni.js";
import "./maximize-2-B07z4fzS.js";
import "./zoom-in-uCZ29dqq.js";
import "./square-D0gg5_xj.js";
import "./eraser-B4pclN2p.js";
import "./Slider-v2yN6a4G.js";
/**
 * @license lucide-react v1.16.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const yr = [
  [
    "path",
    {
      d: "M12 16v5",
      key: "zza2cw",
    },
  ],
  [
    "path",
    {
      d: "M16 14.639V21",
      key: "1s85h0",
    },
  ],
  [
    "path",
    {
      d: "M20 10.656V21",
      key: "q45596",
    },
  ],
  [
    "path",
    {
      d: "m22 3-8.646 8.646a.5.5 0 0 1-.708 0L9.354 8.354a.5.5 0 0 0-.707 0L2 15",
      key: "1fw8x9",
    },
  ],
  [
    "path",
    {
      d: "M4 18.463V21",
      key: "1otddq",
    },
  ],
  [
    "path",
    {
      d: "M8 14.656V21",
      key: "1t2idw",
    },
  ],
];
const _Component = it("chart-no-axes-combined", yr);
const en = "notebook-cell";
const tn = (e, t, r) => {
  if (
    !Number.isInteger(t) ||
    !Number.isInteger(r) ||
    t < 0 ||
    r < 0 ||
    t >= e.length ||
    r >= e.length
  ) {
    throw new Error("Notebook cell drag indexes are outside the cell list.");
  }
  const n = [...e];
  const [a] = n.splice(t, 1);
  if (!a) {
    throw new Error("The dragged notebook cell no longer exists.");
  }
  n.splice(r, 0, a);
  return n;
};
const _Component20 = ({ cellIds: e, children: t, onReorder: r }) => {
  const n = b.useCallback(
    ({ canceled: a, operation: s }) => {
      if (a || !s.source) {
        return;
      }
      if (!N(s.source)) {
        throw new Error("Notebook drag source is not a sortable cell.");
      }
      const { id: i, initialIndex: c, index: f } = s.source;
      if (e[c] !== i) {
        throw new Error("Notebook drag source does not match the cell list.");
      }
      if (c !== f) {
        r(tn(e, c, f));
      }
    },
    [e, r],
  );
  return (
    <Rt plugins={Zr} sensors={Jr} onDragEnd={n}>
      {t}
    </Rt>
  );
};
const nn = (e, t, r) =>
  Yr({
    id: e,
    index: t,
    type: en,
    disabled: r,
  });
const me = {
  compute: {
    label: "Compute",
    promptPlaceholder:
      "Describe a calculation to solve, such as: Find the roots of x³ - 2x - 5 and verify them.",
  },
  visualize: {
    label: "Visualize",
    promptPlaceholder:
      "Describe what to visualize, such as: Plot sin(x) and cos(x) from -2π to 2π and mark their intersections.",
  },
};
const Ve = ({ operation: e, size: t }) =>
  e === "compute" ? <Ut size={t} /> : <_Component size={t} />;
const _Component13 = ({ disabled: e, operation: t, onChange: r }) => {
  const n = b.useId();
  const [a, s] = b.useState(null);
  const i = me[t];
  const c = a !== null;
  return (
    <o.Fragment>
      <_Component2
        size="small"
        color="secondary"
        disabled={e}
        aria-controls={c ? n : undefined}
        aria-expanded={c ? true : undefined}
        aria-haspopup="menu"
        endIcon={<Qt size={15} />}
        startIcon={<Ve operation={t} size={17} />}
        onClick={(f) => {
          s(f.currentTarget);
        }}
      >
        {i.label}
      </_Component2>
      <Yt id={n} anchorEl={a} open={c} onClose={() => s(null)}>
        {Gt.map((f) => (
          <_Component3
            selected={f === t}
            onClick={() => {
              s(null);
              r(f);
            }}
            key={f}
          >
            <Jt>
              <Ve operation={f} size={17} />
            </Jt>
            <Zt>{me[f].label}</Zt>
          </_Component3>
        ))}
      </Yt>
    </o.Fragment>
  );
};
const oe = (e) => ({
  position: "relative",
  flex: 1,
  minWidth: 0,
  minHeight: 0,
  display: "flex",
  flexDirection: "column",
  overflow: "hidden",
  ...Nt(e),
});
const sn = (e) => ({
  flex: 1,
  minHeight: 0,
  minWidth: 0,
  overflowY: "auto",
  padding: e.spacing(4, 2, 8),
  [e.breakpoints.down("sm")]: {
    padding: e.spacing(2, 1, 5),
  },
});
const an = (e) => ({
  width: "min(100%, 72rem)",
  minWidth: 0,
  margin: "0 auto",
  display: "flex",
  flexDirection: "column",
  gap: e.spacing(1.25),
});
const ln = (e, t, r) => ({
  position: "relative",
  minWidth: 0,
  display: "flex",
  flexDirection: "column",
  border: `1px solid ${r ? R(e.palette.error.main, 0.5) : t ? R(e.palette.primary.main, 0.55) : R(e.palette.text.primary, 0.1)}`,
  borderRadius: e.shape.borderRadiusPx,
  backgroundColor: R(e.palette.background.paper, 0.88),
  boxShadow: t ? `0 0 0 3px ${R(e.palette.primary.main, 0.08)}` : e.shadows[1],
  transition: e.transitions.create(["border-color", "box-shadow"], {
    duration: e.transitions.duration.shortest,
  }),
  overflow: "hidden",
});
const dn = {
  minWidth: 0,
  display: "flex",
  flexDirection: "column",
};
const cn = (e) => ({
  minHeight: "2.7rem",
  minWidth: 0,
  display: "flex",
  flexWrap: "wrap",
  alignItems: "center",
  justifyContent: "space-between",
  gap: e.spacing(1),
  padding: e.spacing(0.45, 0.75, 0.4, 1.1),
  borderBottom: `1px solid ${R(e.palette.text.primary, 0.07)}`,
});
const un = {
  minWidth: 0,
  display: "flex",
  flexWrap: "wrap",
  alignItems: "center",
  justifyContent: "flex-end",
  gap: "0.2rem",
};
const pn = {
  minWidth: 0,
  display: "flex",
  flexWrap: "wrap",
  alignItems: "center",
  gap: "0.5rem",
};
const gn = {
  flex: "0 0 auto",
  display: "flex",
  alignItems: "center",
  gap: "0.5rem",
};
const fn = (e) => ({
  flex: "0 0 auto",
  cursor: e ? "default" : "grab",
  touchAction: "none",
  ...(!e && {
    "&:active": {
      cursor: "grabbing",
    },
  }),
});
const bn = (e) => ({
  minHeight: "6rem",
  alignItems: "flex-start",
  padding: e.spacing(1.5),
  "& .MuiInputBase-input": {
    padding: 0,
  },
});
const hn = (e) => ({
  minWidth: 0,
  display: "flex",
  flexDirection: "column",
  gap: e.spacing(1),
  padding: e.spacing(1.4, 1.5, 1.5),
  borderTop: `1px solid ${R(e.palette.text.primary, 0.07)}`,
});
const Ke = (e) => ({
  display: "flex",
  flexWrap: "wrap",
  alignItems: "center",
  justifyContent: "center",
  gap: e.spacing(0.5),
  padding: e.spacing(0.5),
  opacity: 0.72,
  transition: e.transitions.create("opacity", {
    duration: e.transitions.duration.shortest,
  }),
  "&:hover, &:focus-within": {
    opacity: 1,
  },
});
const mn = (e) => ({
  minHeight: "18rem",
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  alignItems: "center",
  gap: e.spacing(2),
  padding: e.spacing(4),
  border: `1px dashed ${R(e.palette.text.primary, 0.14)}`,
  borderRadius: e.shape.borderRadiusPx,
  backgroundColor: R(e.palette.background.paper, 0.5),
  textAlign: "center",
});
const _Component19 = ({
  ariaLabel: e,
  disabled: t,
  placeholder: r,
  value: n,
  onBlur: a,
  onChange: s,
  onFocus: i,
  onRunAndAdvance: c,
  onRunInPlace: f,
  onSave: x,
}) => {
  const g = (p) => {
    if (t || p.nativeEvent.isComposing) {
      return;
    }
    const m = p.metaKey || p.ctrlKey;
    if (p.key === "Enter" && p.shiftKey) {
      p.preventDefault();
      c();
      return;
    }
    if (p.key === "Enter" && m) {
      p.preventDefault();
      f();
      return;
    }
    if (p.key.toLowerCase() === "s" && m) {
      p.preventDefault();
      x();
    }
  };
  return (
    <Xt
      fullWidth={true}
      multiline={true}
      minRows={3}
      maxRows={12}
      disabled={t}
      placeholder={r}
      value={n}
      slotProps={{
        input: {
          "aria-label": e,
        },
      }}
      sx={bn}
      onBlur={a}
      onChange={(p) => s(p.target.value)}
      onFocus={i}
      onKeyDown={g}
    />
  );
};
const vn = ({ persistedSource: e, onSave: t }) => {
  const [r, n] = b.useState(e);
  const [a, s] = b.useState("saved");
  const i = b.useRef(e);
  const c = b.useRef(r);
  const f = b.useRef(null);
  const x = b.useRef(t);
  x.current = t;
  c.current = r;
  b.useEffect(() => {
    const m = i.current;
    i.current = e;
    n((v) => (v === m ? e : v));
    if (c.current === m) {
      s("saved");
    }
  }, [e]);
  const g = b.useCallback(async () => {
    const m = c.current;
    if (m === i.current) {
      s("saved");
      return;
    }
    if (f.current) {
      await f.current;
      if (c.current !== i.current) {
        return g();
      } else {
        return undefined;
      }
    }
    s("saving");
    const v = x
      .current(m)
      .then(() => {
        i.current = m;
        s(c.current === m ? "saved" : "unsaved");
      })
      .catch((k) => {
        s("error");
        throw k;
      })
      .finally(() => {
        f.current = null;
      });
    f.current = v;
    await v;
  }, []);
  const p = b.useCallback((m) => {
    c.current = m;
    n(m);
    s(m === i.current ? "saved" : "unsaved");
  }, []);
  return {
    source: r,
    saveStatus: a,
    hasUnsavedChanges: a !== "saved",
    setSource: p,
    save: g,
  };
};
const yn = {
  minWidth: 0,
  display: "flex",
  flexDirection: "column",
  gap: 1,
  padding: 1.25,
};
const kn = {
  minWidth: 0,
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  gap: 1,
};
const Cn = {
  minWidth: 0,
  display: "flex",
  alignItems: "center",
  gap: 0.75,
};
const _Component9 = ({ figureNumber: e, output: t }) => {
  const r = t.artifactKind !== "file";
  const { load: n } = nr(t.attachmentId, false);
  const a = b.useCallback(async () => {
    const i = await n();
    ir(i, t.name);
  }, [n, t.name]);
  const s = {
    key: `notebook-artifact:${t.attachmentId}`,
    originatingCallId: null,
    attachmentId: t.attachmentId,
    name: t.name,
    mimeType: t.mimeType,
    sizeBytes: t.sizeBytes,
    href: null,
    contentError: null,
    omittedReason: null,
    preview: Qe(t.preview),
    kind: t.artifactKind,
  };
  if (r) {
    return (
      <_Component4
        altText={t.altText ?? t.title ?? t.name}
        artifact={s}
        caption={t.title ?? t.name}
        figureNumber={e}
        mathSyntax="canonical"
        pythonCode={null}
      />
    );
  } else {
    return (
      <_Component7 variant="inset" sx={yn}>
        <I sx={kn}>
          <I sx={Cn}>
            <_Component5 aria-hidden={true} size={18} />
            <W variant="subtitle2" noWrap={true}>
              {t.title ?? t.name}
            </W>
          </I>
          <A
            size="small"
            color="secondary"
            startIcon={<_Component6 size={16} />}
            onClick={() => {
              a();
            }}
          >
            Download
          </A>
        </I>
        <W variant="body2" color="textSecondary">
          Generated file
        </W>
      </_Component7>
    );
  }
};
const _n = (e, t) => {
  switch (e.kind) {
    case "markdown":
      return (
        <_Component8
          content={e.markdown}
          mathSyntax="canonical"
          showCodeCopyButton={true}
        />
      );
    case "artifact":
      return <_Component9 output={e} figureNumber={t} />;
    case "error":
      return (
        <Y severity="error" variant="outlined">
          {e.message}
        </Y>
      );
  }
};
const Sn = ({ areVariableHighlightsEnabled: e, run: t }) => {
  var x;
  if (!t) {
    return null;
  }
  const r = t.status === "running";
  const n = t.status === "canceled";
  if (
    !r &&
    !n &&
    t.outputs.length === 0 &&
    !t.errorMessage &&
    !t.structuredResponse
  ) {
    return null;
  }
  const a = t.outputs.some((g) => g.kind === "error");
  const s = Date.parse(t.startedAt);
  if (Number.isNaN(s)) {
    throw new Error("Notebook run started_at must be a valid timestamp.");
  }
  const i = t.outputs.map((g, p) => <I key={`${t.id}:${p}`}>{_n(g, p + 1)}</I>);
  const c = t.outputs.flatMap((g) =>
    g.kind === "artifact"
      ? [
          {
            key: `notebook-artifact:${g.attachmentId}`,
            originatingCallId: null,
            attachmentId: g.attachmentId,
            name: g.name,
            mimeType: g.mimeType,
            sizeBytes: g.sizeBytes,
            href: null,
            contentError: null,
            omittedReason: null,
            preview: Qe(g.preview),
            kind: g.artifactKind,
          },
        ]
      : [],
  );
  const f = t.status === "completed" ? t.structuredResponse : null;
  return (
    <I aria-live="polite" sx={hn}>
      {r && (
        <_Component0
          ariaLive="off"
          headingLabel="Reasoning"
          runId={t.id}
          startedAt={s}
          statusLabel="Running operation..."
          streamText={t.reasoning ?? ""}
        />
      )}
      {!r && ((x = t.reasoning) == null ? undefined : x.trim()) && (
        <_Component1
          reasoning={t.reasoning}
          durationSeconds={t.durationMs === null ? null : t.durationMs / 1000}
        />
      )}
      {n && (
        <Y severity="info" variant="outlined">
          Run paused.
        </Y>
      )}
      {f && (
        <b.Suspense fallback={null}>
          <_Component10
            appearance="notebook"
            messageIndex={t.executionCount}
            responseNumber={t.executionCount}
            response={f}
            toolCalls={t.toolCalls}
            computationArtifacts={c}
            visualizationArtifacts={c}
            markdownMathFormat="canonical-v1"
            areCitationPreviewsEnabled={false}
            areVariableHighlightsEnabled={e}
            areWriteupActionsEnabled={false}
            sectionIdPrefix={`notebook-run-${t.id}`}
            sectionsAriaLabel="Notebook result sections"
          />
        </b.Suspense>
      )}
      {!f && i}
      {t.errorMessage && !a && (
        <Y severity="error" variant="outlined">
          {t.errorMessage}
        </Y>
      )}
    </I>
  );
};
const jn = (e) =>
  e === "saving"
    ? "Saving"
    : e === "unsaved"
      ? "Unsaved"
      : e === "error"
        ? "Save failed"
        : "Saved";
const In = b.forwardRef(function (
  {
    areVariableHighlightsEnabled: t,
    cell: r,
    isBusy: n,
    isDragDisabled: a,
    sortableIndex: s,
    onDelete: i,
    onDirtyChange: c,
    onPause: f,
    onRun: x,
    onRunAndAdvance: g,
    onSave: p,
    onOperationChange: m,
  },
  v,
) {
  var Ce;
  const [k, y] = b.useState(false);
  const [C, u] = b.useState(false);
  const _ = b.useCallback((E) => p(r.id, E), [r.id, p]);
  const l = vn({
    persistedSource: r.source,
    onSave: _,
  });
  const h = l.save;
  b.useImperativeHandle(
    v,
    () => ({
      save: h,
    }),
    [h],
  );
  b.useEffect(() => {
    c(r.id, l.hasUnsavedChanges);
    return () => c(r.id, false);
  }, [r.id, l.hasUnsavedChanges, c]);
  const d = b.useCallback(
    async (E) => {
      u(true);
      try {
        await h();
        await E();
      } finally {
        u(false);
      }
    },
    [h],
  );
  const w = b.useCallback(() => d(() => x(r.id)), [r.id, x, d]);
  const S = b.useCallback(() => d(() => g(r.id)), [r.id, g, d]);
  const j = r.latestRun;
  const T = (j == null ? undefined : j.status) === "running";
  const L = b.useCallback(async () => {
    if (!j || j.status !== "running") {
      throw new Error("Only a running notebook cell can be paused.");
    }
    u(true);
    try {
      await f(r.id, j.id);
    } finally {
      u(false);
    }
  }, [r.id, f, j]);
  const D = n || C;
  const M = a || D;
  const { ref: J, handleRef: le } = nn(r.id, s, M);
  const at =
    (j == null ? undefined : j.status) === "failed" ||
    (j == null ? undefined : j.outputs.some((E) => E.kind === "error")) ===
      true;
  const ke = me[r.operation];
  return (
    <I ref={J} data-notebook-cell-id={r.id} sx={(E) => ln(E, k, at)}>
      <I sx={dn}>
        <I sx={cn}>
          <I sx={pn}>
            <_Component12
              ref={le}
              size="small"
              tooltip="Drag to reorder cell"
              aria-label="Reorder cell"
              disabled={M}
              sx={fn(M)}
            >
              <_Component11 size={17} />
            </_Component12>
            <I sx={gn}>
              <_Component13
                disabled={D}
                operation={r.operation}
                onChange={(E) => {
                  d(() => m(r.id, E));
                }}
              />
              {!T && (
                <A
                  size="small"
                  variant="contained"
                  startIcon={<_Component14 size={15} fill="currentColor" />}
                  disabled={D || !l.source.trim()}
                  onClick={() => {
                    w();
                  }}
                >
                  Run
                </A>
              )}
              {T && (
                <A
                  size="small"
                  color="error"
                  variant="contained"
                  startIcon={<_Component15 fontSize="small" />}
                  disabled={C}
                  aria-label="Pause run"
                  onClick={() => {
                    L();
                  }}
                >
                  {C ? "Pausing" : "Pause"}
                </A>
              )}
            </I>
            <W
              variant="body2"
              color={
                l.saveStatus === "error"
                  ? "error"
                  : l.saveStatus === "unsaved"
                    ? "warning"
                    : "textSecondary"
              }
            >
              {jn(l.saveStatus)}
            </W>
            {r.isStale &&
              ((Ce = r.latestRun) == null ? undefined : Ce.status) ===
                "completed" && (
                <_Component16
                  size="small"
                  color="warning"
                  label="Result outdated"
                />
              )}
          </I>
          <I sx={un}>
            <_Component12
              size="medium"
              tooltip="Save step"
              aria-label="Save step"
              disabled={l.saveStatus === "saved" || D}
              onClick={() => {
                l.save();
              }}
            >
              <_Component17 size={19} />
            </_Component12>
            <_Component12
              size="medium"
              color="error"
              tooltip="Delete step"
              aria-label="Delete step"
              disabled={D}
              onClick={() => {
                i(r.id);
              }}
            >
              <_Component18 size={19} />
            </_Component12>
          </I>
        </I>
        <_Component19
          ariaLabel={`${ke.label} prompt`}
          disabled={D}
          placeholder={ke.promptPlaceholder}
          value={l.source}
          onBlur={() => {
            y(false);
          }}
          onChange={l.setSource}
          onFocus={() => y(true)}
          onRunAndAdvance={() => {
            S();
          }}
          onRunInPlace={() => {
            w();
          }}
          onSave={() => {
            h();
          }}
        />
        <Sn areVariableHighlightsEnabled={t} run={r.latestRun} />
      </I>
    </I>
  );
});
const He = (e) => {
  window.requestAnimationFrame(() => {
    var r;
    const t = document.querySelector(
      `[data-notebook-cell-id="${CSS.escape(e)}"]`,
    );
    if ((r = t == null ? undefined : t.querySelector("textarea")) != null) {
      r.focus();
    }
  });
};
const Dn = (e, t) => {
  const r = new Set(e.cells.map((a) => a.id));
  const n = t.cells.find((a) => !r.has(a.id));
  if (!n) {
    throw new Error("The notebook response did not include the created step.");
  }
  return n.id;
};
const On = b.forwardRef(function (
  {
    actions: t,
    areVariableHighlightsEnabled: r,
    notebook: n,
    onCellDirtyChange: a,
  },
  s,
) {
  const i = b.useRef(new Map());
  const [c, f] = b.useState(null);
  const [x, g] = b.useState(null);
  const p = n.cells;
  const m = Ue(n);
  const v = b.useMemo(() => {
    if (!x) {
      return p;
    }
    if (x.length !== p.length) {
      throw new Error("Optimistic notebook cell order has the wrong length.");
    }
    const d = new Map(p.map((w) => [w.id, w]));
    return x.map((w) => {
      const S = d.get(w);
      if (!S) {
        throw new Error(`Optimistic notebook cell '${w}' no longer exists.`);
      }
      return S;
    });
  }, [p, x]);
  const k = b.useMemo(() => v.map((d) => d.id), [v]);
  const y = t.isBusy || m || x !== null;
  const C = b.useCallback(
    async (d) => {
      var w;
      for (const S of p) {
        if (S.id !== d) {
          await ((w = i.current.get(S.id)) == null ? undefined : w.save());
        }
      }
    },
    [p],
  );
  b.useImperativeHandle(
    s,
    () => ({
      saveAllCells: C,
    }),
    [C],
  );
  const u = b.useCallback(async () => {
    await C();
    const d = await t.createCell("compute");
    const w = Dn(n, d);
    He(w);
    return d;
  }, [t, n, C]);
  const _ = b.useCallback(
    async (d) => {
      const w = p.findIndex((j) => j.id === d);
      if (w < 0) {
        throw new Error("The active notebook step no longer exists.");
      }
      await t.runCell(d);
      const S = p[w + 1];
      if (S) {
        He(S.id);
        return;
      }
      await u();
    },
    [t, p, u],
  );
  const l = b.useCallback(
    async (d) => {
      g(d);
      try {
        await t.reorderCells(d);
      } finally {
        g(null);
      }
    },
    [t],
  );
  const h = b.useCallback(async () => {
    if (c) {
      await C(c);
      await t.deleteCell(c);
      f(null);
    }
  }, [t, c, C]);
  return (
    <I sx={an}>
      {t.conflict && (
        <Y
          severity="warning"
          variant="outlined"
          action={
            <A size="small" color="warning" onClick={t.clearConflict}>
              Dismiss
            </A>
          }
        >
          {t.conflict.message}
        </Y>
      )}
      <_Component20 cellIds={k} onReorder={l}>
        {v.map((d, w) => (
          <In
            ref={(S) => {
              if (S) {
                i.current.set(d.id, S);
              } else {
                i.current.delete(d.id);
              }
            }}
            cell={d}
            areVariableHighlightsEnabled={r}
            isBusy={y}
            isDragDisabled={y || v.length < 2}
            sortableIndex={w}
            onDelete={async (S) => f(S)}
            onDirtyChange={a}
            onPause={t.cancelRun}
            onRun={t.runCell}
            onRunAndAdvance={_}
            onSave={(S, j) =>
              t.updateCell(S, {
                source: j,
              })
            }
            onOperationChange={(S, j) =>
              t.updateCell(S, {
                operation: j,
              })
            }
            key={d.id}
          />
        ))}
      </_Component20>
      {p.length === 0 && (
        <I sx={mn}>
          <W variant="h5">What would you like to do?</W>
          <W variant="body1" color="textSecondary">
            Describe the math in plain language. The notebook handles the
            computation tools for you.
          </W>
          <I sx={Ke}>
            <A
              variant="contained"
              startIcon={<Ee size={16} />}
              disabled={y}
              onClick={() => {
                u();
              }}
            >
              Add cell
            </A>
          </I>
        </I>
      )}
      {p.length > 0 && (
        <I sx={Ke}>
          <A
            size="small"
            color="secondary"
            startIcon={<Ee size={16} />}
            disabled={y}
            onClick={() => {
              u();
            }}
          >
            Add cell
          </A>
        </I>
      )}
      <_Component21
        open={c !== null}
        title="Delete this step?"
        message="The prompt and its saved result will be removed from this notebook."
        confirmLabel="Delete step"
        pendingConfirmLabel="Deleting"
        confirmColor="error"
        isPending={t.isBusy}
        onClose={() => f(null)}
        onConfirm={h}
      />
    </I>
  );
});
const Pn = ({
  errorMessage: e,
  isSaving: t,
  open: r,
  onDiscard: n,
  onSaveAndContinue: a,
  onStay: s,
}) => (
  <_Component25
    open={r}
    aria-labelledby="notebook-unsaved-changes-title"
    onClose={t ? undefined : s}
  >
    <_Component22 id="notebook-unsaved-changes-title">
      Save notebook changes?
    </_Component22>
    <_Component23>
      <W variant="body2" color="textSecondary">
        Some step or title edits have not reached the server. Save them before
        leaving, or explicitly discard the local drafts.
      </W>
      {e && (
        <Y severity="error" variant="outlined">
          {e}
        </Y>
      )}
    </_Component23>
    <_Component24>
      <A variant="outlined" disabled={t} onClick={s}>
        Stay here
      </A>
      <A color="error" disabled={t} onClick={n}>
        Discard and continue
      </A>
      <A variant="contained" disabled={t} onClick={a}>
        {t ? "Saving…" : "Save and continue"}
      </A>
    </_Component24>
  </_Component25>
);
const En = ({ hasUnsavedChanges: e, saveChanges: t }) => {
  const [r, n] = b.useState(false);
  const [a, s] = b.useState(null);
  const i = b.useRef(e);
  i.current = e;
  const c = b.useCallback(() => i.current, []);
  const f = vr({
    shouldBlockFn: c,
    enableBeforeUnload: c,
    withResolver: true,
  });
  const x = b.useCallback(() => {
    s(null);
    if (f.status === "blocked") {
      f.reset();
    }
  }, [f]);
  const g = b.useCallback(() => {
    s(null);
    if (f.status === "blocked") {
      f.proceed();
    }
  }, [f]);
  const p = b.useCallback(async () => {
    if (f.status === "blocked") {
      n(true);
      s(null);
      try {
        await t();
        f.proceed();
      } catch (m) {
        s(ut(m));
      } finally {
        n(false);
      }
    }
  }, [f, t]);
  b.useEffect(() => {
    if (!e && f.status === "blocked") {
      f.proceed();
    }
  }, [f, e]);
  return {
    isBlocked: f.status === "blocked",
    isSaving: r,
    errorMessage: a,
    stay: x,
    discardAndProceed: g,
    saveAndProceed: p,
  };
};
const Rn = {
  flex: "0 0 auto",
  display: "flex",
  alignItems: "center",
  gap: 0.7,
};
const _Component27 = ({
  breadcrumbItems: e,
  currentItemContent: t,
  headerToolbar: r,
  onReveal: n,
  status: a,
  toolbarItems: s,
}) => (
  <Vt
    actions={
      <Kt
        revealAriaLabel="Reveal active notebook in explorer"
        selectedChat={null}
        toolbarItems={s}
        onReveal={n}
      />
    }
    ariaLabel="Notebook workspace toolbar"
    leading={r}
    identity={
      <Lt
        ariaLabel="Current notebook path"
        currentItemIcon="notebook"
        currentItemContent={t}
        items={e}
        variant="toolbar"
        overflowMenuAriaLabel="Previous notebook folder levels"
        pathLevelLabel="notebook folder"
      />
    }
    status={a}
    statusAriaLabel={a ? "Notebook status" : undefined}
  />
);
const An = ({ headerToolbar: e, notebook: t, onReveal: r }) => {
  const n = Ht(t);
  const a = Wt(t.folderId);
  const s = b.useRef(null);
  const [i, c] = b.useState(() => new Set());
  const [f, x] = b.useState(true);
  const g = n.isRunning || Ue(t);
  const p = b.useMemo(
    () => [
      ...(a.data ?? []).map(({ folder: u }) => ({
        id: u.id,
        label: u.name,
        isCurrent: false,
        isSelectable: false,
      })),
      {
        id: t.id,
        label: t.title,
        isCurrent: true,
      },
    ],
    [a.data, t.id, t.title],
  );
  const m = b.useCallback(
    async (u) => {
      const _ = u.trim();
      if (_) {
        if (_ !== t.title) {
          await n.updateNotebook({
            title: _,
          });
        }
        return true;
      } else {
        return false;
      }
    },
    [n, t.title],
  );
  const v = b.useCallback((u, _) => {
    c((l) => {
      if (l.has(u) === _) {
        return l;
      }
      const h = new Set(l);
      if (_) {
        h.add(u);
      } else {
        h.delete(u);
      }
      return h;
    });
  }, []);
  const k = b.useCallback(async () => {
    var u;
    await ((u = s.current) == null ? undefined : u.saveAllCells());
  }, []);
  const y = b.useCallback(() => {
    x((u) => !u);
  }, []);
  const C = En({
    hasUnsavedChanges: i.size > 0,
    saveChanges: k,
  });
  if (t.folderId && a.isError) {
    throw new Error("The notebook folder path could not be loaded.", {
      cause: a.error,
    });
  }
  return (
    <o.Fragment>
      <_Component27
        breadcrumbItems={p}
        currentItemContent={
          <Ft
            editAriaLabel="Edit notebook title"
            inputAriaLabel="Notebook title"
            isPending={n.isBusy}
            maxLength={qt}
            title={t.title}
            onRename={m}
          />
        }
        headerToolbar={e}
        onReveal={r}
        toolbarItems={
          <o.Fragment>
            <Mt
              areVariableHighlightsEnabled={f}
              scopeLabel="this notebook"
              onToggle={y}
            />
            <Bt id={`notebook-bookmarks-button-${t.id}`} />
          </o.Fragment>
        }
        status={
          <I sx={Rn}>
            {g && <_Component26 size={17} />}
            <W variant="caption" color="textSecondary">
              {g ? "Running notebook" : n.isBusy ? "Saving notebook" : "Saved"}
            </W>
          </I>
        }
      />
      <I sx={sn}>
        <On
          ref={s}
          notebook={t}
          actions={n}
          areVariableHighlightsEnabled={f}
          onCellDirtyChange={v}
        />
      </I>
      <Pn
        open={C.isBlocked}
        isSaving={C.isSaving}
        errorMessage={C.errorMessage}
        onDiscard={C.discardAndProceed}
        onSaveAndContinue={() => {
          C.saveAndProceed();
        }}
        onStay={C.stay}
      />
    </o.Fragment>
  );
};
const NotebookWorkspaceContent = ({
  headerToolbar: e,
  onReveal: t,
  requestedNotebookId: r,
}) => {
  const n = $t(r);
  if (r) {
    if (n.isPending) {
      return (
        <I sx={oe}>
          <_Component27
            breadcrumbItems={[
              {
                id: "opening-notebook",
                label: "Opening notebook",
                isCurrent: true,
              },
            ]}
            headerToolbar={e}
            onReveal={t}
          />
          <_Component28
            text="Opening notebook"
            subtext="Preparing steps and saved results."
            tone="workspace"
          />
        </I>
      );
    } else if (n.isError || !n.data) {
      return (
        <I sx={oe}>
          <_Component27
            breadcrumbItems={[
              {
                id: "notebook-unavailable",
                label: "Notebook unavailable",
                isCurrent: true,
              },
            ]}
            headerToolbar={e}
            onReveal={t}
          />
          <_Component29
            title="Notebook unavailable"
            subtitle="This notebook could not be loaded. It may have been deleted or you may not have access."
            icon={<Re size={30} />}
          />
        </I>
      );
    } else {
      return (
        <I sx={oe}>
          <An
            headerToolbar={e}
            notebook={n.data}
            onReveal={t}
            key={n.data.id}
          />
        </I>
      );
    }
  } else {
    return (
      <I sx={oe}>
        <_Component27
          breadcrumbItems={[
            {
              id: "notebooks",
              label: "Notebooks",
              isCurrent: true,
            },
          ]}
          headerToolbar={e}
          onReveal={t}
        />
        <_Component29
          title="Select a notebook"
          subtitle="Choose a notebook from the Explorer, or use Add notebook in the header to create one."
          icon={<Re size={30} />}
        />
      </I>
    );
  }
};
export { NotebookWorkspaceContent };
